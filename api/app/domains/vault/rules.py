"""Rule IDs this module owns. Authoritative logic lives in `dif/rules/rules.yaml`."""

VAULT_RULES = {
    "VAULT-001": "A stored document failed its SHA-256 integrity check (possible tampering).",
}
