# Copy to infra/.env and fill in. Never commit the real .env.
# Auth is Better Auth JWT only (P2-6 — the legacy password login is deleted).
MAISHA_BETTER_AUTH_URL=
MAISHA_SESSION_SECRET=change-me-preview-token-hmac-key
# Optional: point the API at a non-default Mahsa or DB
# MAISHA_MAHSA_URL=http://dif:8088
# MAISHA_DATABASE_URL=sqlite:////data/maisha.db
