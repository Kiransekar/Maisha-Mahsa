# Caddy reverse proxy with automatic TLS (PRD §11.1). Replace the host with your domain.
# Run Caddy on the VPS in front of the api service.
maisha.example.com {
	encode gzip
	reverse_proxy 127.0.0.1:8000
}
