# PROGRESS — append-only work log (MMX-1.0 §0.2)

Format: `[ticket-id] [status] [date] [agent] [commit] [note]`
Statuses: `DONE` · `WIP` · `BLOCKED-CA` · `BLOCKED-HUMAN` · `DEFERRED`.
This is the only place agents record status. Never mark the immutable spec (`docs/MASTER_PLAN.md`).

---

[GOV] DONE 2026-07-20 ORCH - Governance bootstrap: spec moved to docs/MASTER_PLAN.md (444), CLAUDE.md pointer added, PROGRESS.md + PROGRESS_BOARD.md created, CODEOWNERS + CI guard (fail-on-touch) for the immutable spec.
[WS1.E1] DONE 2026-07-20 ORCH - Statutory-oracle framework: vector schema {id,statute,section,source/citation_url,ca_initials,ca_date,target,inputs,expected}, YAML vectors under api/tests/statutory_oracle/vectors/, target registry + CI-blocking pytest runner (picked up by `make test-py`). Rust-parity leg is a hook only (WS3, not yet ported). Seeded with the 5 defect vectors.
[WS1.C1] DONE 2026-07-20 ORCH - 194J threshold 30k→50k (single + aggregate). Value cited: docs/MASTER_PLAN.md §WS1.C1 (Finance Act 2025). Locked by oracle vector tds_194j_fy2526. ca_initials PENDING.
[WS1.C2] DONE 2026-07-20 ORCH - 194I per-month restructure: ₹50,000 per month or part thereof, TDS on full month's rent, annual-aggregate logic removed for 194I. Locked by vectors tds_194i_40k_nomo / tds_194i_55k_full. Value cited: §WS1.C2. ca_initials PENDING.
[WS1.C3] DONE 2026-07-20 ORCH - ESI truncate-then-round root-cause fix: ceil applied on the exact Decimal product before any int truncation; every `int(Decimal(...)*rate)` in payroll/statutory.py removed. Anti-pattern grep-gate scripts/check_no_truncate_round.sh added. Locked by vector esi_20001_gross (151,651).
[WS1.C4] DONE 2026-07-20 ORCH - Company tax 115BAA path = 22%+10% surcharge+4% cess (25.168%), MAT excluded; caller chooses regime explicitly (regime_115baa). Non-115BAA company base rate has no CA vector → that branch raises BLOCKED-CA. Locked by vector company_115baa_1cr. Value cited: §WS1.C4.
[WS1.C5] DONE 2026-07-20 ORCH - Vault statutory retention 7y→8y computed from FY-end (not upload date). Locked by vector retention_invoice_8y_fy_end. Migration for existing rows: DEFERRED to WS4.2 (multi-tenant migration engineering) — noted below.
[WS1.C5-migration] DEFERRED 2026-07-20 ORCH - Recompute stored retention_until for existing vault rows. Belongs with WS4.2 Alembic/Postgres migration work; pure-calc fix already live so all NEW rows are correct.
[WS1.A1] DONE 2026-07-20 OPUS - statutory_regime module (api/app/core/statutory_regime.py): regime selector on earlier-of-credit-or-payment vs 2026-04-01 boundary, form map (16→130/16A→131/24Q→138/3CD→26/15GH→121), Tax Year label, payment-code loader shipped EMPTY (codes BLOCKED-CA §0.6). SPEC-WS1A.md written. 6 oracle vectors (ids regime_*). Verify `pytest tests/statutory_oracle -k regime` green (6/6).
[WS1.A2] BLOCKED-CA 2026-07-20 ORCH - Citation re-point + scripts/check_no_stale_citations.sh. Blocked on the 1961→2025 SECTION map (spec enumerates only the FORM map); a hard grep-gate now would fail CI across domains that legitimately still use 1961 forms until regime_for is wired into the TDS engines. Unblock: CA-sourced section map + payment codes.
[WS1.A3] BLOCKED-CA 2026-07-20 ORCH - Regenerate Form 138/130/131 + challan payment codes. Blocked: needs CA-provided samples in tests/statutory_oracle/fixtures/ (§0.6). No fixtures present.
[WS1.B1] DONE(core) 2026-07-20 OPUS - Code on Wages s.2(y) engine (api/app/core/statutory_wage.py): statutory_wage_base(components, in_kind) = Basic+DA+retaining + excluded-excess-over-50% add-back + in-kind ≤15%. 4 algorithm-derived oracle vectors (compliant/add-back/overtime/in-kind). In-kind %-base is an interpretation flagged BLOCKED-CA (§0.7). REMAINING: route PF/ESI/gratuity/bonus/leave-encashment through it (mechanical; safe for existing basic-only tests since wage_base==basic there) — tracked as WS1.B1-wiring.
[WS1.B1-wiring] DONE 2026-07-20 OPUS(fanout) - Rewired payroll PF/ESI (+ bonus/ECR) to compute on statutory_wage_base(components) at the caller level (compute_components/build_snapshot/ecr_text via _structure_wage_base); pure scalar functions in statutory.py unchanged, so basic-only tests hold (wage_base==basic). Oracle vector wiring_pf_esi_on_underweighted_ctc (payroll_components target, end-to-end). Opus-verified PASS, reverify green.
[WS1.B2] DONE 2026-07-20 OPUS(fanout) - statutory.gratuity_hybrid(doj, exit, boundary=2025-11-21, old_base, new_base): pre-boundary years on last-drawn Basic, on/after on s.2(y) base, 15/26 reused, FTE ≥1yr gate, exact Decimal→single rupee round. 3 gratuity_hybrid vectors. BLOCKED-CA-flagged: the year-apportionment convention at the boundary is a computational interpretation (no statutory number invented). Opus-verified PASS (64 payroll+oracle green).
[WS1.B3] DONE 2026-07-20 SONNET(fanout) - check_ctc_compliance + _rebalance_suggestion + PayrollService.validate_ctc: warns when Basic+DA < 50% (reuses statutory_wage.EXCLUDED_CAP_FRACTION, no hardcoded 0.5), proposes total-preserving restructure (trim special→LTA→HRA), never mutates stored structure; required-minimum ceil'd. 8 unit+service tests. Opus-verified PASS. (Verifier noted service.py also carries the B1-wiring money-math change — that is the WS1.B1-wiring ticket's work, separately vector-locked & passed; UI/API test deferred: dispatched scope was unit + one service test.)
[WS9.3] DONE 2026-07-20 SONNET(fanout) - Draft-IRN honesty: DRAFT_IRN_LABEL (byte-for-byte per §WS9.3 line 169) on the sole local IRN surface gst_calc.einvoice_payload (IrnStatus + QrData.Caption); scripts/check_no_draft_irn.sh gate (wired into make gates by ORCH); label test in tests/unit/gst. No PDF/QR-image invoice surface exists yet — gate catches future ones. Opus-verified PASS.
[WS1.D8] DONE 2026-07-20 SONNET(fanout) - payables_calc.msme_form1_period (half-year window + due 31 Oct / 30 Apr per §WS1.D8) + msme_form1_pack (MSME vendors outstanding >45d, per-vendor lines, due date); dates injected. Fixture snapshot test. Opus-verified PASS (values cited to spec, nothing invented).
[FANOUT-1] DONE 2026-07-20 ORCH - Parallel workflow wf_d543bdb8-e63: 3 impl streams (opus payroll→sonnet B3; sonnet WS9.3; sonnet WS1.D8) + 4 opus adversarial verifiers + 1 opus gate. 9 agents, 0 errors. Independent make verify green (463 py + rust + eval 13/13 + lint + 2 grep-gates), 21 oracle vectors.
[WS3.1] DONE 2026-07-20 OPUS(fanout+ORCH) - Rust recompute port (Prime Directive): new mahsa::recompute module — slab_tax, pf_esi (+s.2(y) wage base), tds, itc, gst_fees, gratuity_bonus, all pure integer-paise mirrors of the Python. ORCH scaffolded module tree + shared round_rupee/ceil_rupee + tests/parity.rs (replays the shared oracle vectors against Rust → paisa parity by construction; honest coverage report for unported targets). 4 ported targets vector-locked (esi, statutory_wage_base, tds_on_payment, gratuity_hybrid); slab/itc/gst_fees proven by Rust unit tests mirroring Python. Rust 95 tests incl. parity. ORCH fixed a double-rounding parity defect in tds.rs (single round-to-rupee to match Python; the 4 vectors used round-rupee amounts and hid it — found by the opus verifier, regression test added for ₹50,004.95). Verifiers: 5 PASS + 1 FAIL(tds, now fixed). make verify green.
[WS3.1-remaining] NOTE 2026-07-20 ORCH - Not ported yet (coverage gaps printed by parity test): itr_computation(115BAA), regime_for, form_name, retention_until, payroll_components(integration). WS3.2 randomized cross-language proptest fuzz (Python↔Rust bridge) not built — vector-parity is the current gate.
[FANOUT-2] DONE 2026-07-20 ORCH - Parallel workflow wf_433e0596-8c3: 6 disjoint Rust fills (opus: slab/pf_esi/tds/gratuity; sonnet: itc/gst_fees) + 6 opus parity verifiers + 1 opus gate. 13 agents, 0 errors. Opus verifier caught the tds double-rounding defect the green suite hid — the value of adversarial verify.
[WS3.3] DONE 2026-07-20 OPUS(fanout) - Killed default-healthy: every fold module's missing-signal default flipped 1.0→0.0 (§0.4: absent = unknown, never healthy); Completeness{expected,missing,complete} on the Fold struct + per-module EXPECTED_SIGNALS; old defaults_to_healthy tests inverted + absence-behaviour tests added. Opus-verified PASS. ORCH fix: payroll.rs edit was lost to a concurrent-write race (only module missing EXPECTED_SIGNALS → gate compile error) — restored to match siblings.
[WS3.4] DONE 2026-07-20 OPUS(fanout) - Verdict object app/core/verdict.py: {figures[{key,value_paise}], rule_pack_version, org_id, hash}; hash reuses core.audit.canonical_json+sha256 (no new dep); org_id keyword-only, session-sourced (§0.8), bound into hash; FoldResult.verdict() surfaces it. 7 binding tests (tamper/order/org/version all detectable). Opus-verified PASS.
[WS3.5a] DONE 2026-07-20 SONNET(fanout) - mahsa_coverage.json (repo root) + app/core/mahsa_coverage.py: derived from dif/tests/parity.rs PORTED + oracle targets; is_recomputed/badge_state (verified iff Rust-recomputed, else honest_pending; unknown defaults honest_pending). Opus-verified PASS.
[WS3.5b] DONE 2026-07-20 SONNET(fanout) - Honest-state badges: answer_card.html tri-state (✓ recomputed / ◐ honest-pending / ⚠ unbacked) driven by mahsa_coverage.badge_state, +CSS, coverage-driven badge test (not hardcoded). Opus-verified PASS.
[FANOUT-3] DONE 2026-07-20 ORCH - Parallel workflow wf_14c79357-930: 3-way safe-parallel (opus WS3.3 Rust ‖ opus WS3.4 verdict Py ‖ sonnet WS3.5a coverage Py) → sonnet WS3.5b → 4 opus verifiers + opus gate. 9 agents, 0 errors. Gate initially RED on a lost-edit compile error in payroll.rs (concurrent-write race); ORCH root-caused + fixed; independent make verify green (Rust 95→ incl parity + 476 py + eval 13/13 + lint + grep-gates).
[WS3-live-wiring] DONE 2026-07-20 ORCH - Wired recompute into the LIVE /fold path (Prime Directive §0.4, done directly not via fanout). FoldRequest gains recompute_claims[{target,inputs,claimed_paise,label}]; fold_handler recomputes each via recompute::check_claims and, on a recomputable MISMATCH, inserts a MAHSA-PARITY-001 Block rule + escalates status to Red BEFORE unfold (so the shape/requires_approval reflect it); an unrecomputable target stays honest-pending (recomputed_paise=None), never a block. FoldResponse gains recompute[]. 12 targets dispatched (esi_employee/employer, pf_*, eps, statutory_wage_base, tds_on_payment, annual_income_tax, gratuity_hybrid/required, bonus, late_fee/interest_3b). Python mahsa_client: RecomputeClaim/RecomputeCheck models + fold(recompute_claims=) send + parse; honest_pending property. Tests: 4 Rust dispatch unit + 3 Rust integration (mismatch blocks / correct verifies / unrecomputable honest-pending) + 3 Python parse. Backward-compatible (empty claims = prior behaviour). Rust 107 tests, py 479. make verify green.
[WS7.3] DONE 2026-07-21 OPUS(fanout) - Today view (Owner landing): app/web/today.py assembler + today.html + today_router.py (GET /today) + today.css. 3 regions: cash strip (runway via Verified-Number chip / ◐ honest-pending), Needs-you queue (real pending_approvals, one-tap + ₹), Trouble radar (deadlines in alert grammar what/when/₹-consequence/one-tap, ranked by ₹). Penalties-avoided badge-backed (real avoided fees) or labelled estimate — never invented. ORCH wired router+nav. Opus-self-verified green.
[WS7.5] DONE 2026-07-21 OPUS(fanout) - Exception Inbox (research #1: Xero 243-vote bulk-reconcile): app/web/exceptions.py + exception_inbox.html + inbox_bulk_preview.html + exceptions_router.py (GET /inbox, POST /inbox/bulk) + css. 5 queues; Awaiting-approval ← approvals, Mahsa-blocked ← recompute MAHSA-PARITY/FigureVerdict.blocked; Needs-doc/categorization/Feed-broken honest-empty (no fabricated items). KEY: bulk-ops WITH PREVIEW — dry-run lists affected rows + ₹/ITC impact, no mutation until confirm=true. Items ranked by ₹ impact. ORCH wired router+nav. Opus-self-verified green.
[WS7-TODAY-INBOX] DONE 2026-07-21 ORCH - Parallel workflow wf_6b8c7dff-31d: WS7.3+WS7.5 (2 opus) — Build phase 2/2 done; verify+gate agents hit session cap so ORCH wired routers (main.py include_router today+inbox) + nav (base.html Today/Exception-Inbox) + ran the gate. make verify green. FLAG: app 500-handler still 'Something went wrong' (research anti-pattern #6) → WS7 follow-on (4-question error template).
[WS7.1] DONE 2026-07-20 OPUS(fanout) - Design-system foundation (research-grounded): tokens.css verification family --c-verify teal/◐ indigo/✕ violet STRICTLY separate from money green/red (fixes the .vmark--ok=money-green trust-confusion bug) + .tnum tabular numerals; format.py canonical inr()/inr_rupees() lakh/crore renderer (₹12,34,567) as the single money surface (delegates to ported money.py, untouched); scripts/check_money_format.sh QG.3 lint (proven catches py+template violations; baselines 2 pre-existing: eway.py machine-artifact grouping + actions.py:79 toast); docs/WS7_DESIGN_TOKENS.md component-states inventory. 43 web tests. Opus-verified PASS.
[WS7.2] DONE 2026-07-20 SONNET(fanout) - Verified-Number chip + Working panel (answers research T1 silent-drift): native <details> chip (no JS) using verification tokens (✓ teal/◐/✕ — NOT money green); working_panel.html = inputs→formula→citations→documents→verdict-hash→report-issue, driven by verify.py/verdict.py (honest-pending→◐ never fabricated ✓; missing data→honest 'not yet sealed'); ◐→✓ lock-in keyframes; verified_number.css (app.css untouched, scoped override). 7 render tests. FLAG: ask.py Figure/Answer lack per-figure Verdict → verified figures show honest partial panel; threading real Verdict through ask flow = follow-on. Opus-verified PASS.
[WS7-FOUND] DONE 2026-07-20 ORCH - Parallel workflow wf_5a0de475-bc2 (English-only; i18n/WS7.10 dropped per user): WS7.1→WS7.2 + 2 opus verifiers + opus gate. 5 agents, 0 errors. Grounded in docs/WS7_UX_RESEARCH.md. make verify green (768 py + rust incl fuzz + eval + lint + grep-gates).
[WS7-UXR] DONE 2026-07-20 ORCH - WS7 UX research (grounds the UI build; feeds WS7.V). Parallel workflow wf_dc73660b-ee7: 8 research angles (real WebSearch+WebFetch on Reddit/Capterra/SoftwareAdvice/CA advisories/HN) → opus adversarial citation verifier PER ANGLE (re-fetched URLs, DROPPED 12 fabricated/unsupported findings) → opus synthesis. 17 agents, 0 errors, 430 web fetches, 81 VERIFIED cited findings. docs/WS7_UX_RESEARCH.md (434 lines): 12 ranked painpoint themes, per-theme design decisions, validate/challenge mapping to every WS7 ticket, 14 competitor anti-patterns, WS7.V open questions. Key: T1 silent number-drift VALIDATES our recompute+badge+append-only thesis; Xero 243-vote/3yr bulk-reconcile = loudest signal → WS7.5. Ready to build WS7.1 design foundation next.
[WS1.D2] DONE 2026-07-20 OPUS(fanout) - QRMP (new app/domains/gst/qrmp.py; gst_calc untouched): filing_calendar(profile monthly|qrmp|composition), pmt06_fixed_sum=35% prev-quarter cash (spec value, exact paise) + self-assessed; QRMP→quarterly GSTR-1/3B + monthly PMT-06 + IFF(m1-2); obligation_penalty reuses ported late_fee_3b/interest_3b unchanged. 10 tests. BLOCKED-CA: statutory due-date calendar days (injected; pending_ca if absent). Opus-verified PASS.
[WS1.D3] DONE 2026-07-20 SONNET(fanout) - CMP-08 (new app/domains/gst/cmp08.py): build_cmp08 quarterly composition statement (outward tax at injected composition_rate, RCM inward at normal rate via ported rcm_liability, interest via ported interest_3b). 7 tests. BLOCKED-CA: composition rates 1/5/6% (mandatory param, raises if absent — never invented). Opus-verified PASS.
[WS9.2] DONE 2026-07-20 SONNET(fanout) - Bank CSV parsers: existing TreasuryService.import_csv is a GENERIC header-alias parser; verified empirically it already normalizes SBI/Kotak/Yes/IndusInd/IDFC/Federal/RBL → 7 realistic fixtures + per-bank + parametrized contract tests (same normalized shape, exact paise). ZERO code change (ponytail reuse). Opus-verified PASS.
[WS8.1] DONE(core-slice) 2026-07-20 SONNET(fanout) - Audit Pack app/core/audit_pack.py: build_audit_pack → TB/P&L/BS/GL sections, each figure {label,value_paise,badge(mahsa_coverage.badge_state),evidence_ref} + pack integrity hash (canonical_json+sha256, tamper-evident). Excel/PDF + statutory registers/26AS/MSME-ageing = follow-on. Tests: 4 sections, honest-pending not badged verified, tamper-evident hash. Opus-verified PASS.
[P0D-P2-R1] DONE 2026-07-20 ORCH - Parallel workflow wf_3fce87e4-744: [D2→D3 pipeline] + WS9.2 + WS8.1 + 4 opus verifiers + opus gate. 9 agents, 0 errors. No ported path touched (fuzz green). Independent make verify green (747 py + rust + eval + lint + grep-gates).
[WS1.D1] DONE 2026-07-20 OPUS(fanout) - New TDS/TCS sections (payables_calc, ADDED not modifying ported tds_on_payment): 194Q 0.1% on excess >₹50L/vendor/FY + TDS-primacy suppresses TCS 206C(1H); 194T 10% >₹20k; TCS s.394 + 206AA/206AB higher-rate overlay apply_higher_rate (separate, not baked into tds_on_payment). SPEC-WS1D1.md. 14 unit tests (boundaries + primacy). BLOCKED-CA (params that RAISE when fired without value, never invented): TCS-goods rate+threshold, 206AA no-PAN + 206AB non-filer floor rates. Opus-verified PASS.
[WS1.D4] DONE 2026-07-20 SONNET(fanout) - IMS workflow (new app/domains/gst/ims.py; gst_calc/itc_setoff untouched): accept→eligible/reject→ineligible/pending<deadline→pending/pending≥deadline→deemed_accepted; ims_disposition + eligible-ITC aggregate (paise). 7 tests. BLOCKED-CA: exact deemed-accept deadline (injected date param, no clock). Opus-verified PASS.
[WS1.D7] DONE 2026-07-20 OPUS(fanout) - E-way bill (new app/domains/gst/eway.py): eway_required (₹50k inter-state boundary spec-cited; intra thresholds injected from WS2 packs=BLOCKED, returns pending not a guess) + build_eway_json NIC Part-A/B artifact with EWB_PREPARE_LABEL honesty marker (prepare-and-download, not filed). 4 tests. Opus-verified PASS.
[P0-D-R1] DONE 2026-07-20 ORCH - Parallel workflow wf_e0e0aecd-771: WS1.D1(opus)/D4(sonnet)/D7(opus) + 3 opus verifiers + opus gate. 7 agents, 0 errors. No ported path modified (parity fuzz green); test_oracle.py untouched (oracle-vectoring these is WS1.E2, CA-gated — domain unit tests lock the spec-cited values now). Independent make verify green (705 py + rust incl. fuzz + eval 13/13 + lint + grep-gates).
[WS5.3] DONE 2026-07-20 ORCH(direct) - Per-role landing app/core/landing.py: ROLE_LANDING (Owner→today, Accountant→exception_inbox, CA→audit_room, §WS7.4) + vault-sensitivity clearance on the canonical Role model (ROLE_CLEARANCE, can_view_sensitivity, fail-closed on unknown; supersedes vault_calc's legacy role ranks — routes through this once vault is org-scoped WS4.2). 5 tests, ruff+mypy clean. Product-confirmable defaults. Done directly (subagents session-limited). make verify green.
[WS5.2] DONE 2026-07-20 SONNET(fanout) - Approval matrices app/core/approval_matrix.py (new; approvals.py F4 queue untouched): decide_approval(plan,role,action,amount,matrix_config) — Basics/Startup fixed defaults ignore config; Growth honours role×amount×action config; statutory-filing action ALWAYS hard-gates to Owner/Admin, uncircumventable by config (security-critical negative tested). Reuses rbac.can + entitlements.STATUTORY_GRACE. 18 tests. Approver ₹1L default product-confirmable. ORCH-gated (verifiers session-limited).
[WS6.2] DONE 2026-07-20 SONNET(fanout) - Quantity gates: WS6.1's quantity_gate already correct (ok→soft_warn→grace→block, ceiling always visible, upsell skips a plan that can't absorb the count); de-stubbed comments + 25 boundary-derived tests (headcount 10/50/200, seats, entities). Limits product-confirmable.
[WS6.3] DONE 2026-07-20 SONNET(fanout) - Upgrade-trigger events app/core/upgrade_triggers.py: 5 triggers (employee#11→gratuity/bonus, first SAFE→equity, AATO ₹5Cr→e-invoice, 2nd GSTIN→Growth, board meeting→secretarial); fires at/after threshold, not below; data-driven; no clock. 17 tests. 2nd-GSTIN target_plan=growth product-confirmable.
[WS4.4] DONE 2026-07-20 OPUS(fanout) - Per-tenant hash-chain: audit.py/audit_store.py made per-org (independent genesis+head, verify_chain(org) scoped, tamper in A undetectable-to/independent-of B); EXISTING canonical_json/compute_hash algorithm unchanged (30 audit tests incl. existing green + verdict tests green); edit-log helper + compute_daily_root(org) anchor record (external timestamping = ops/Human); docs/AUDIT_TRAIL_COMPLIANCE.md (MCA edit-log/non-disablable/tamper-evident mapping). ORCH-gated.
[P1-R2] DONE 2026-07-20 ORCH - Parallel workflow wf_7c06e6da-b7b: WS5.2/WS6.2/WS6.3 (sonnet) + WS4.4 (opus). Build phase 4/4 landed; opus verifiers + gate hit a SESSION LIMIT (reset 8:10pm IST) so ORCH ran the gate + spot-checks instead. Independent make verify green (675 py + rust + eval 13/13 + lint + grep-gates + RLS coverage).
[WS5.1] DONE 2026-07-20 OPUS(fanout) - RBAC role model app/core/rbac.py: Role/Capability enums + ROLE_CAPABILITIES data matrix + pure can(role,capability,context); Investor-link time-box/report-scope/watermark; role_change_event() audit-chainable PII-free emitter. Permission-matrix test (independent EXPECTED table + coverage guard, spec negatives: Accountant no approvals, CA read-only, investor scoped). Role→capability defaults product-confirmable. Not router-wired (WS4.3/5.2). Opus-verified PASS.
[WS6.1] DONE 2026-07-20 OPUS(fanout) - Entitlement architecture app/core/entitlements.py + plans.yaml: registry auto-derived from 12 domain manifests (116 keys) + platform baseline; cumulative Basics71/Startup+34/Growth+11 (validate_registry fails loud if data stops partitioning); is_entitled + guard()→GuardDecision + statutory-grace override (never blocks a filing; keys-only log; upsell-after) + quantity_gate stub; plan_from_context session-only (§0.8). 18 tests. Per-feature plan placement + grace set + quantity limits = product/CA-confirmable defaults. Not router-wired. Opus-verified PASS.
[WS4.7] DONE(db-layer) 2026-07-20 OPUS(fanout) - Tenancy red-team suite (DB/RLS layer): RLS extended to 6 money/PII tables (vendors/customers/bank_accounts/employees/tds_returns/documents+storage_prefix) → 14 tenant tables all RLS-covered; rls_redteam.sql = data-driven per-op×per-table NEGATIVE matrix (cross-org SELECT/INSERT/UPDATE/DELETE all blocked + fail-closed + storage-path isolation) over 12 tables, live vs Postgres. Route-level + real object-storage red-team honestly deferred to WS4.2/4.3. Opus-verified PASS.
[P1-R1] DONE 2026-07-20 ORCH - Parallel workflow wf_4487f969-1c1: WS5.1‖WS6.1‖WS4.7 (3 opus) + 3 opus verifiers + opus gate. 7 agents, 0 errors. Independent make verify green (604 py + rust + eval 13/13 + lint + grep-gates) + live RLS red-team PASSED.
[WS4.1] DONE 2026-07-20 OPUS - Multi-tenant target schema (P1, precedes all P1 code). SPEC-WS4.md (org→entity→gstin_registration→domain tables; BIGINT paise; uuid; storage prefix scheme). infra/db/multitenant/{001_tenancy,002_domain_rls}.sql: tenancy core + app_current_org() session GUC (org from JWT/session never request body, §0.8) + non-superuser maisha_app role + RLS (ENABLE+FORCE, USING+WITH CHECK) on all 8 tables incl. representative domain tables (bills/invoices/journal_entries/gst_returns). scripts/check_rls_coverage.sh CI gate (wired into make gates; proven to catch a leaky table). scripts/rls_redteam.sh + rls_redteam.sql: LIVE proof vs real Postgres — org-A session cannot read/write/update org-B rows + fail-closed when unbound; PASSED. make verify green. Supabase provisioning = Human (§0.7). NEXT: WS4.2 replay all 36 tables + SQLite importer.
[WS3.2-fuzz] DONE 2026-07-20 ORCH - Randomized Py↔Rust parity fuzz (WS3.2 completed). tests/integration/test_parity_fuzz.py: per ported target, generate CASES=300 seeded PAISE-GRANULAR random inputs (straddling rounding edges/thresholds — where round-number vectors are blind), compute the Python figure, send as a claim to the REAL Mahsa binary via verify_claims; assert every one verifies to the paisa (mismatch → block with both values in the note). 16 single-value targets + itc multi-value = ~5100 random parity checks/run, all green — no divergence. Bridge = the existing /fold recompute path (no PyO3/FFI). Non-vacuous guards (len==CASES, some non-zero recompute). WS3.2 = vector-parity + live-block + fuzz all done. py 530. make verify green.
[WS3-itc-multivalue] DONE 2026-07-20 ORCH - Ported itc_setoff as a MULTI-VALUE recompute target. Extended the claim/check model: a claim is single (claimed_paise) OR multi (claimed_values name→paise map, field-wise subset match); RecomputeCheck gains recomputed_values; has_mismatch + honest_pending handle both (Rust + Python). recompute_multi("itc_setoff") → per-head cash+remaining credit; wired into check_claim (live gate) + parity (PORTED[9]) + 2 oracle vectors + coverage. gst file_gstr3b attaches an itc_setoff claim (compute-time hook); verify_figure carries multi unchanged. Live-proven: field-wise verify + tamper-one-head blocks. Rust 112, py 513, 33 oracle vectors. make verify green.
[WS3-verified-flow] DONE 2026-07-20 ORCH - app/core/verify.py verify_figure(mahsa, claim) → FigureVerdict{verified,blocked,honest_pending}: the single path by which on-demand figures reach the §0.4 gate (batch → run_loop; on-demand → this). Mahsa-down raises (never silently unverified). Wired tax /advance-tax/234c endpoint to it (Golden Rule; returns verified/mahsa_blocked). 3 unit (fake-mahsa verdict mapping) + 2 integration vs real binary (correct 234c verifies, tamper blocks, 115baa hook verifies live). py 509. make verify green. Prime Directive now CLOSED-LOOP for both batch (payroll/payables/gst/tax-234e) and on-demand (234b/234c/115baa) figures.
[WS3-tax-hooks] DONE 2026-07-20 ORCH - Compute-time recompute-claim hooks for the on-demand tax figures (not persisted → no batch producer). advance_tax_interest attaches an interest_234c claim (total_234c); itr_computation company+115baa attaches a company_tax_115baa claim (normal_tax), firm/LLP attaches none; module builder interest_234b_claim for 234B callers. Claim inputs = exact compute args; targets parity-locked. 4 round-trip unit tests. py 504. make verify green.
[WS3-tax-ports] DONE 2026-07-20 ORCH - Ported interest_234b, interest_234c, company_tax_115baa to Rust recompute (slab_tax.rs, exact integer paise mirroring tax_calc; 234c rounds per-installment, 115baa 25.168% i128-safe). Wired all 3 into the live dispatch (mod.rs recompute()) + parity harness (PORTED[8] + dispatch) + 5 oracle vectors (ws1_tax_interest_115baa.yaml) + test_oracle.py TARGETS + mahsa_coverage.json. Parity green (Rust==Python==oracle: 234c ₹20,200, 234b ₹20,000, 115baa ₹25,16,800). Rust 111, py 500, 31 oracle vectors. make verify green. NOTE: these are on-demand tax figures (not persisted rows), so a BATCH producer (iterate+claim) isn't feasible like 234e/tds; they are now live-gate-available (a flow computing them can attach a claim) — compute-time claim hook is the follow-on.
[WS3-domain-producers] DONE 2026-07-20 ORCH - gst/payables/tax producers via parallel workflow wf_1a37c46f-e08 (opus tax-Rust-port → 3 parallel sonnet producers → 4 opus verifiers + opus gate; 9 agents). tax-rust-port: late_fee_234e ported to Rust recompute (slab_tax.rs + live dispatch + parity PORTED[5] + oracle vector + mahsa_coverage). Producers (recompute_claims overrides): payables→tds_on_payment per bill (category recovered by replay); gst→interest_3b per filed-late 3B; tax→late_fee_234e per filed-late TDS return. Round-trip unit tests. ORCH reconciliation of 2 verifier-caught defects: (1) payables emitted 194C-individual claims Rust couldn't reproduce because Rust tds_on_payment hardcoded payee_type='company' — ROOT-CAUSE fixed: added payee_type param (tds.rs+mod.rs+parity.rs) + 194C individual/company parity vectors, so 1% is now recomputable; (2) gst late_fee_3b claim used is_nil=(cash==0) proxy (is_nil not persisted) → dropped that claim, interest_3b only (late fee honest-pending). Also fixed a pytest basename collision (4×test_recompute_claims.py → unique names). Rust 108, py 495. make verify green.
[WS3-payroll-producer] DONE 2026-07-20 ORCH - Payroll domain now EMITS its claims end-to-end. BaseDomainService.recompute_claims(session) hook (default [] → honest-pending); PayrollService overrides it to emit wage_base + pf_employee/employer + esi_employee/employer claims per active employee (same inputs the figures were computed on — payslip_recompute_claims + compute_components now returns wage_base). run_loop calls service.recompute_claims and passes to mahsa.fold, so EVERY domain gets the gate uniformly (payroll live, others incremental). PT/TDS/LOP not yet ported → no claim (honest-pending). Proven against the REAL Mahsa binary (mahsa_server): payroll's real claims all verify (green) + a ₹1-tampered figure → Red MAHSA-PARITY block. Tests: 2 payroll unit (builder shape + unported-excluded) + 2 integration (verify-live + tamper-blocks-live); updated 4 fake-mahsa fold stubs for the new kwarg. py 482. make verify green. NEXT: repeat the producer pattern for gst/payables/tax as their targets are already ported (tds, itc, gst late-fee, gratuity).
[WS7-SPA-API] DONE 2026-07-21 ORCH - JSON API for the React SPA: app/web/api_router.py GET /api/today + /api/inbox, reusing the SAME pure assemblers the HTMX pages render (build_today / build_inbox) so SPA and server-rendered app cannot drift into two truths; exceptions_router._collect renamed collect_sources and shared (no duplicated Mahsa fold logic). Honest-degradation preserved: Mahsa down -> 200 + mahsa_up:false + honest-empty, never a fabricated verified figure. 2 integration tests pin the shape AND the degradation contract. Fits the existing /api/<domain> convention.
[WS7-BRAND-RESEARCH] DONE 2026-07-21 ORCH - docs/BRAND_THEME.md written. Recovered the pre-crash workflow wf_ac62dda1-ec7 (5 research angles + 4 of 5 verifiers complete; cross-session resume unavailable so the journal was harvested and ONLY the missing trust-precision-visual verifier re-run). That verifier DROPPED 2 findings (a fabricated Bloomberg-amber rationale from an LLM-authored repo; paraphrased Wise quotes presented as quotation) and corrected 4 (Stripe bg is #ffffff not #f6f9fc; "single accent" is Mercury/Ramp/Brex/Linear's pattern, NOT Wise's). Doc = 5 verified premium-finance patterns + Indian-market conventions (lakh-native proof, named statutes, up-front regulatory disclosure) + our system: warm-paper light canvas, brass accent chosen by elimination (money ramps + verification ramps + every competitor hue excluded), borders-not-shadows, tabular numerals + mono for statutory identifiers, hierarchy by size/tracking not weight. KEY FINDING: the existing frontend/ placeholder tokens (#0e1116 near-black + #2dd4bf teal) ARE the generic-AI-startup look the research warns against (Digits is the in-segment example). Verification-family separation independently validated by GitHub Primer. Canvas + accent flagged as open product decisions in §7.
[WS7-SPA-BRAND] DONE 2026-07-21 ORCH - React SPA rebuilt on the brand system (decision LOCKED with user: warm-paper light canvas + brass accent). frontend/src/theme/tokens.css replaced the generic-AI placeholder (#0e1116/#2dd4bf) with the BRAND_THEME palette; verification hues keep the SAME hue families as the HTMX tokens.css, retuned for light-canvas contrast. New: lib/money.ts (Intl en-IN lakh/crore — native, no formatting dep, no hand-rolled grouping to drift from format.py) + components/VerifiedNumber.tsx (VerifyChip + VerifiedNumber; state comes from the payload so a fabricated ✓ is impossible by construction; unknown state falls to ✕ never ✓). Today.tsx + Inbox.tsx are now REAL, reading /api/today + /api/inbox: honest-pending ◐ rendered as-is, null ₹-impact renders "not yet known — we don't guess" never ₹0, unwired inbox queues print their stub_note so an empty queue can't be mistaken for a genuine zero, Mahsa-down shows an explicit banner. Shell restyled (hairline borders, brass active state, no shadows/gradients per BRAND_THEME §3). npm run build + tsc clean, oxlint clean, make verify green. NOT wired: SPA bulk ops — the server contract is preview-then-confirm and shipping a bulk button without the two-step would be the silent-bulk-mutation anti-pattern; lands with the preview step or not at all.
[WS7-BLEND] DONE 2026-07-21 ORCH - docs/WS7_BUILD_CONTRACT.md: the JOIN of WS7_UX_RESEARCH (what must never happen to the user) and BRAND_THEME (what it looks/sounds like when we prevent it). One table: painpoint -> brand expression -> component rule -> status; a WS7 component is not done until every applicable row is satisfied or deferred with a reason. SELF-AUDIT found the first React pass was brand-correct but painpoint-BLIND, and 4 gaps were fixed: (T7) the WS7.2 working panel had REGRESSED to a title tooltip even though /api/today was already sending inputs/formula/citations/verdict_hash — rebuilt as a native <details> panel mirroring the HTMX version (no JS), incl. a "this number looks wrong" escape hatch on every figure; (T6) errors now use the 4-question template (what happened / is my money+filing safe / what next / traceable ref) per anti-pattern #6 "Something went wrong" = taunting — components/ErrorState.tsx; (T4) NEW effectiveState(state, stale): a ✓ on stale inputs downgrades to ◐ with a stated reason — staleness can only downgrade, never upgrade; (#14) failure no longer renders a blank shell — last-known payload renders below the error marked stale, since a blank ledger reads as data loss. Added vitest (first FE test runner) + 5 tests on the trust branch: stale-downgrade, never-upgrade-to-verified, lakh/crore grouping (₹12,34,567 not ₹1,234,567), and unknown-amount renders "—" never ₹0. tsc+vite build clean, oxlint clean (1 fast-refresh warning), vitest 5/5, make verify green. NOT wired: frontend tests into `make verify` (needs node in the CI env — decide before it counts as a gate); SPA bulk ops still pending the preview-then-confirm two-step (T3, the loudest signal in the research — next).
[WS7-E2E-BUILD] DONE 2026-07-21 ORCH - WS7 UI/UX end-to-end via 2 parallel workflows (26 agents, 0 errors). Build round wf_b4c4b314-e2f (16 agents): 4 JSON API tickets + 6 SPA screens, each screen adversarially verified. Result was 5 FAIL / 1 PASS - the verify layer is the only reason we know. CARDINALS caught: (a) WS7.7 /api/health/connections was NEVER REGISTERED (grep api_health = 0 hits) so the component fetched a dead route AND its copy asserted a downgrade behaviour that zero call sites performed; (b) WS7.6 sub-rupee drift rendered through inr()=round(paise/100) so claimed 12345678 vs recomputed 12345638 both printed the same rupee string and the mismatch read "differs by ₹0" - the exact paise-level drift the product exists to catch, made invisible; (c) WS7.5 confirm re-POSTed the LIVE selection not the previewed rows, with checkboxes still interactive behind the open preview; (d) WS7.8 CSV import fired with no preview (bulk insert + balance rewrite) and a VACUOUS test proven by mutation (deleting pickFirstFigure's body left it green); (e) WS7.4 runway_months null rendered as "not burning - unbounded" though the backend emits null BOTH for revenue>=burn AND for an empty ledger, so a new user with no data was told runway is unbounded. Fix round wf_ff7e18b8-b39 (10 agents, opus fix -> independent opus re-verify w/ mandatory mutation-check): 4/5 PASS; ORCH finished the 5th (onboarding asserted "net effect on balance" = credits-debits, but service.py OVERWRITES balance from the CSV's own balance column - now detects hasBalanceColumn and reports the statement's actual closing balance, or "not yet known - we don't guess"; regression test mutation-checked). ORCH root-cause fixes in shared files (agents correctly reported rather than cross-edited): ErrorState gained operation="write" + committed=n and no longer claims "nothing was changed" after a failed mutation (that bug was MINE, and it had propagated to 2 screens); lib/trace.ts useTraceId replacing ids that regenerated every render; --color-on-accent token; VerifiedNumber Freshness = boolean|"unknown" so "we couldn't check" is no longer stated as "your inputs are stale" (fails closed either way). All 4 routers wired: 9 endpoints live (/api/today /inbox /inbox/bulk /approvals /approvals/{domain}/decide /health/connections /domains /domains/{domain} /audit). App.tsx routes + Shell nav wired (nav appended never reshuffled per T2). GATES: make verify green (rust+py+eval 13/13+lint+grep-gates), tsc clean, vite build clean, oxlint clean (4 pre-existing fast-refresh warnings), vitest 116/116.
[WS7-E2E-OPEN] NOTE 2026-07-21 ORCH - Carried forward, NOT done: (1) api_bulk.py confirm loop calls record_decision(domain=...) with no row identity, so two previewed rows in one domain issue two identical domain-level decisions and committed_count counts rows not decisions - the SPA now sends exactly the previewed ids but the server does not honour them at row granularity; (2) no RBAC gate on bulk approve or on any /api SPA route (matches the existing HTML flow - needs one decision applied to both surfaces, not a unilateral divergence); (3) frontend tests are NOT in `make verify` (needs node in CI - decide before it counts as a gate); (4) WS7.9 mobile PWA + Lighthouse budget and WS7.2 per-figure Verdict threading through ask.py remain unbuilt.
[WS1.E2-provenance] DONE 2026-07-21 ORCH - Statutory sourcing rescoped + enforced. docs/STATUTORY_SOURCING.md: provenance taxonomy primary|derived|interpretation|unsourced; the EXPERT backlog is 6 questions, not 300 vectors. Root cause found: most vectors cited "MMX-1.0 §WS1.A1" — OUR OWN SPEC — and the CI gate only asserted a `source` key EXISTS, so self-citation passed. New test_vector_provenance: primary requires resolvable citation_url + citation_locator (bare link fails), citing our own spec as primary is rejected, interpretation requires alternatives_considered + un-self-signed ca_initials. Workflow wf_7b1c87b4-2b8 (14 agents, 7 cite + 7 adversarial re-fetch verifiers): 6/7 files PASS. Now 65 pass / 2 deliberate reds. All citations bound to real instruments (incometaxindia.gov.in Act compilations w/ amendment footnotes, thc.nic.in Gazette PDF of Finance Act 2025, esic.gov.in Rules, indiacode.nic.in Companies Act) — zero commentary sites.
[WS1-DEFECTS-FOUND] BLOCKED-HUMAN 2026-07-21 ORCH - The citation pass found REAL DEFECTS in shipped code. Agents REPORTED, did not fix (§0.7: discovered conflict between spec and current law escalates to Human). (1) OFF-BY-ONE AT EVERY TDS THRESHOLD — payables_calc.py:83-86 uses `amount >= single`, but 194C/194J/194I/194Q/194T are all worded "no deduction where the amount does not exceed X", so deduction triggers at amount > X. At EXACTLY the threshold we deduct TDS the law does not require. Verifier quoted the proviso verbatim from the Dept's own text. (2) Vector tds_194j_50k_boundary ENCODES the wrong boundary (expects applicable at exactly ₹50,000) — left unsourced ON PURPOSE so CI stays red rather than sourcing a value the statute contradicts. (3) GRATUITY ELIGIBILITY — statutory.py applies a >=1yr floor to EVERY employee, but CoSS 2020 s.53(1) requires 5 years continuous service; the 1yr rule is the FIXED-TERM-employment exception. (4) NO GRATUITY CEILING — CoSS s.53(3) requires a notified cap; gratuity_hybrid applies none (unbounded). (5) s.2(y) ADD-BACK DENOMINATOR — first proviso aggregates clauses (a)-(i) only; gratuity (j) and retrenchment (k) are outside that span but the engine treats every non-INCLUDED key as excluded. (6) OPEN-WORLD EXCLUSION — s.2(y)'s exclusion list is CLOSED; engine assumes anything not included IS excluded, erring toward UNDER-computing PF/ESI/gratuity (under-paying statutory dues). (7) special_allowance classification is uncited and materially affects 2 expected values. (8) ITC set-off hardcodes IGST->CGST before IGST->SGST though Rule 88A + Circular 98/17/2019 grant "any order and in any proportion" — silent discretionary choice. (9) 234B rounding cited to s.288A in a docstring; s.288A rounds TOTAL INCOME — correct authority is Rule 119A(c).
[WS1-SOURCE-TRAPS] NOTE 2026-07-21 ORCH - Source-integrity findings worth keeping: (a) ESIC publishes TWO inconsistent editions of ESI (Central) Rules r.51 — the "As on 26.04.2024" PDF still prints PRE-2019 rates (4.75%) in the rule body despite its own footnote; the "As on 01.04.2021" edition carries the correct 3.25%/0.75%. Cite the latter. (b) Commentary sites (ClearTax/Kanakkupillai/CAclubindia) state Form 130 issues under s.395 of the IT Act 2025; the agent read the enacted Gazette and could not confirm it — Form 130 left UNSOURCED rather than cite the restatement. (c) MoLE "Additional FAQs on Labour Codes (as on 16.03.2026)" Sl.17 asks our gratuity-transition question DIRECTLY — that may convert interpretation-Q1 into a citable primary answer; needs a fetch+read before we keep treating it as interpretation. (d) A fabricated Finance-Act-2012 footnote attribution in the 234E vector was caught by re-fetch and REMOVED, not reworded.
[WS1.C1-boundary] DONE 2026-07-21 ORCH (owner-approved) - TDS THRESHOLD OFF-BY-ONE FIXED, both engines. payables_calc.tds_on_payment and dif/src/recompute/tds.rs both used `amount >= single` (and `>= aggregate`); every governing proviso exempts the payment where the amount "does not exceed" the threshold, so AT the threshold no duty arises and it begins one paisa above. Cited: s.194J(1) first proviso cl.(B)(i) w/ footnote 43 (Act No. 7 of 2025 subst. "thirty"->"fifty"), s.194I proviso, s.194C(5) — all read verbatim off the Dept's own text and adversarially re-fetched. Changed to strict `>` in BOTH engines in the same change (a one-sided fix would have broken paisa-parity and blocked every 194J claim at the fold gate). Affects 194C/194J/194H/194I single + aggregate. Direction of the error: we were deducting tax the statute does not require, at exactly the threshold.
[WS1.C1-boundary-tests] DONE 2026-07-21 ORCH - The suite was LOCKING THE DEFECT IN. 5 tests asserted the wrong behaviour and all were green: oracle vector tds_194j_50k_boundary (expected applicable:true at exactly ₹50,000 — the agent had refused to source it and deliberately left CI red rather than cite a value the statute contradicts), Python test_194j_professional_above_threshold + test_create_bill_deducts_tds, Rust tds_194j_50k_boundary + t_194j_professional_above_threshold, plus integration fold_recompute_mismatch_blocks. Replaced with PAIRED boundary vectors/tests (at-threshold => no TDS, one paisa above => TDS on the whole sum) in BOTH languages — a single-sided boundary test passes under a constant answer and proves nothing. Non-boundary tests moved off ₹50,000 to ₹60,000 so they test rate/wiring, not the edge. Vector tds_194j_50k_boundary_no_tds_at_threshold is now provenance=primary with citation_url + locator. Statutory oracle: 2 reds -> 1 red (only Form 130 sourcing remains). py 914 pass / 1 red, rust 90+18+4 all green.
[LESSON] NOTE 2026-07-21 ORCH - Both engines agreed to the paisa on the wrong answer for the entire life of the project, and Python<->Rust parity + the fuzz harness could never have revealed it: parity proves CONSISTENCY, the oracle proves CORRECTNESS. This is the concrete justification for §0.6 and for keeping an external truth anchor even though the product is for CFOs rather than CAs.
[SESSION-RECON] NOTE 2026-07-22 ORCH - The 2026-07-21 late session crashed before logging. Entries below reconstructed from the artifacts themselves (docstrings, migrations, tests), then the WHOLE tree proven by one `make ci` run before commit — nothing below is claimed beyond what that gate proves.
[WS4.3] DONE 2026-07-21 (logged 07-22) - AUTH-CONSOLIDATE: ONE authentication path. Better Auth (TypeScript layer, owner-configured) issues JWTs; app/core/betterauth.py only VERIFIES via real JWKS (EdDSA/Ed25519 pinned allow-list, never "none", never token-chosen; exp/iss/aud all required) and resolves a Principal. §0.8 RLS binding: engine checkout event re-binds app_current_org GUC from the VERIFIED claim on EVERY pooled-connection checkout (request B can never inherit request A's org; unauth -> NULL -> matches no rows). infra/db/multitenant/003_identity.sql: password_credentials/mfa_totp (per-user, no org dim — matches app_users precedent) + tenant-scoped sessions. tests/integration/test_auth_e2e.py drives the REAL app over REAL HTTP with a REAL Ed25519 keypair served from a live localhost JWKS endpoint; includes the forged-principal mutation test (inserting `return Principal(attacker)` at the top of get_principal fails the suite — the identity must come from THIS request's token).
[WS5.1-wiring+WS6.1-wiring] DONE 2026-07-21 (logged 07-22) - rbac_deps.py: pure rbac.can() unchanged; dependency layer turns denial into a 403 naming the missing capability (not the resource), denial audit-chained; role from betterauth Principal only. entitlement_deps.py: require_feature/require_quantity dependency factories over entitlements.guard(); plan resolution per §0.8 (no third source). test_rbac_matrix.py + test_entitlement_routes.py drive real routes.
[WS4.2] DONE(replay+importer) 2026-07-21 (logged 07-22) - Alembic 0002 multitenant-core replay (tenant_core schema; tenancy+domain+identity tables EACH with its RLS policy in the same revision, SQL inlined for migration immutability) + 0003 vault retention 7y->8y recompute for existing rows (the WS1.C5-migration debt, now paid) + test_import_roundtrip.py (SQLite tenant import w/ checksum reconciliation).
[WS10.4] DONE(drafts) 2026-07-21 (logged 07-22) - Legal kit: docs/legal/{TOS,PRIVACY,DPA}_DRAFT.md + DISCLAIMERS.md (drafts FOR COUNSEL REVIEW, no compliance asserted — §0.6 applied to legal claims); app/core/legal.py versioned acceptance mechanics (re-accept on version bump); alembic 0004 legal_acceptance table WITH RLS in-revision; served + acceptance-logged; test_legal.py.
[WS7.4+WS7.7-api] DONE 2026-07-21 (logged 07-22) - api_domains.py (five hubs + CA Audit Room JSON, reusing collect_health/collect_kpis/badge_state — §0.4 single honesty gate) + api_health.py (connection-health/staleness; route owns the clock, app/core/freshness owns the logic) + test_freshness.py. Fixes the WS7-E2E finding (a): /api/health/connections is now REGISTERED in main.py.
[WS4.8] DONE 2026-07-21 (logged 07-22) - THE gate: scripts/ci_gate.sh is the single list of steps; `make ci` and .github/workflows/verify.yml both run it VERBATIM (local/CI drift is impossible by construction). cargo build FIRST so mahsa-needing integration tests can never silently skip; adds frontend gate (npm ci + tsc + vitest + oxlint) — closing WS7-E2E-OPEN item (3). KNOWN_STATUTORY_GAPS mechanism: oracle failures must equal the declared gap set EXACTLY — an undeclared failure is a regression (red), a stale entry is red too, so a permanently-red-step culture cannot form. Plus check_money_format.sh grep-gate wired into make gates.
[WS1.E2-form130] DONE 2026-07-22 ORCH - THE LAST ORACLE RED CLOSED. regime_form_map_salary_cert_2025 unsourced->primary: the Department's own forms document ("Form No. 130_131_132_133 (Earlier Form No. 16/16A/16B/16C/16D/16E/27D)") retrieved via Internet Archive snapshot (20260412144536) of the Akamai-blocked incometaxindia.gov.in URL and read verbatim (pages 1-2): Form 16 -> Form No. 130 under s.395(4) of the Income-tax Act 2025, rule 215 of I.T. Rules 2026, issued against Form 138, due 15 June following the Tax Year. Also confirms 16A->131, 16B/C/D/E->132, 27D->133 for the wider map. The commentary sites' "s.395" claim the 07-21 agent refused to cite is now departmentally confirmed as s.395(4). KNOWN_STATUTORY_GAPS in ci_gate.sh emptied per its own stale-entry rule. Oracle 69/69, zero gaps. `make ci` ALL GREEN (19 steps: cargo build/test/clippy, ruff, mypy, 4 grep-gates, unit, integration, oracle-gaps-vs-regressions, E2E real loop, live RLS red-team, golden eval, npm ci, tsc, vitest, oxlint).
[fix:itc-order] DONE 2026-07-22 OPUS - Defect #8: itc_setoff hardcoded IGST->CGST-before-SGST; Rule 88A r/w Circular No. 98/17/2019-GST (para 3 + para 4 table rows II/III, fetched live from cbic-gst.gov.in, quoted verbatim) permits "in any order and in any proportion" with IGST credit "completely exhausted mandatorily". Replaced with the CASH-MINIMIZING allocation in BOTH engines identically (api/app/domains/gst/gst_calc.py::itc_setoff + dif/src/recompute/itc.rs): uncovered needs (liability minus own credit) first, CGST-before-SGST tie-break (total cash invariant to it), then mandatory exhaustion CGST-first; deterministic, clock-free, integer paise. Order-choice recorded per owner decision 2026-07-22 as provenance=interpretation, ca_initials OWNER, alternatives_considered (fixed CGST-first = old engine/GSTN default, proportional split, SGST-first) in ws1d_itc_setoff.yaml + 2 new vectors where old and new figures DIFFER (3000->1000 and 10000->5000 paise cash); 2 pre-existing primary vectors unchanged (order-invariant, re-verified). PAIRED boundary tests both engines (needs exactly covered -> 0 cash; one paisa short -> 1 paisa cash) + mandatory-exhaustion carry-forward test. Green: cargo test 115 (incl parity.rs oracle leg), pytest gst+oracle 138, parity fuzz + verified flow 20 (against rebuilt mahsa binary), ruff+mypy clean.
[WS1.C-labour-codes] DONE 2026-07-22 AGENT - Defects #3/#4/#5/#6/#7 from [WS1-DEFECTS-FOUND] fixed in BOTH engines in the same change (paisa-parity kept; parity.rs replays every vector). (#3) gratuity eligibility: CoSS 2020 s.53(1) FIVE-year floor ("continuous service for not less than five years", read verbatim off aA2020-36.pdf) now the default; the 1-year floor confined to fixed_term=True (s.53(1) second proviso + MoLE FAQ 16.03.2026 Sl.14/19) — gratuity_hybrid gained a fixed_term param (Py+Rust+dispatcher+parity). The old oracle vector paying a 2-year non-FTE ₹30,000 LOCKED THE DEFECT IN; replaced with PAIRED at-threshold/one-short vectors on BOTH floors (5y payable / 4y364d nil; FTE 1y payable / 11mo nil; 2y non-FTE nil vs 2y FTE ₹30k). (#4) s.53(3) ceiling implemented at ₹20,00,000 in gratuity_hybrid AND gratuity_required: cap existence primary (s.53(3) verbatim); AMOUNT is S.O. 1420(E) 29-03-2018 (PoG s.4(3), PIB-confirmed via wayback 20240217082025) carried over by s.164(2)(a) ("deemed to have been done ... under the corresponding provisions of this Code") — no CoSS-specific s.53(3) notification could be sourced, so the carry-over ships provenance=interpretation ca_initials OWNER with alternatives (incl. no-cap-until-notified, rejected). Paired under/over ceiling vectors (₹19,61,538 passes through / raw ₹20,76,923 clamps to exactly 200,000,000 paise). (#5) s.2(y) add-back numerator now aggregates clauses (a)-(i) ONLY per the verbatim proviso; gratuity (j) and retrenchment/ex-gratia (k) excluded from wages but OUTSIDE the span — statutory_wage_base grew a third bucket (excluded_terminal) through Py, Rust fn, claim inputs, dispatcher, parity, fuzz. Denominator read as ALL money components incl. (j)/(k) — interpretation, alternatives recorded. (#6) exclusion list is CLOSED ("but does not include—(a)...(k)", full list quoted verbatim in ws1b_wage_base.yaml): unknown components now INCLUDE in wages (err toward paying dues). MoLE FAQ Sl.13 reads the opposite way and is RECORDED as a rejected alternative (FAQ is not an instrument; opening limb says "all remuneration"). (#7) special_allowance reclassified INCLUDED — RPFC (II) WB v. Vivekananda Vidyamandir (SC 28-02-2019, indiankanoon 126246109) universality test + no matching (a)-(k) clause; provenance=interpretation ca_initials OWNER; wiring vector proves PF ₹1,680 / ESI ₹105/₹455 on the ₹14k base (old classification gave ₹10k). All owner-decided interpretations per the 2026-07-22 no-external-CA decision. KNOWN_STATUTORY_GAPS untouched (empty — everything sourced). NOT FIXED (recorded): s.53(2) "or part thereof in excess of six months" final-part-year round-up is not implemented (engine counts whole anniversary years); vectors use whole-year inputs so no expected value depends on it — needs its own ticket. Tests: api unit 793 pass/1 skip, payroll+oracle 141 pass, parity fuzz + full loop 32 pass vs real mahsa; dif cargo test 97+18+1+4 green, clippy -D warnings clean; ruff+mypy clean. 12 pre-existing integration failures (401s from concurrent auth work in rbac_deps/main.py, upstream of payroll math) are NOT from this change — payroll full-loop and parity fuzz pass.
[fix:rbac-api] DONE 2026-07-22 OPUS - WS7-E2E-OPEN(2): RBAC on EVERY /api route + HTMX mutate/approve routes, one decision both surfaces. All 68 /api routes now declare a capability via rbac_deps (router-level `read` baseline; mutations add `write`; expense claim-approve `approve_payment`; audit room `view_audit`). NEW rbac_deps.require_filing(action): WS5.2 statutory hard gate WIRED to approval_matrix.decide_approval (Owner/Admin only, stricter than the approve_filing capability — Approver refused; action validated against STATUTORY_FILING_ACTIONS at import) on gst/gstr3b, tax/tds-returns, compliance/deadlines/{id}/file. HTMX: /approvals/{domain}/decide gated approve_payment + attributed to VERIFIED principal (was settings.default_user_id; now uses get_mahsa seam), /inbox/bulk mirrors JSON preview(read)/commit(approve_payment) split + principal attribution, action drawer + vault OCR + history/capture + parallel start/observe gated `write`. require() closures carry required_capability marker -> test_rbac_matrix coverage guard: deployed /api route set must EQUAL hand-written API_ROUTE_GATES table AND each route's declared markers must match (new unguarded route = red CI); full matrix test = 68 routes x 6 roles over real HTTP with real Ed25519 JWKS tokens (plan=growth so 402 never masks RBAC), denials assert the FIRST failing gate's exact detail. Spec negative held: Accountant cannot approve anywhere (JSON, HTMX single, HTMX bulk). Unit: require_filing paired grid + marker tests. Fixtures: semantics tests (test_app, test_api_domains) use the blessed get_principal Owner override, matrix stays on real signed tokens. Green: py 801u+202i+95 oracle, mypy 180 files, ruff, Rust 120 (untouched — no money math changed). Skipped: email-send routes (/investor/send, /cfo/brief/send) left ungated — no book mutation; gate with `export` if leak surface matters.
[fix:bulk-rows] DONE 2026-07-22 OPUS - WS7-E2E-OPEN(1): bulk decisions now carry ROW identity end-to-end. record_decision(+item_id) puts "[row <id>]" inside the hashed audit `query` (core_payload-safe — a NEW payload key would break verify_chain recomputation, so identity rides in an already-hashed field; history untouched) + Decision.item_id column (nullable; alembic 0005_decision_item_id, pre-fix rows/whole-domain approvals stay NULL, pre-fix audit shape byte-identical when item_id absent). Both confirm loops (api_bulk.py JSON + exceptions_router.py HTMX) pass item_id=row.id: two rows in one domain = two DISTINGUISHABLE sealed decisions; an id outside the preview is reported skipped and never decided; committed_count = rows actually decided. Response contract unchanged -> zero frontend change. Tests: unit x2 (two-rows-one-domain distinguishable in audit+Decision, chain still verifies; no-item_id keeps exact pre-fix query) + integration x2 (real app/Mahsa: 2 rows/1 domain -> 2 distinct audit entries + item_ids; stray id on confirm rejected, no entry names it). Mutation-checked: reverting item_id=row.id makes both new integration tests FAIL (verified live, then restored). 814 py unit+bulk green, ruff+mypy clean, dif cargo test green (no money math touched — no Rust mirror needed).
[fix:verdict-ask] DONE 2026-07-22 SONNET - WS7-E2E-OPEN(4b): per-figure app.core.verify.FigureVerdict threaded through Ask Maisha. Root cause: ask.py's Figure carried only an ad-hoc "check"/"pending"/"warn" string derived straight from mahsa_coverage, a SECOND vocabulary alongside the FigureVerdict type the on-demand recompute path (verify.py) already returns — no drift bug yet, but the richer verified/blocked/honest_pending object was never attached, so the HTMX working-panel always rendered its honest "nothing sealed" stub even under a ✓ badge (the WS7.2 FLAG). Fix (no new verification logic, no new Mahsa call): new `_verdict(target, fact_backed)` in app/core/ask.py is now the SINGLE source of truth (not fact-backed -> blocked; fact-backed + mahsa_coverage.is_recomputed -> verified; fact-backed + unported -> honest_pending, never upgraded); `_badge` is now a thin projection of it, so the two representations cannot diverge. Figure gained a `verdict: FigureVerdict` field threaded by `_figures`; answer_card.html's vstate now reads `f.verdict.verified`/`.honest_pending` directly instead of re-parsing the badge string (unknown falls to "unbacked", never "verified"). No SPA Ask surface exists yet (checked App.tsx/routes — none), so nothing to wire there. Tests: test_figures_thread_real_verdict_for_ported_and_unported (ported+unported+invented-number in one response show verified/honest_pending/blocked correctly, off the real mahsa_coverage.json) + test_verdict_lookup_is_real_not_hardcoded (monkeypatches is_recomputed to always True, proves the unported target's verdict was genuinely driven by the live lookup, not hardcoded) + existing test_verified_number.py fixture updated to pass real FigureVerdict objects. 805 py unit green (50/50 on `-k "ask or llm"`), mypy+ruff clean on touched files. No money math changed -> no Rust mirror / oracle vector update needed.
[WS1-DEFECTS-FIXED] DONE 2026-07-22 ORCH - Workflow wf_f64e8354-07d (12 agents, 0 errors): defects #3-#9 + WS7-E2E-OPEN (1)(2)(4b) fixed, each adversarially verified w/ mandatory mutation-check. Owner decision recorded: no external CA — research-verified primary citations are the truth source; genuine interpretation choices implemented + documented provenance=interpretation ca_initials OWNER. fix:labour-codes (#3 gratuity 5y floor per CoSS s.53(1), fixed_term=True carries the 1y exception; #4 ₹20L ceiling s.53(3) via s.164(2)(a) carry-over of S.O. 1420(E); #5 add-back span clauses (a)-(i) only, (j)/(k) in new excluded_terminal bucket; #6 CLOSED exclusion list — unknown components INCLUDE in wages, err toward paying dues; #7 special_allowance INCLUDED per Vivekananda Vidyamandir universality test) — BOTH engines same change, defect-locking vectors replaced with PAIRED statute-cited vectors, verifier re-fetched both instruments (indiacode aA2019-29/aA2020-36 PDFs) and killed 5/5 mutations. fix:itc-order (#8: Rule 88A + Circular 98/17/2019 — cash-minimizing IGST allocation across CGST/SGST, deterministic, both engines identical, parity fuzz green, order-choice documented as interpretation w/ alternatives). fix:rbac-api (WS7-E2E-OPEN (2): all 68 /api routes declare capabilities via rbac_deps; statutory filing hard-gated Owner/Admin; Accountant-cannot-approve negative; route×role coverage guard fails on any new unguarded route). fix:bulk-rows (WS7-E2E-OPEN (1): record_decision carries row identity end-to-end; decision.item_id queryable mirror, audit chain extended never mutated; confirm processes EXACTLY the previewed ids, mismatch rejected; committed_count = decisions). fix:verdict-ask (WS7.2 remainder: per-figure Verdict threading through ask.py from the existing verify path; unported stays honest-pending; fabricated-verified impossible by construction). Verify round: 5 PASS, 1 FAIL (citations-234b residuals) — ORCH reconciled: 234B docstring now says the SHORTFALL (s.234B(1) interest base) is what Rule 119A(c) rounds; round-down was TEST-LOCKED in NEITHER engine (all prior inputs already ₹100 multiples — vacuous) → rounding-exercising paired tests in both engines + oracle vector interest_234b_rule_119a_rounddown_exercised (₹1,00,050 → base ₹1,00,000; without rounding the figure differs, so parity replay kills the mutation cross-engine). Also fixed 0005_decision_item_id vs create_all-baseline duplicate-column (existence guard; fresh + pre-existing DBs both migrate). make ci ALL GREEN (19 steps). WS1-DEFECTS-FOUND fully closed.
