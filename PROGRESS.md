# PROGRESS — append-only work log (MMX-1.0 §0.2)

Format: `[ticket-id] [status] [date] [agent] [commit] [note]`
Statuses: `DONE` · `WIP` · `BLOCKED-CA` · `BLOCKED-HUMAN` · `DEFERRED`.
This is the only place agents record status. Never mark the immutable spec (`docs/MASTER_PLAN.md`).

---

[GOV] DONE 2026-07-20 ORCH - Governance bootstrap: spec moved to docs/MASTER_PLAN.md (444), CLAUDE.md pointer added, PROGRESS.md + PROGRESS_BOARD.md created, CODEOWNERS + CI guard (fail-on-touch) for the immutable spec.
[WS1.E1] DONE 2026-07-20 ORCH - Statutory-oracle framework: vector schema {id,statute,section,source/citation_url,ca_initials,ca_date,target,inputs,expected}, YAML vectors under api/tests/statutory_oracle/vectors/, target registry + CI-blocking pytest runner (picked up by `make test-py`). Rust-parity leg is a hook only (WS3, not yet ported). Seeded with the 5 defect vectors.
[WS1.C1] DONE 2026-07-20 ORCH - 194J threshold 30k→50k (single + aggregate). Value cited: docs/MASTER_PLAN.md §WS1.C1 (Finance Act 2025). Locked by oracle vector tds_194j_fy2526. ca_initials PENDING.
[WS1.C2] DONE 2026-07-20 ORCH - 194I per-month restructure: ₹50,000 per month or part thereof, TDS on full month's rent, annual-aggregate logic removed for 194I. Locked by vectors tds_194i_40k_nomo / tds_194i_55k_full. Value cited: §WS1.C2. ca_initials PENDING.
[WS1.C3] DONE 2026-07-20 ORCH - ESI truncate-then-round root-cause fix: ceil applied on the exact Decimal product before any int truncation; every `int(Decimal(...)*rate)` in payroll/statutory.py removed. Anti-pattern grep-gate scripts/check_no_truncate_round.sh added. Locked by vector esi_20001_gross (151,651).
[WS1.C4] DONE 2026-07-20 ORCH - Company tax 115BAA path = 22%+10% surcharge+4% cess (25.168%), MAT excluded; caller chooses regime explicitly (regime_115baa). Non-115BAA company base rate has no CA vector → that branch raises BLOCKED-CA. Locked by vector company_115baa_1cr. Value cited: §WS1.C4.
[WS1.C5] DONE 2026-07-20 ORCH - Vault statutory retention 7y→8y computed from FY-end (not upload date). Locked by vector retention_invoice_8y_fy_end. Migration for existing rows: DEFERRED to WS4.2 (multi-tenant migration engineering) — noted below.
[WS1.C5-migration] DEFERRED 2026-07-20 ORCH - Recompute stored retention_until for existing vault rows. Belongs with WS4.2 Alembic/Postgres migration work; pure-calc fix already live so all NEW rows are correct.
[WS1.A1] DONE 2026-07-20 OPUS - statutory_regime module (api/app/core/statutory_regime.py): regime selector on earlier-of-credit-or-payment vs 2026-04-01 boundary, form map (16→130/16A→131/24Q→138/3CD→26/15GH→121), Tax Year label, payment-code loader shipped EMPTY (codes BLOCKED-CA §0.6). SPEC-WS1A.md written. 6 oracle vectors (ids regime_*). Verify `pytest tests/statutory_oracle -k regime` green (6/6).
[WS1.A2] BLOCKED-CA 2026-07-20 ORCH - Citation re-point + scripts/check_no_stale_citations.sh. Blocked on the 1961→2025 SECTION map (spec enumerates only the FORM map); a hard grep-gate now would fail CI across domains that legitimately still use 1961 forms until regime_for is wired into the TDS engines. Unblock: CA-sourced section map + payment codes.
[WS1.A3] BLOCKED-CA 2026-07-20 ORCH - Regenerate Form 138/130/131 + challan payment codes. Blocked: needs CA-provided samples in tests/statutory_oracle/fixtures/ (§0.6). No fixtures present.
[WS1.B1] DONE(core) 2026-07-20 OPUS - Code on Wages s.2(y) engine (api/app/core/statutory_wage.py): statutory_wage_base(components, in_kind) = Basic+DA+retaining + excluded-excess-over-50% add-back + in-kind ≤15%. 4 algorithm-derived oracle vectors (compliant/add-back/overtime/in-kind). In-kind %-base is an interpretation flagged BLOCKED-CA (§0.7). REMAINING: route PF/ESI/gratuity/bonus/leave-encashment through it (mechanical; safe for existing basic-only tests since wage_base==basic there) — tracked as WS1.B1-wiring.
[WS1.B1-wiring] DONE 2026-07-20 OPUS(fanout) - Rewired payroll PF/ESI (+ bonus/ECR) to compute on statutory_wage_base(components) at the caller level (compute_components/build_snapshot/ecr_text via _structure_wage_base); pure scalar functions in statutory.py unchanged, so basic-only tests hold (wage_base==basic). Oracle vector wiring_pf_esi_on_underweighted_ctc (payroll_components target, end-to-end). Opus-verified PASS, reverify green.
[WS1.B2] DONE 2026-07-20 OPUS(fanout) - statutory.gratuity_hybrid(doj, exit, boundary=2025-11-21, old_base, new_base): pre-boundary years on last-drawn Basic, on/after on s.2(y) base, 15/26 reused, FTE ≥1yr gate, exact Decimal→single rupee round. 3 gratuity_hybrid vectors. BLOCKED-CA-flagged: the year-apportionment convention at the boundary is a computational interpretation (no statutory number invented). Opus-verified PASS (64 payroll+oracle green).
[WS1.B3] DONE 2026-07-20 SONNET(fanout) - check_ctc_compliance + _rebalance_suggestion + PayrollService.validate_ctc: warns when Basic+DA < 50% (reuses statutory_wage.EXCLUDED_CAP_FRACTION, no hardcoded 0.5), proposes total-preserving restructure (trim special→LTA→HRA), never mutates stored structure; required-minimum ceil'd. 8 unit+service tests. Opus-verified PASS. (Verifier noted service.py also carries the B1-wiring money-math change — that is the WS1.B1-wiring ticket's work, separately vector-locked & passed; UI/API test deferred: dispatched scope was unit + one service test.)
[WS9.3] DONE 2026-07-20 SONNET(fanout) - Draft-IRN honesty: DRAFT_IRN_LABEL (byte-for-byte per §WS9.3 line 169) on the sole local IRN surface gst_calc.einvoice_payload (IrnStatus + QrData.Caption); scripts/check_no_draft_irn.sh gate (wired into make gates by ORCH); label test in tests/unit/gst. No PDF/QR-image invoice surface exists yet — gate catches future ones. Opus-verified PASS.
[WS1.D8] DONE 2026-07-20 SONNET(fanout) - payables_calc.msme_form1_period (half-year window + due 31 Oct / 30 Apr per §WS1.D8) + msme_form1_pack (MSME vendors outstanding >45d, per-vendor lines, due date); dates injected. Fixture snapshot test. Opus-verified PASS (values cited to spec, nothing invented).
[FANOUT-1] DONE 2026-07-20 ORCH - Parallel workflow wf_d543bdb8-e63: 3 impl streams (opus payroll→sonnet B3; sonnet WS9.3; sonnet WS1.D8) + 4 opus adversarial verifiers + 1 opus gate. 9 agents, 0 errors. Independent make verify green (463 py + rust + eval 13/13 + lint + 2 grep-gates), 21 oracle vectors.
[WS3.1] DONE 2026-07-20 OPUS(fanout+ORCH) - Rust recompute port (Prime Directive): new mahsa::recompute module — slab_tax, pf_esi (+s.2(y) wage base), tds, itc, gst_fees, gratuity_bonus, all pure integer-paise mirrors of the Python. ORCH scaffolded module tree + shared round_rupee/ceil_rupee + tests/parity.rs (replays the shared oracle vectors against Rust → paisa parity by construction; honest coverage report for unported targets). 4 ported targets vector-locked (esi, statutory_wage_base, tds_on_payment, gratuity_hybrid); slab/itc/gst_fees proven by Rust unit tests mirroring Python. Rust 95 tests incl. parity. ORCH fixed a double-rounding parity defect in tds.rs (single round-to-rupee to match Python; the 4 vectors used round-rupee amounts and hid it — found by the opus verifier, regression test added for ₹50,004.95). Verifiers: 5 PASS + 1 FAIL(tds, now fixed). make verify green.
[WS3.1-remaining] NOTE 2026-07-20 ORCH - Not ported yet (coverage gaps printed by parity test): itr_computation(115BAA), regime_for, form_name, retention_until, payroll_components(integration). WS3.2 randomized cross-language proptest fuzz (Python↔Rust bridge) not built — vector-parity is the current gate.
[FANOUT-2] DONE 2026-07-20 ORCH - Parallel workflow wf_433e0596-8c3: 6 disjoint Rust fills (opus: slab/pf_esi/tds/gratuity; sonnet: itc/gst_fees) + 6 opus parity verifiers + 1 opus gate. 13 agents, 0 errors. Opus verifier caught the tds double-rounding defect the green suite hid — the value of adversarial verify.
[WS3.3] DONE 2026-07-20 OPUS(fanout) - Killed default-healthy: every fold module's missing-signal default flipped 1.0→0.0 (§0.4: absent = unknown, never healthy); Completeness{expected,missing,complete} on the Fold struct + per-module EXPECTED_SIGNALS; old defaults_to_healthy tests inverted + absence-behaviour tests added. Opus-verified PASS. ORCH fix: payroll.rs edit was lost to a concurrent-write race (only module missing EXPECTED_SIGNALS → gate compile error) — restored to match siblings.
[WS3.4] DONE 2026-07-20 OPUS(fanout) - Verdict object app/core/verdict.py: {figures[{key,value_paise}], rule_pack_version, org_id, hash}; hash reuses core.audit.canonical_json+sha256 (no new dep); org_id keyword-only, session-sourced (§0.8), bound into hash; FoldResult.verdict() surfaces it. 7 binding tests (tamper/order/org/version all detectable). Opus-verified PASS.
[WS3.5a] DONE 2026-07-20 SONNET(fanout) - mahsa_coverage.json (repo root) + app/core/mahsa_coverage.py: derived from dif/tests/parity.rs PORTED + oracle targets; is_recomputed/badge_state (verified iff Rust-recomputed, else honest_pending; unknown defaults honest_pending). Opus-verified PASS.
[WS3.5b] DONE 2026-07-20 SONNET(fanout) - Honest-state badges: answer_card.html tri-state (✓ recomputed / ◐ honest-pending / ⚠ unbacked) driven by mahsa_coverage.badge_state, +CSS, coverage-driven badge test (not hardcoded). Opus-verified PASS.
[FANOUT-3] DONE 2026-07-20 ORCH - Parallel workflow wf_14c79357-930: 3-way safe-parallel (opus WS3.3 Rust ‖ opus WS3.4 verdict Py ‖ sonnet WS3.5a coverage Py) → sonnet WS3.5b → 4 opus verifiers + opus gate. 9 agents, 0 errors. Gate initially RED on a lost-edit compile error in payroll.rs (concurrent-write race); ORCH root-caused + fixed; independent make verify green (Rust 95→ incl parity + 476 py + eval 13/13 + lint + grep-gates).
[WS3-live-wiring] DONE 2026-07-20 ORCH - Wired recompute into the LIVE /fold path (Prime Directive §0.4, done directly not via fanout). FoldRequest gains recompute_claims[{target,inputs,claimed_paise,label}]; fold_handler recomputes each via recompute::check_claims and, on a recomputable MISMATCH, inserts a MAHSA-PARITY-001 Block rule + escalates status to Red BEFORE unfold (so the shape/requires_approval reflect it); an unrecomputable target stays honest-pending (recomputed_paise=None), never a block. FoldResponse gains recompute[]. 12 targets dispatched (esi_employee/employer, pf_*, eps, statutory_wage_base, tds_on_payment, annual_income_tax, gratuity_hybrid/required, bonus, late_fee/interest_3b). Python mahsa_client: RecomputeClaim/RecomputeCheck models + fold(recompute_claims=) send + parse; honest_pending property. Tests: 4 Rust dispatch unit + 3 Rust integration (mismatch blocks / correct verifies / unrecomputable honest-pending) + 3 Python parse. Backward-compatible (empty claims = prior behaviour). Rust 107 tests, py 479. make verify green.
[WS7.3] DONE 2026-07-21 OPUS(fanout) - Today view (Owner landing): app/web/today.py assembler + today.html + today_router.py (GET /today) + today.css. 3 regions: cash strip (runway via Verified-Number chip / ◐ honest-pending), Needs-you queue (real pending_approvals, one-tap + ₹), Trouble radar (deadlines in alert grammar what/when/₹-consequence/one-tap, ranked by ₹). Penalties-avoided badge-backed (real avoided fees) or labelled estimate — never invented. ORCH wired router+nav. Opus-self-verified green.
[WS7.5] DONE 2026-07-21 OPUS(fanout) - Exception Inbox (research #1: Xero 243-vote bulk-reconcile): app/web/exceptions.py + exception_inbox.html + inbox_bulk_preview.html + exceptions_router.py (GET /inbox, POST /inbox/bulk) + css. 5 queues; Awaiting-approval ← approvals, Mahsa-blocked ← recompute MAHSA-PARITY/FigureVerdict.blocked; Needs-doc/categorization/Feed-broken honest-empty (no fabricated items). KEY: bulk-ops WITH PREVIEW — dry-run lists affected rows + ₹/ITC impact, no mutation until confirm=true. Items ranked by ₹ impact. ORCH wired router+nav. Opus-self-verified green.
[WS7-TODAY-INBOX] DONE 2026-07-21 ORCH - Parallel workflow wf_6b8c7dff-31d: WS7.3+WS7.5 (2 opus) — Build phase 2/2 done; verify+gate agents hit session cap so ORCH wired routers (main.py include_router today+inbox) + nav (base.html Today/Exception-Inbox) + ran the gate. make verify green. FLAG: app 500-handler still 'Something went wrong' (research anti-pattern #6) → WS7 follow-on (4-question error template).
[WS7.1] DONE 2026-07-20 OPUS(fanout) - Design-system foundation (research-grounded): tokens.css verification family --c-verify teal/◐ indigo/✕ violet STRICTLY separate from money green/red (fixes the .vmark--ok=money-green trust-confusion bug) + .tnum tabular numerals; format.py canonical inr()/inr_rupees() lakh/crore renderer (₹12,34,567) as the single money surface (delegates to ported money.py, untouched); scripts/check_money_format.sh QG.3 lint (proven catches py+template violations; baselines 2 pre-existing: eway.py machine-artifact grouping + actions.py:79 toast); docs/WS7_DESIGN_TOKENS.md component-states inventory. 43 web tests. Opus-verified PASS.
[WS7.2] DONE 2026-07-20 SONNET(fanout) - Verified-Number chip + Working panel (answers research T1 silent-drift): native <details> chip (no JS) using verification tokens (✓ teal/◐/✕ — NOT money green); working_panel.html = inputs→formula→citations→documents→verdict-hash→report-issue, driven by verify.py/verdict.py (honest-pending→◐ never fabricated ✓; missing data→honest 'not yet sealed'); ◐→✓ lock-in keyframes; verified_number.css (app.css untouched, scoped override). 7 render tests. FLAG: ask.py Figure/Answer lack per-figure Verdict → verified figures show honest partial panel; threading real Verdict through ask flow = follow-on. Opus-verified PASS.
[WS7-FOUND] DONE 2026-07-20 ORCH - Parallel workflow wf_5a0de475-bc2 (English-only; i18n/WS7.10 dropped per user): WS7.1→WS7.2 + 2 opus verifiers + opus gate. 5 agents, 0 errors. Grounded in docs/WS7_UX_RESEARCH.md. make verify green (768 py + rust incl fuzz + eval + lint + grep-gates).
[WS7-UXR] DONE 2026-07-20 ORCH - WS7 UX research (grounds the UI build; feeds WS7.V). Parallel workflow wf_dc73660b-ee7: 8 research angles (real WebSearch+WebFetch on Reddit/Capterra/SoftwareAdvice/CA advisories/HN) → opus adversarial citation verifier PER ANGLE (re-fetched URLs, DROPPED 12 fabricated/unsupported findings) → opus synthesis. 17 agents, 0 errors, 430 web fetches, 81 VERIFIED cited findings. docs/WS7_UX_RESEARCH.md (434 lines): 12 ranked painpoint themes, per-theme design decisions, validate/challenge mapping to every WS7 ticket, 14 competitor anti-patterns, WS7.V open questions. Key: T1 silent number-drift VALIDATES our recompute+badge+append-only thesis; Xero 243-vote/3yr bulk-reconcile = loudest signal → WS7.5. Ready to build WS7.1 design foundation next.
[WS1.D2] DONE 2026-07-20 OPUS(fanout) - QRMP (new app/domains/gst/qrmp.py; gst_calc untouched): filing_calendar(profile monthly|qrmp|composition), pmt06_fixed_sum=35% prev-quarter cash (spec value, exact paise) + self-assessed; QRMP→quarterly GSTR-1/3B + monthly PMT-06 + IFF(m1-2); obligation_penalty reuses ported late_fee_3b/interest_3b unchanged. 10 tests. BLOCKED-CA: statutory due-date calendar days (injected; pending_ca if absent). Opus-verified PASS.
[WS1.D3] DONE 2026-07-20 SONNET(fanout) - CMP-08 (new app/domains/gst/cmp08.py): build_cmp08 quarterly composition statement (outward tax at injected composition_rate, RCM inward at normal rate via ported rcm_liability, interest via ported interest_3b). 7 tests. BLOCKED-CA: composition rates 1/5/6% (mandatory param, raises if absent — never invented). Opus-verified PASS.
[WS9.2] DONE 2026-07-20 SONNET(fanout) - Bank CSV parsers: existing TreasuryService.import_csv is a GENERIC header-alias parser; verified empirically it already normalizes SBI/Kotak/Yes/IndusInd/IDFC/Federal/RBL → 7 realistic fixtures + per-bank + parametrized contract tests (same normalized shape, exact paise). ZERO code change (ponytail reuse). Opus-verified PASS.
[WS8.1] DONE(core-slice) 2026-07-20 SONNET(fanout) - Audit Pack app/core/audit_pack.py: build_audit_pack → TB/P&L/BS/GL sections, each figure {label,value_paise,badge(mahsa_coverage.badge_state),evidence_ref} + pack integrity hash (canonical_json+sha256, tamper-evident). Excel/PDF + statutory registers/26AS/MSME-ageing = follow-on. Tests: 4 sections, honest-pending not badged verified, tamper-evident hash. Opus-verified PASS.
[P0D-P2-R1] DONE 2026-07-20 ORCH - Parallel workflow wf_3fce87e4-744: [D2→D3 pipeline] + WS9.2 + WS8.1 + 4 opus verifiers + opus gate. 9 agents, 0 errors. No ported path touched (fuzz green). Independent make verify green (747 py + rust + eval + lint + grep-gates).
[WS1.D1] DONE 2026-07-20 OPUS(fanout) - New TDS/TCS sections (payables_calc, ADDED not modifying ported tds_on_payment): 194Q 0.1% on excess >₹50L/vendor/FY + TDS-primacy suppresses TCS 206C(1H); 194T 10% >₹20k; TCS s.394 + 206AA/206AB higher-rate overlay apply_higher_rate (separate, not baked into tds_on_payment). SPEC-WS1D1.md. 14 unit tests (boundaries + primacy). BLOCKED-CA (params that RAISE when fired without value, never invented): TCS-goods rate+threshold, 206AA no-PAN + 206AB non-filer floor rates. Opus-verified PASS.
[WS1.D4] DONE 2026-07-20 SONNET(fanout) - IMS workflow (new app/domains/gst/ims.py; gst_calc/itc_setoff untouched): accept→eligible/reject→ineligible/pending<deadline→pending/pending≥deadline→deemed_accepted; ims_disposition + eligible-ITC aggregate (paise). 7 tests. BLOCKED-CA: exact deemed-accept deadline (injected date param, no clock). Opus-verified PASS.
[WS1.D7] DONE 2026-07-20 OPUS(fanout) - E-way bill (new app/domains/gst/eway.py): eway_required (₹50k inter-state boundary spec-cited; intra thresholds injected from WS2 packs=BLOCKED, returns pending not a guess) + build_eway_json NIC Part-A/B artifact with EWB_PREPARE_LABEL honesty marker (prepare-and-download, not filed). 4 tests. Opus-verified PASS.
[P0-D-R1] DONE 2026-07-20 ORCH - Parallel workflow wf_e0e0aecd-771: WS1.D1(opus)/D4(sonnet)/D7(opus) + 3 opus verifiers + opus gate. 7 agents, 0 errors. No ported path modified (parity fuzz green); test_oracle.py untouched (oracle-vectoring these is WS1.E2, CA-gated — domain unit tests lock the spec-cited values now). Independent make verify green (705 py + rust incl. fuzz + eval 13/13 + lint + grep-gates).
[WS5.3] DONE 2026-07-20 ORCH(direct) - Per-role landing app/core/landing.py: ROLE_LANDING (Owner→today, Accountant→exception_inbox, CA→audit_room, §WS7.4) + vault-sensitivity clearance on the canonical Role model (ROLE_CLEARANCE, can_view_sensitivity, fail-closed on unknown; supersedes vault_calc's legacy role ranks — routes through this once vault is org-scoped WS4.2). 5 tests, ruff+mypy clean. Product-confirmable defaults. Done directly (subagents session-limited). make verify green.
[WS5.2] DONE 2026-07-20 SONNET(fanout) - Approval matrices app/core/approval_matrix.py (new; approvals.py F4 queue untouched): decide_approval(plan,role,action,amount,matrix_config) — Basics/Startup fixed defaults ignore config; Growth honours role×amount×action config; statutory-filing action ALWAYS hard-gates to Owner/Admin, uncircumventable by config (security-critical negative tested). Reuses rbac.can + entitlements.STATUTORY_GRACE. 18 tests. Approver ₹1L default product-confirmable. ORCH-gated (verifiers session-limited).
[WS6.2] DONE 2026-07-20 SONNET(fanout) - Quantity gates: WS6.1's quantity_gate already correct (ok→soft_warn→grace→block, ceiling always visible, upsell skips a plan that can't absorb the count); de-stubbed comments + 25 boundary-derived tests (headcount 10/50/200, seats, entities). Limits product-confirmable.
[WS6.3] DONE 2026-07-20 SONNET(fanout) - Upgrade-trigger events app/core/upgrade_triggers.py: 5 triggers (employee#11→gratuity/bonus, first SAFE→equity, AATO ₹5Cr→e-invoice, 2nd GSTIN→Growth, board meeting→secretarial); fires at/after threshold, not below; data-driven; no clock. 17 tests. 2nd-GSTIN target_plan=growth product-confirmable.
[WS4.4] DONE 2026-07-20 OPUS(fanout) - Per-tenant hash-chain: audit.py/audit_store.py made per-org (independent genesis+head, verify_chain(org) scoped, tamper in A undetectable-to/independent-of B); EXISTING canonical_json/compute_hash algorithm unchanged (30 audit tests incl. existing green + verdict tests green); edit-log helper + compute_daily_root(org) anchor record (external timestamping = ops/Human); docs/AUDIT_TRAIL_COMPLIANCE.md (MCA edit-log/non-disablable/tamper-evident mapping). ORCH-gated.
[P1-R2] DONE 2026-07-20 ORCH - Parallel workflow wf_7c06e6da-b7b: WS5.2/WS6.2/WS6.3 (sonnet) + WS4.4 (opus). Build phase 4/4 landed; opus verifiers + gate hit a SESSION LIMIT (reset 8:10pm IST) so ORCH ran the gate + spot-checks instead. Independent make verify green (675 py + rust + eval 13/13 + lint + grep-gates + RLS coverage).
[WS5.1] DONE 2026-07-20 OPUS(fanout) - RBAC role model app/core/rbac.py: Role/Capability enums + ROLE_CAPABILITIES data matrix + pure can(role,capability,context); Investor-link time-box/report-scope/watermark; role_change_event() audit-chainable PII-free emitter. Permission-matrix test (independent EXPECTED table + coverage guard, spec negatives: Accountant no approvals, CA read-only, investor scoped). Role→capability defaults product-confirmable. Not router-wired (WS4.3/5.2). Opus-verified PASS.
[WS6.1] DONE 2026-07-20 OPUS(fanout) - Entitlement architecture app/core/entitlements.py + plans.yaml: registry auto-derived from 12 domain manifests (116 keys) + platform baseline; cumulative Basics71/Startup+34/Growth+11 (validate_registry fails loud if data stops partitioning); is_entitled + guard()→GuardDecision + statutory-grace override (never blocks a filing; keys-only log; upsell-after) + quantity_gate stub; plan_from_context session-only (§0.8). 18 tests. Per-feature plan placement + grace set + quantity limits = product/CA-confirmable defaults. Not router-wired. Opus-verified PASS.
[WS4.7] DONE(db-layer) 2026-07-20 OPUS(fanout) - Tenancy red-team suite (DB/RLS layer): RLS extended to 6 money/PII tables (vendors/customers/bank_accounts/employees/tds_returns/documents+storage_prefix) → 14 tenant tables all RLS-covered; rls_redteam.sql = data-driven per-op×per-table NEGATIVE matrix (cross-org SELECT/INSERT/UPDATE/DELETE all blocked + fail-closed + storage-path isolation) over 12 tables, live vs Postgres. Route-level + real object-storage red-team honestly deferred to WS4.2/4.3. Opus-verified PASS.
[P1-R1] DONE 2026-07-20 ORCH - Parallel workflow wf_4487f969-1c1: WS5.1‖WS6.1‖WS4.7 (3 opus) + 3 opus verifiers + opus gate. 7 agents, 0 errors. Independent make verify green (604 py + rust + eval 13/13 + lint + grep-gates) + live RLS red-team PASSED.
[WS4.1] DONE 2026-07-20 OPUS - Multi-tenant target schema (P1, precedes all P1 code). SPEC-WS4.md (org→entity→gstin_registration→domain tables; BIGINT paise; uuid; storage prefix scheme). infra/db/multitenant/{001_tenancy,002_domain_rls}.sql: tenancy core + app_current_org() session GUC (org from JWT/session never request body, §0.8) + non-superuser maisha_app role + RLS (ENABLE+FORCE, USING+WITH CHECK) on all 8 tables incl. representative domain tables (bills/invoices/journal_entries/gst_returns). scripts/check_rls_coverage.sh CI gate (wired into make gates; proven to catch a leaky table). scripts/rls_redteam.sh + rls_redteam.sql: LIVE proof vs real Postgres — org-A session cannot read/write/update org-B rows + fail-closed when unbound; PASSED. make verify green. Supabase provisioning = Human (§0.7). NEXT: WS4.2 replay all 36 tables + SQLite importer.
[WS3.2-fuzz] DONE 2026-07-20 ORCH - Randomized Py↔Rust parity fuzz (WS3.2 completed). tests/integration/test_parity_fuzz.py: per ported target, generate CASES=300 seeded PAISE-GRANULAR random inputs (straddling rounding edges/thresholds — where round-number vectors are blind), compute the Python figure, send as a claim to the REAL Mahsa binary via verify_claims; assert every one verifies to the paisa (mismatch → block with both values in the note). 16 single-value targets + itc multi-value = ~5100 random parity checks/run, all green — no divergence. Bridge = the existing /fold recompute path (no PyO3/FFI). Non-vacuous guards (len==CASES, some non-zero recompute). WS3.2 = vector-parity + live-block + fuzz all done. py 530. make verify green.
[WS3-itc-multivalue] DONE 2026-07-20 ORCH - Ported itc_setoff as a MULTI-VALUE recompute target. Extended the claim/check model: a claim is single (claimed_paise) OR multi (claimed_values name→paise map, field-wise subset match); RecomputeCheck gains recomputed_values; has_mismatch + honest_pending handle both (Rust + Python). recompute_multi("itc_setoff") → per-head cash+remaining credit; wired into check_claim (live gate) + parity (PORTED[9]) + 2 oracle vectors + coverage. gst file_gstr3b attaches an itc_setoff claim (compute-time hook); verify_figure carries multi unchanged. Live-proven: field-wise verify + tamper-one-head blocks. Rust 112, py 513, 33 oracle vectors. make verify green.
[WS3-verified-flow] DONE 2026-07-20 ORCH - app/core/verify.py verify_figure(mahsa, claim) → FigureVerdict{verified,blocked,honest_pending}: the single path by which on-demand figures reach the §0.4 gate (batch → run_loop; on-demand → this). Mahsa-down raises (never silently unverified). Wired tax /advance-tax/234c endpoint to it (Golden Rule; returns verified/mahsa_blocked). 3 unit (fake-mahsa verdict mapping) + 2 integration vs real binary (correct 234c verifies, tamper blocks, 115baa hook verifies live). py 509. make verify green. Prime Directive now CLOSED-LOOP for both batch (payroll/payables/gst/tax-234e) and on-demand (234b/234c/115baa) figures.
[WS3-tax-hooks] DONE 2026-07-20 ORCH - Compute-time recompute-claim hooks for the on-demand tax figures (not persisted → no batch producer). advance_tax_interest attaches an interest_234c claim (total_234c); itr_computation company+115baa attaches a company_tax_115baa claim (normal_tax), firm/LLP attaches none; module builder interest_234b_claim for 234B callers. Claim inputs = exact compute args; targets parity-locked. 4 round-trip unit tests. py 504. make verify green.
[WS3-tax-ports] DONE 2026-07-20 ORCH - Ported interest_234b, interest_234c, company_tax_115baa to Rust recompute (slab_tax.rs, exact integer paise mirroring tax_calc; 234c rounds per-installment, 115baa 25.168% i128-safe). Wired all 3 into the live dispatch (mod.rs recompute()) + parity harness (PORTED[8] + dispatch) + 5 oracle vectors (ws1_tax_interest_115baa.yaml) + test_oracle.py TARGETS + mahsa_coverage.json. Parity green (Rust==Python==oracle: 234c ₹20,200, 234b ₹20,000, 115baa ₹25,16,800). Rust 111, py 500, 31 oracle vectors. make verify green. NOTE: these are on-demand tax figures (not persisted rows), so a BATCH producer (iterate+claim) isn't feasible like 234e/tds; they are now live-gate-available (a flow computing them can attach a claim) — compute-time claim hook is the follow-on.
[WS3-domain-producers] DONE 2026-07-20 ORCH - gst/payables/tax producers via parallel workflow wf_1a37c46f-e08 (opus tax-Rust-port → 3 parallel sonnet producers → 4 opus verifiers + opus gate; 9 agents). tax-rust-port: late_fee_234e ported to Rust recompute (slab_tax.rs + live dispatch + parity PORTED[5] + oracle vector + mahsa_coverage). Producers (recompute_claims overrides): payables→tds_on_payment per bill (category recovered by replay); gst→interest_3b per filed-late 3B; tax→late_fee_234e per filed-late TDS return. Round-trip unit tests. ORCH reconciliation of 2 verifier-caught defects: (1) payables emitted 194C-individual claims Rust couldn't reproduce because Rust tds_on_payment hardcoded payee_type='company' — ROOT-CAUSE fixed: added payee_type param (tds.rs+mod.rs+parity.rs) + 194C individual/company parity vectors, so 1% is now recomputable; (2) gst late_fee_3b claim used is_nil=(cash==0) proxy (is_nil not persisted) → dropped that claim, interest_3b only (late fee honest-pending). Also fixed a pytest basename collision (4×test_recompute_claims.py → unique names). Rust 108, py 495. make verify green.
[WS3-payroll-producer] DONE 2026-07-20 ORCH - Payroll domain now EMITS its claims end-to-end. BaseDomainService.recompute_claims(session) hook (default [] → honest-pending); PayrollService overrides it to emit wage_base + pf_employee/employer + esi_employee/employer claims per active employee (same inputs the figures were computed on — payslip_recompute_claims + compute_components now returns wage_base). run_loop calls service.recompute_claims and passes to mahsa.fold, so EVERY domain gets the gate uniformly (payroll live, others incremental). PT/TDS/LOP not yet ported → no claim (honest-pending). Proven against the REAL Mahsa binary (mahsa_server): payroll's real claims all verify (green) + a ₹1-tampered figure → Red MAHSA-PARITY block. Tests: 2 payroll unit (builder shape + unported-excluded) + 2 integration (verify-live + tamper-blocks-live); updated 4 fake-mahsa fold stubs for the new kwarg. py 482. make verify green. NEXT: repeat the producer pattern for gst/payables/tax as their targets are already ported (tds, itc, gst late-fee, gratuity).
[WS7-SPA-API] DONE 2026-07-21 ORCH - JSON API for the React SPA: app/web/api_router.py GET /api/today + /api/inbox, reusing the SAME pure assemblers the HTMX pages render (build_today / build_inbox) so SPA and server-rendered app cannot drift into two truths; exceptions_router._collect renamed collect_sources and shared (no duplicated Mahsa fold logic). Honest-degradation preserved: Mahsa down -> 200 + mahsa_up:false + honest-empty, never a fabricated verified figure. 2 integration tests pin the shape AND the degradation contract. Fits the existing /api/<domain> convention.
[WS7-BRAND-RESEARCH] DONE 2026-07-21 ORCH - docs/BRAND_THEME.md written. Recovered the pre-crash workflow wf_ac62dda1-ec7 (5 research angles + 4 of 5 verifiers complete; cross-session resume unavailable so the journal was harvested and ONLY the missing trust-precision-visual verifier re-run). That verifier DROPPED 2 findings (a fabricated Bloomberg-amber rationale from an LLM-authored repo; paraphrased Wise quotes presented as quotation) and corrected 4 (Stripe bg is #ffffff not #f6f9fc; "single accent" is Mercury/Ramp/Brex/Linear's pattern, NOT Wise's). Doc = 5 verified premium-finance patterns + Indian-market conventions (lakh-native proof, named statutes, up-front regulatory disclosure) + our system: warm-paper light canvas, brass accent chosen by elimination (money ramps + verification ramps + every competitor hue excluded), borders-not-shadows, tabular numerals + mono for statutory identifiers, hierarchy by size/tracking not weight. KEY FINDING: the existing frontend/ placeholder tokens (#0e1116 near-black + #2dd4bf teal) ARE the generic-AI-startup look the research warns against (Digits is the in-segment example). Verification-family separation independently validated by GitHub Primer. Canvas + accent flagged as open product decisions in §7.
[WS7-SPA-BRAND] DONE 2026-07-21 ORCH - React SPA rebuilt on the brand system (decision LOCKED with user: warm-paper light canvas + brass accent). frontend/src/theme/tokens.css replaced the generic-AI placeholder (#0e1116/#2dd4bf) with the BRAND_THEME palette; verification hues keep the SAME hue families as the HTMX tokens.css, retuned for light-canvas contrast. New: lib/money.ts (Intl en-IN lakh/crore — native, no formatting dep, no hand-rolled grouping to drift from format.py) + components/VerifiedNumber.tsx (VerifyChip + VerifiedNumber; state comes from the payload so a fabricated ✓ is impossible by construction; unknown state falls to ✕ never ✓). Today.tsx + Inbox.tsx are now REAL, reading /api/today + /api/inbox: honest-pending ◐ rendered as-is, null ₹-impact renders "not yet known — we don't guess" never ₹0, unwired inbox queues print their stub_note so an empty queue can't be mistaken for a genuine zero, Mahsa-down shows an explicit banner. Shell restyled (hairline borders, brass active state, no shadows/gradients per BRAND_THEME §3). npm run build + tsc clean, oxlint clean, make verify green. NOT wired: SPA bulk ops — the server contract is preview-then-confirm and shipping a bulk button without the two-step would be the silent-bulk-mutation anti-pattern; lands with the preview step or not at all.
[WS7-BLEND] DONE 2026-07-21 ORCH - docs/WS7_BUILD_CONTRACT.md: the JOIN of WS7_UX_RESEARCH (what must never happen to the user) and BRAND_THEME (what it looks/sounds like when we prevent it). One table: painpoint -> brand expression -> component rule -> status; a WS7 component is not done until every applicable row is satisfied or deferred with a reason. SELF-AUDIT found the first React pass was brand-correct but painpoint-BLIND, and 4 gaps were fixed: (T7) the WS7.2 working panel had REGRESSED to a title tooltip even though /api/today was already sending inputs/formula/citations/verdict_hash — rebuilt as a native <details> panel mirroring the HTMX version (no JS), incl. a "this number looks wrong" escape hatch on every figure; (T6) errors now use the 4-question template (what happened / is my money+filing safe / what next / traceable ref) per anti-pattern #6 "Something went wrong" = taunting — components/ErrorState.tsx; (T4) NEW effectiveState(state, stale): a ✓ on stale inputs downgrades to ◐ with a stated reason — staleness can only downgrade, never upgrade; (#14) failure no longer renders a blank shell — last-known payload renders below the error marked stale, since a blank ledger reads as data loss. Added vitest (first FE test runner) + 5 tests on the trust branch: stale-downgrade, never-upgrade-to-verified, lakh/crore grouping (₹12,34,567 not ₹1,234,567), and unknown-amount renders "—" never ₹0. tsc+vite build clean, oxlint clean (1 fast-refresh warning), vitest 5/5, make verify green. NOT wired: frontend tests into `make verify` (needs node in the CI env — decide before it counts as a gate); SPA bulk ops still pending the preview-then-confirm two-step (T3, the loudest signal in the research — next).
[WS7-E2E-BUILD] DONE 2026-07-21 ORCH - WS7 UI/UX end-to-end via 2 parallel workflows (26 agents, 0 errors). Build round wf_b4c4b314-e2f (16 agents): 4 JSON API tickets + 6 SPA screens, each screen adversarially verified. Result was 5 FAIL / 1 PASS - the verify layer is the only reason we know. CARDINALS caught: (a) WS7.7 /api/health/connections was NEVER REGISTERED (grep api_health = 0 hits) so the component fetched a dead route AND its copy asserted a downgrade behaviour that zero call sites performed; (b) WS7.6 sub-rupee drift rendered through inr()=round(paise/100) so claimed 12345678 vs recomputed 12345638 both printed the same rupee string and the mismatch read "differs by ₹0" - the exact paise-level drift the product exists to catch, made invisible; (c) WS7.5 confirm re-POSTed the LIVE selection not the previewed rows, with checkboxes still interactive behind the open preview; (d) WS7.8 CSV import fired with no preview (bulk insert + balance rewrite) and a VACUOUS test proven by mutation (deleting pickFirstFigure's body left it green); (e) WS7.4 runway_months null rendered as "not burning - unbounded" though the backend emits null BOTH for revenue>=burn AND for an empty ledger, so a new user with no data was told runway is unbounded. Fix round wf_ff7e18b8-b39 (10 agents, opus fix -> independent opus re-verify w/ mandatory mutation-check): 4/5 PASS; ORCH finished the 5th (onboarding asserted "net effect on balance" = credits-debits, but service.py OVERWRITES balance from the CSV's own balance column - now detects hasBalanceColumn and reports the statement's actual closing balance, or "not yet known - we don't guess"; regression test mutation-checked). ORCH root-cause fixes in shared files (agents correctly reported rather than cross-edited): ErrorState gained operation="write" + committed=n and no longer claims "nothing was changed" after a failed mutation (that bug was MINE, and it had propagated to 2 screens); lib/trace.ts useTraceId replacing ids that regenerated every render; --color-on-accent token; VerifiedNumber Freshness = boolean|"unknown" so "we couldn't check" is no longer stated as "your inputs are stale" (fails closed either way). All 4 routers wired: 9 endpoints live (/api/today /inbox /inbox/bulk /approvals /approvals/{domain}/decide /health/connections /domains /domains/{domain} /audit). App.tsx routes + Shell nav wired (nav appended never reshuffled per T2). GATES: make verify green (rust+py+eval 13/13+lint+grep-gates), tsc clean, vite build clean, oxlint clean (4 pre-existing fast-refresh warnings), vitest 116/116.
[WS7-E2E-OPEN] NOTE 2026-07-21 ORCH - Carried forward, NOT done: (1) api_bulk.py confirm loop calls record_decision(domain=...) with no row identity, so two previewed rows in one domain issue two identical domain-level decisions and committed_count counts rows not decisions - the SPA now sends exactly the previewed ids but the server does not honour them at row granularity; (2) no RBAC gate on bulk approve or on any /api SPA route (matches the existing HTML flow - needs one decision applied to both surfaces, not a unilateral divergence); (3) frontend tests are NOT in `make verify` (needs node in CI - decide before it counts as a gate); (4) WS7.9 mobile PWA + Lighthouse budget and WS7.2 per-figure Verdict threading through ask.py remain unbuilt.
[WS1.E2-provenance] DONE 2026-07-21 ORCH - Statutory sourcing rescoped + enforced. docs/STATUTORY_SOURCING.md: provenance taxonomy primary|derived|interpretation|unsourced; the EXPERT backlog is 6 questions, not 300 vectors. Root cause found: most vectors cited "MMX-1.0 §WS1.A1" — OUR OWN SPEC — and the CI gate only asserted a `source` key EXISTS, so self-citation passed. New test_vector_provenance: primary requires resolvable citation_url + citation_locator (bare link fails), citing our own spec as primary is rejected, interpretation requires alternatives_considered + un-self-signed ca_initials. Workflow wf_7b1c87b4-2b8 (14 agents, 7 cite + 7 adversarial re-fetch verifiers): 6/7 files PASS. Now 65 pass / 2 deliberate reds. All citations bound to real instruments (incometaxindia.gov.in Act compilations w/ amendment footnotes, thc.nic.in Gazette PDF of Finance Act 2025, esic.gov.in Rules, indiacode.nic.in Companies Act) — zero commentary sites.
[WS1-DEFECTS-FOUND] BLOCKED-HUMAN 2026-07-21 ORCH - The citation pass found REAL DEFECTS in shipped code. Agents REPORTED, did not fix (§0.7: discovered conflict between spec and current law escalates to Human). (1) OFF-BY-ONE AT EVERY TDS THRESHOLD — payables_calc.py:83-86 uses `amount >= single`, but 194C/194J/194I/194Q/194T are all worded "no deduction where the amount does not exceed X", so deduction triggers at amount > X. At EXACTLY the threshold we deduct TDS the law does not require. Verifier quoted the proviso verbatim from the Dept's own text. (2) Vector tds_194j_50k_boundary ENCODES the wrong boundary (expects applicable at exactly ₹50,000) — left unsourced ON PURPOSE so CI stays red rather than sourcing a value the statute contradicts. (3) GRATUITY ELIGIBILITY — statutory.py applies a >=1yr floor to EVERY employee, but CoSS 2020 s.53(1) requires 5 years continuous service; the 1yr rule is the FIXED-TERM-employment exception. (4) NO GRATUITY CEILING — CoSS s.53(3) requires a notified cap; gratuity_hybrid applies none (unbounded). (5) s.2(y) ADD-BACK DENOMINATOR — first proviso aggregates clauses (a)-(i) only; gratuity (j) and retrenchment (k) are outside that span but the engine treats every non-INCLUDED key as excluded. (6) OPEN-WORLD EXCLUSION — s.2(y)'s exclusion list is CLOSED; engine assumes anything not included IS excluded, erring toward UNDER-computing PF/ESI/gratuity (under-paying statutory dues). (7) special_allowance classification is uncited and materially affects 2 expected values. (8) ITC set-off hardcodes IGST->CGST before IGST->SGST though Rule 88A + Circular 98/17/2019 grant "any order and in any proportion" — silent discretionary choice. (9) 234B rounding cited to s.288A in a docstring; s.288A rounds TOTAL INCOME — correct authority is Rule 119A(c).
[WS1-SOURCE-TRAPS] NOTE 2026-07-21 ORCH - Source-integrity findings worth keeping: (a) ESIC publishes TWO inconsistent editions of ESI (Central) Rules r.51 — the "As on 26.04.2024" PDF still prints PRE-2019 rates (4.75%) in the rule body despite its own footnote; the "As on 01.04.2021" edition carries the correct 3.25%/0.75%. Cite the latter. (b) Commentary sites (ClearTax/Kanakkupillai/CAclubindia) state Form 130 issues under s.395 of the IT Act 2025; the agent read the enacted Gazette and could not confirm it — Form 130 left UNSOURCED rather than cite the restatement. (c) MoLE "Additional FAQs on Labour Codes (as on 16.03.2026)" Sl.17 asks our gratuity-transition question DIRECTLY — that may convert interpretation-Q1 into a citable primary answer; needs a fetch+read before we keep treating it as interpretation. (d) A fabricated Finance-Act-2012 footnote attribution in the 234E vector was caught by re-fetch and REMOVED, not reworded.
[WS1.C1-boundary] DONE 2026-07-21 ORCH (owner-approved) - TDS THRESHOLD OFF-BY-ONE FIXED, both engines. payables_calc.tds_on_payment and dif/src/recompute/tds.rs both used `amount >= single` (and `>= aggregate`); every governing proviso exempts the payment where the amount "does not exceed" the threshold, so AT the threshold no duty arises and it begins one paisa above. Cited: s.194J(1) first proviso cl.(B)(i) w/ footnote 43 (Act No. 7 of 2025 subst. "thirty"->"fifty"), s.194I proviso, s.194C(5) — all read verbatim off the Dept's own text and adversarially re-fetched. Changed to strict `>` in BOTH engines in the same change (a one-sided fix would have broken paisa-parity and blocked every 194J claim at the fold gate). Affects 194C/194J/194H/194I single + aggregate. Direction of the error: we were deducting tax the statute does not require, at exactly the threshold.
[WS1.C1-boundary-tests] DONE 2026-07-21 ORCH - The suite was LOCKING THE DEFECT IN. 5 tests asserted the wrong behaviour and all were green: oracle vector tds_194j_50k_boundary (expected applicable:true at exactly ₹50,000 — the agent had refused to source it and deliberately left CI red rather than cite a value the statute contradicts), Python test_194j_professional_above_threshold + test_create_bill_deducts_tds, Rust tds_194j_50k_boundary + t_194j_professional_above_threshold, plus integration fold_recompute_mismatch_blocks. Replaced with PAIRED boundary vectors/tests (at-threshold => no TDS, one paisa above => TDS on the whole sum) in BOTH languages — a single-sided boundary test passes under a constant answer and proves nothing. Non-boundary tests moved off ₹50,000 to ₹60,000 so they test rate/wiring, not the edge. Vector tds_194j_50k_boundary_no_tds_at_threshold is now provenance=primary with citation_url + locator. Statutory oracle: 2 reds -> 1 red (only Form 130 sourcing remains). py 914 pass / 1 red, rust 90+18+4 all green.
[LESSON] NOTE 2026-07-21 ORCH - Both engines agreed to the paisa on the wrong answer for the entire life of the project, and Python<->Rust parity + the fuzz harness could never have revealed it: parity proves CONSISTENCY, the oracle proves CORRECTNESS. This is the concrete justification for §0.6 and for keeping an external truth anchor even though the product is for CFOs rather than CAs.
[SESSION-RECON] NOTE 2026-07-22 ORCH - The 2026-07-21 late session crashed before logging. Entries below reconstructed from the artifacts themselves (docstrings, migrations, tests), then the WHOLE tree proven by one `make ci` run before commit — nothing below is claimed beyond what that gate proves.
[WS4.3] DONE 2026-07-21 (logged 07-22) - AUTH-CONSOLIDATE: ONE authentication path. Better Auth (TypeScript layer, owner-configured) issues JWTs; app/core/betterauth.py only VERIFIES via real JWKS (EdDSA/Ed25519 pinned allow-list, never "none", never token-chosen; exp/iss/aud all required) and resolves a Principal. §0.8 RLS binding: engine checkout event re-binds app_current_org GUC from the VERIFIED claim on EVERY pooled-connection checkout (request B can never inherit request A's org; unauth -> NULL -> matches no rows). infra/db/multitenant/003_identity.sql: password_credentials/mfa_totp (per-user, no org dim — matches app_users precedent) + tenant-scoped sessions. tests/integration/test_auth_e2e.py drives the REAL app over REAL HTTP with a REAL Ed25519 keypair served from a live localhost JWKS endpoint; includes the forged-principal mutation test (inserting `return Principal(attacker)` at the top of get_principal fails the suite — the identity must come from THIS request's token).
[WS5.1-wiring+WS6.1-wiring] DONE 2026-07-21 (logged 07-22) - rbac_deps.py: pure rbac.can() unchanged; dependency layer turns denial into a 403 naming the missing capability (not the resource), denial audit-chained; role from betterauth Principal only. entitlement_deps.py: require_feature/require_quantity dependency factories over entitlements.guard(); plan resolution per §0.8 (no third source). test_rbac_matrix.py + test_entitlement_routes.py drive real routes.
[WS4.2] DONE(replay+importer) 2026-07-21 (logged 07-22) - Alembic 0002 multitenant-core replay (tenant_core schema; tenancy+domain+identity tables EACH with its RLS policy in the same revision, SQL inlined for migration immutability) + 0003 vault retention 7y->8y recompute for existing rows (the WS1.C5-migration debt, now paid) + test_import_roundtrip.py (SQLite tenant import w/ checksum reconciliation).
[WS10.4] DONE(drafts) 2026-07-21 (logged 07-22) - Legal kit: docs/legal/{TOS,PRIVACY,DPA}_DRAFT.md + DISCLAIMERS.md (drafts FOR COUNSEL REVIEW, no compliance asserted — §0.6 applied to legal claims); app/core/legal.py versioned acceptance mechanics (re-accept on version bump); alembic 0004 legal_acceptance table WITH RLS in-revision; served + acceptance-logged; test_legal.py.
[WS7.4+WS7.7-api] DONE 2026-07-21 (logged 07-22) - api_domains.py (five hubs + CA Audit Room JSON, reusing collect_health/collect_kpis/badge_state — §0.4 single honesty gate) + api_health.py (connection-health/staleness; route owns the clock, app/core/freshness owns the logic) + test_freshness.py. Fixes the WS7-E2E finding (a): /api/health/connections is now REGISTERED in main.py.
[WS4.8] DONE 2026-07-21 (logged 07-22) - THE gate: scripts/ci_gate.sh is the single list of steps; `make ci` and .github/workflows/verify.yml both run it VERBATIM (local/CI drift is impossible by construction). cargo build FIRST so mahsa-needing integration tests can never silently skip; adds frontend gate (npm ci + tsc + vitest + oxlint) — closing WS7-E2E-OPEN item (3). KNOWN_STATUTORY_GAPS mechanism: oracle failures must equal the declared gap set EXACTLY — an undeclared failure is a regression (red), a stale entry is red too, so a permanently-red-step culture cannot form. Plus check_money_format.sh grep-gate wired into make gates.
[WS1.E2-form130] DONE 2026-07-22 ORCH - THE LAST ORACLE RED CLOSED. regime_form_map_salary_cert_2025 unsourced->primary: the Department's own forms document ("Form No. 130_131_132_133 (Earlier Form No. 16/16A/16B/16C/16D/16E/27D)") retrieved via Internet Archive snapshot (20260412144536) of the Akamai-blocked incometaxindia.gov.in URL and read verbatim (pages 1-2): Form 16 -> Form No. 130 under s.395(4) of the Income-tax Act 2025, rule 215 of I.T. Rules 2026, issued against Form 138, due 15 June following the Tax Year. Also confirms 16A->131, 16B/C/D/E->132, 27D->133 for the wider map. The commentary sites' "s.395" claim the 07-21 agent refused to cite is now departmentally confirmed as s.395(4). KNOWN_STATUTORY_GAPS in ci_gate.sh emptied per its own stale-entry rule. Oracle 69/69, zero gaps. `make ci` ALL GREEN (19 steps: cargo build/test/clippy, ruff, mypy, 4 grep-gates, unit, integration, oracle-gaps-vs-regressions, E2E real loop, live RLS red-team, golden eval, npm ci, tsc, vitest, oxlint).
[fix:itc-order] DONE 2026-07-22 OPUS - Defect #8: itc_setoff hardcoded IGST->CGST-before-SGST; Rule 88A r/w Circular No. 98/17/2019-GST (para 3 + para 4 table rows II/III, fetched live from cbic-gst.gov.in, quoted verbatim) permits "in any order and in any proportion" with IGST credit "completely exhausted mandatorily". Replaced with the CASH-MINIMIZING allocation in BOTH engines identically (api/app/domains/gst/gst_calc.py::itc_setoff + dif/src/recompute/itc.rs): uncovered needs (liability minus own credit) first, CGST-before-SGST tie-break (total cash invariant to it), then mandatory exhaustion CGST-first; deterministic, clock-free, integer paise. Order-choice recorded per owner decision 2026-07-22 as provenance=interpretation, ca_initials OWNER, alternatives_considered (fixed CGST-first = old engine/GSTN default, proportional split, SGST-first) in ws1d_itc_setoff.yaml + 2 new vectors where old and new figures DIFFER (3000->1000 and 10000->5000 paise cash); 2 pre-existing primary vectors unchanged (order-invariant, re-verified). PAIRED boundary tests both engines (needs exactly covered -> 0 cash; one paisa short -> 1 paisa cash) + mandatory-exhaustion carry-forward test. Green: cargo test 115 (incl parity.rs oracle leg), pytest gst+oracle 138, parity fuzz + verified flow 20 (against rebuilt mahsa binary), ruff+mypy clean.
[WS1.C-labour-codes] DONE 2026-07-22 AGENT - Defects #3/#4/#5/#6/#7 from [WS1-DEFECTS-FOUND] fixed in BOTH engines in the same change (paisa-parity kept; parity.rs replays every vector). (#3) gratuity eligibility: CoSS 2020 s.53(1) FIVE-year floor ("continuous service for not less than five years", read verbatim off aA2020-36.pdf) now the default; the 1-year floor confined to fixed_term=True (s.53(1) second proviso + MoLE FAQ 16.03.2026 Sl.14/19) — gratuity_hybrid gained a fixed_term param (Py+Rust+dispatcher+parity). The old oracle vector paying a 2-year non-FTE ₹30,000 LOCKED THE DEFECT IN; replaced with PAIRED at-threshold/one-short vectors on BOTH floors (5y payable / 4y364d nil; FTE 1y payable / 11mo nil; 2y non-FTE nil vs 2y FTE ₹30k). (#4) s.53(3) ceiling implemented at ₹20,00,000 in gratuity_hybrid AND gratuity_required: cap existence primary (s.53(3) verbatim); AMOUNT is S.O. 1420(E) 29-03-2018 (PoG s.4(3), PIB-confirmed via wayback 20240217082025) carried over by s.164(2)(a) ("deemed to have been done ... under the corresponding provisions of this Code") — no CoSS-specific s.53(3) notification could be sourced, so the carry-over ships provenance=interpretation ca_initials OWNER with alternatives (incl. no-cap-until-notified, rejected). Paired under/over ceiling vectors (₹19,61,538 passes through / raw ₹20,76,923 clamps to exactly 200,000,000 paise). (#5) s.2(y) add-back numerator now aggregates clauses (a)-(i) ONLY per the verbatim proviso; gratuity (j) and retrenchment/ex-gratia (k) excluded from wages but OUTSIDE the span — statutory_wage_base grew a third bucket (excluded_terminal) through Py, Rust fn, claim inputs, dispatcher, parity, fuzz. Denominator read as ALL money components incl. (j)/(k) — interpretation, alternatives recorded. (#6) exclusion list is CLOSED ("but does not include—(a)...(k)", full list quoted verbatim in ws1b_wage_base.yaml): unknown components now INCLUDE in wages (err toward paying dues). MoLE FAQ Sl.13 reads the opposite way and is RECORDED as a rejected alternative (FAQ is not an instrument; opening limb says "all remuneration"). (#7) special_allowance reclassified INCLUDED — RPFC (II) WB v. Vivekananda Vidyamandir (SC 28-02-2019, indiankanoon 126246109) universality test + no matching (a)-(k) clause; provenance=interpretation ca_initials OWNER; wiring vector proves PF ₹1,680 / ESI ₹105/₹455 on the ₹14k base (old classification gave ₹10k). All owner-decided interpretations per the 2026-07-22 no-external-CA decision. KNOWN_STATUTORY_GAPS untouched (empty — everything sourced). NOT FIXED (recorded): s.53(2) "or part thereof in excess of six months" final-part-year round-up is not implemented (engine counts whole anniversary years); vectors use whole-year inputs so no expected value depends on it — needs its own ticket. Tests: api unit 793 pass/1 skip, payroll+oracle 141 pass, parity fuzz + full loop 32 pass vs real mahsa; dif cargo test 97+18+1+4 green, clippy -D warnings clean; ruff+mypy clean. 12 pre-existing integration failures (401s from concurrent auth work in rbac_deps/main.py, upstream of payroll math) are NOT from this change — payroll full-loop and parity fuzz pass.
[fix:rbac-api] DONE 2026-07-22 OPUS - WS7-E2E-OPEN(2): RBAC on EVERY /api route + HTMX mutate/approve routes, one decision both surfaces. All 68 /api routes now declare a capability via rbac_deps (router-level `read` baseline; mutations add `write`; expense claim-approve `approve_payment`; audit room `view_audit`). NEW rbac_deps.require_filing(action): WS5.2 statutory hard gate WIRED to approval_matrix.decide_approval (Owner/Admin only, stricter than the approve_filing capability — Approver refused; action validated against STATUTORY_FILING_ACTIONS at import) on gst/gstr3b, tax/tds-returns, compliance/deadlines/{id}/file. HTMX: /approvals/{domain}/decide gated approve_payment + attributed to VERIFIED principal (was settings.default_user_id; now uses get_mahsa seam), /inbox/bulk mirrors JSON preview(read)/commit(approve_payment) split + principal attribution, action drawer + vault OCR + history/capture + parallel start/observe gated `write`. require() closures carry required_capability marker -> test_rbac_matrix coverage guard: deployed /api route set must EQUAL hand-written API_ROUTE_GATES table AND each route's declared markers must match (new unguarded route = red CI); full matrix test = 68 routes x 6 roles over real HTTP with real Ed25519 JWKS tokens (plan=growth so 402 never masks RBAC), denials assert the FIRST failing gate's exact detail. Spec negative held: Accountant cannot approve anywhere (JSON, HTMX single, HTMX bulk). Unit: require_filing paired grid + marker tests. Fixtures: semantics tests (test_app, test_api_domains) use the blessed get_principal Owner override, matrix stays on real signed tokens. Green: py 801u+202i+95 oracle, mypy 180 files, ruff, Rust 120 (untouched — no money math changed). Skipped: email-send routes (/investor/send, /cfo/brief/send) left ungated — no book mutation; gate with `export` if leak surface matters.
[fix:bulk-rows] DONE 2026-07-22 OPUS - WS7-E2E-OPEN(1): bulk decisions now carry ROW identity end-to-end. record_decision(+item_id) puts "[row <id>]" inside the hashed audit `query` (core_payload-safe — a NEW payload key would break verify_chain recomputation, so identity rides in an already-hashed field; history untouched) + Decision.item_id column (nullable; alembic 0005_decision_item_id, pre-fix rows/whole-domain approvals stay NULL, pre-fix audit shape byte-identical when item_id absent). Both confirm loops (api_bulk.py JSON + exceptions_router.py HTMX) pass item_id=row.id: two rows in one domain = two DISTINGUISHABLE sealed decisions; an id outside the preview is reported skipped and never decided; committed_count = rows actually decided. Response contract unchanged -> zero frontend change. Tests: unit x2 (two-rows-one-domain distinguishable in audit+Decision, chain still verifies; no-item_id keeps exact pre-fix query) + integration x2 (real app/Mahsa: 2 rows/1 domain -> 2 distinct audit entries + item_ids; stray id on confirm rejected, no entry names it). Mutation-checked: reverting item_id=row.id makes both new integration tests FAIL (verified live, then restored). 814 py unit+bulk green, ruff+mypy clean, dif cargo test green (no money math touched — no Rust mirror needed).
[fix:verdict-ask] DONE 2026-07-22 SONNET - WS7-E2E-OPEN(4b): per-figure app.core.verify.FigureVerdict threaded through Ask Maisha. Root cause: ask.py's Figure carried only an ad-hoc "check"/"pending"/"warn" string derived straight from mahsa_coverage, a SECOND vocabulary alongside the FigureVerdict type the on-demand recompute path (verify.py) already returns — no drift bug yet, but the richer verified/blocked/honest_pending object was never attached, so the HTMX working-panel always rendered its honest "nothing sealed" stub even under a ✓ badge (the WS7.2 FLAG). Fix (no new verification logic, no new Mahsa call): new `_verdict(target, fact_backed)` in app/core/ask.py is now the SINGLE source of truth (not fact-backed -> blocked; fact-backed + mahsa_coverage.is_recomputed -> verified; fact-backed + unported -> honest_pending, never upgraded); `_badge` is now a thin projection of it, so the two representations cannot diverge. Figure gained a `verdict: FigureVerdict` field threaded by `_figures`; answer_card.html's vstate now reads `f.verdict.verified`/`.honest_pending` directly instead of re-parsing the badge string (unknown falls to "unbacked", never "verified"). No SPA Ask surface exists yet (checked App.tsx/routes — none), so nothing to wire there. Tests: test_figures_thread_real_verdict_for_ported_and_unported (ported+unported+invented-number in one response show verified/honest_pending/blocked correctly, off the real mahsa_coverage.json) + test_verdict_lookup_is_real_not_hardcoded (monkeypatches is_recomputed to always True, proves the unported target's verdict was genuinely driven by the live lookup, not hardcoded) + existing test_verified_number.py fixture updated to pass real FigureVerdict objects. 805 py unit green (50/50 on `-k "ask or llm"`), mypy+ruff clean on touched files. No money math changed -> no Rust mirror / oracle vector update needed.
[WS1-DEFECTS-FIXED] DONE 2026-07-22 ORCH - Workflow wf_f64e8354-07d (12 agents, 0 errors): defects #3-#9 + WS7-E2E-OPEN (1)(2)(4b) fixed, each adversarially verified w/ mandatory mutation-check. Owner decision recorded: no external CA — research-verified primary citations are the truth source; genuine interpretation choices implemented + documented provenance=interpretation ca_initials OWNER. fix:labour-codes (#3 gratuity 5y floor per CoSS s.53(1), fixed_term=True carries the 1y exception; #4 ₹20L ceiling s.53(3) via s.164(2)(a) carry-over of S.O. 1420(E); #5 add-back span clauses (a)-(i) only, (j)/(k) in new excluded_terminal bucket; #6 CLOSED exclusion list — unknown components INCLUDE in wages, err toward paying dues; #7 special_allowance INCLUDED per Vivekananda Vidyamandir universality test) — BOTH engines same change, defect-locking vectors replaced with PAIRED statute-cited vectors, verifier re-fetched both instruments (indiacode aA2019-29/aA2020-36 PDFs) and killed 5/5 mutations. fix:itc-order (#8: Rule 88A + Circular 98/17/2019 — cash-minimizing IGST allocation across CGST/SGST, deterministic, both engines identical, parity fuzz green, order-choice documented as interpretation w/ alternatives). fix:rbac-api (WS7-E2E-OPEN (2): all 68 /api routes declare capabilities via rbac_deps; statutory filing hard-gated Owner/Admin; Accountant-cannot-approve negative; route×role coverage guard fails on any new unguarded route). fix:bulk-rows (WS7-E2E-OPEN (1): record_decision carries row identity end-to-end; decision.item_id queryable mirror, audit chain extended never mutated; confirm processes EXACTLY the previewed ids, mismatch rejected; committed_count = decisions). fix:verdict-ask (WS7.2 remainder: per-figure Verdict threading through ask.py from the existing verify path; unported stays honest-pending; fabricated-verified impossible by construction). Verify round: 5 PASS, 1 FAIL (citations-234b residuals) — ORCH reconciled: 234B docstring now says the SHORTFALL (s.234B(1) interest base) is what Rule 119A(c) rounds; round-down was TEST-LOCKED in NEITHER engine (all prior inputs already ₹100 multiples — vacuous) → rounding-exercising paired tests in both engines + oracle vector interest_234b_rule_119a_rounddown_exercised (₹1,00,050 → base ₹1,00,000; without rounding the figure differs, so parity replay kills the mutation cross-engine). Also fixed 0005_decision_item_id vs create_all-baseline duplicate-column (existence guard; fresh + pre-existing DBs both migrate). make ci ALL GREEN (19 steps). WS1-DEFECTS-FOUND fully closed.
[WS8.2] DONE 2026-07-22 AGENT - CA query threads pinned to entries + deterministic sampling. app/core/ca_threads.py: raise->respond-with-doc->resolve state machine; EVERY event sealed onto the EXISTING hash-chained audit_log via audit_store.append using a new AuditEntry-core payload builder (thread_event_payload, mirrors edit_log_payload: descriptor inside the hashed query field carries thread/entry_ref/doc + note SHA256, raw note text NEVER enters the chain — it lives only in the ca_thread_event mirror row, which records its sealed this_hash). History extended, never mutated: audit.py/audit_store.py untouched. respond-with-doc validates the vault documents.id EXISTS (refused, not recorded, otherwise); resolve only from responded (an unanswered query cannot be closed). Sampling helper sample_selection(org, domain, date range, n): NO RNG — seed=sha256(org|canonical_json(spec)), candidates (journal entries in range, source==domain) ranked by sha256(seed:id), n smallest, each voucher carrying its linked vault doc bundle (documents pinned by voucher id or human reference); same org+spec = byte-identical sample forever. Surfaces BOTH: /api JSON in api_domains.py (GET+POST /api/audit/threads[/{id}][/respond|/resolve], GET /api/audit/sample) + HTMX audit room (threads section w/ forms in audit.html, POST /audit/threads[...] in main.py) with the SAME capability decision: raise/read/resolve = view_audit (CA holds it, Investor does not), respond-with-doc = write (Accountant/Owner/Admin; CA excluded by construction — no new capability needed, none invented). Alembic 0006_ca_threads per the 0004 pattern: tenant_core SQL inlined verbatim from new infra/db/multitenant/005_ca_threads.sql, BOTH tables ship ENABLE RLS + policy + minimal grants (event table append-only; thread UPDATE(state) only) in the same revision — check_rls_coverage.sh green on both paths; SQLite branch existence-guarded per the 0005 lesson. Tests: unit x5 (full lifecycle chained into verify_chain incl. tamper->broken + refused transitions seal NOTHING + PII-absence + sampling determinism against an INDEPENDENT hash-rank recomputation) — 2 live mutation-checks (rank->first-n-by-id, seal-event->skip-audit) both KILL tests, verified then restored; integration in test_rbac_matrix.py: +12 API_ROUTE_GATES rows (6 thread/sample + 3 concurrent WS8.1 pack routes so the coverage guard stays whole) + export gate row, full-matrix 6 roles x 78 routes green, HTTP lifecycle (CA raises, CA DENIED respond 403 write, Accountant responds w/ real doc, CA resolves, /api/audit chain_intact + all 3 events sealed + JWT-subject attribution), sampling-route determinism, HTMX mirror (raise 303, CA respond denied, thread renders). Green: py unit 817+1skip, rbac matrix 37, api_domains+app 52, migrations 17, ruff clean, mypy clean on ALL WS8.2 files. KNOWN ISSUE not mine: concurrent WS8.1 pack code in api_domains.py (_audit_pack_entity_data, lines ~396-434) has 14 pre-existing mypy union-attr errors — left to its owning agent.
[WS8.1] DONE 2026-07-22 AGENT - Audit Pack COMPLETE (remainder of core slice). app/core/audit_pack.py: +3 sections all pulled from EXISTING domain services, zero recomputation — statutory_registers (stored TdsReturn rows: total_deducted honest-pending, late_filing_fee under its GENUINE ported target late_fee_234e → verified; stored GstReturn rows: tax_payable/late_fee/interest all honest-pending per GstService.recompute_claims' own reasoning; payroll build_snapshot burn/LWF/bonus figures), form_26as_reconciliation (consumes tax_calc.reconcile_26as OUTPUT per-TAN; no 26AS statement store exists yet so the endpoint passes None → honest-empty note "No Form 26AS statement loaded", never a vacuous reconciled-from-two-empty-lists; builder proven against real reconcile output in tests), msme_ageing (payables ap_aging buckets + total; msme_max_days_unpaid as a hash-covered section NOTE since it is days not paise, names s.15 + EXCEEDED when >45). Integrity hash now also covers section_notes; new pure verify_pack_integrity(pack) recomputes the seal (figure/badge/note tamper → False). Artifacts: pack_to_csv_zip (stdlib csv+zipfile — openpyxl NOT a dep, checked api/pyproject.toml, no new dep added; cover sheet embeds hash, every row carries badge TEXT via fail-closed badge_text map VERIFIED/PENDING/BLOCKED, unknown → PENDING never VERIFIED) + app/core/pdf.py audit_pack_pdf (REUSES existing ReportLab _GRID/_doc/_inr payslip machinery; hash on cover, badge column, §0.4 legend). Endpoints in api_domains.py: GET /api/audit/pack (view_audit) + /api/audit/pack.zip + /api/audit/pack.pdf (export — Approver can view but not download, no capability invented), org bound to session principal (§0.8), rules_version from LIVE mahsa /health, Mahsa down → stated 503 never a fabricated version; assembler uses concrete Ledger/Payables/Payroll service types (fixes the 14 union-attr mypy errors WS8.2 flagged). HTMX audit room: download links in audit.html. PENDING_SECTIONS now only fixed_asset_register. Tests: unit test_audit_pack.py 18 (section completeness incl. honest-empty 26AS, 234E-verified-vs-deducted-pending, GST-never-verified, per-TAN recon reuse, MSME note, zip cover hash + badge-text honesty in the artifact, verify_pack_integrity kills figure/badge/note tampering, PDF stream-decoded assert hash+VERIFIED+PENDING present) + integration test_audit_pack_api.py 5 vs REAL Mahsa (CA full pack w/ honest badges end-to-end from a genuinely-filed late TDS return, artifact downloads embed pack hash, Approver 403 on export, Investor 403, Mahsa-down 503). Green: full py suite 1125 passed + 2 pre-existing skips, ruff + mypy clean on all touched files, frontend untouched and gate re-verified (tsc -b, vitest 150, oxlint).
[WS7.9-core] DONE 2026-07-22 AGENT - React SPA is a PWA (WS7-E2E-OPEN item (4), core slice). (a) frontend/public/manifest.webmanifest: name Maisha-Mahsa, colors from theme/tokens.css (background #faf8f4 ground, theme #8a6a1f brass accent — no invented palette), icon set generated FROM the existing favicon.svg mark (not redrawn) via the already-installed system gi.Rsvg+pycairo (no new npm/image-tooling dep): frontend/public/icons/{icon,maskable}-{192,512}.png + apple-touch-icon.png, maskable variants full-bleed on the warm-paper ground with the mark sized inside the OS safe-zone. (b) frontend/public/sw.js: hand-rolled plain JS in public/ (zero build config — Vite copies it verbatim, sidesteps the webworker-vs-dom tsconfig lib conflict a hand-rolled sw.ts would hit under tsc -b). Cache-first for /assets/* (Vite content-hashes these, so identical URL = identical bytes). /api/* is network-first with NO cache fallback — deliberate, and the §0.4-critical decision of this ticket: serving a cached 200 back would require inventing a new "this response is stale" signal and threading it through every screen; instead an offline GET just fails like the server being down, which the app ALREADY treats correctly (ErrorState.classify -> api_down; screens mark last-known data `stale` -> effectiveState downgrades ✓ to ◐, never renders a cached ✓ as current). Non-GET (writes) never intercepted. App-shell navigation gets a network-first + cache-fallback so an installed/offline reload isn't a bare browser error (no figures in it, not a §0.4 concern). Registered from src/main.tsx, PROD-only (avoids fighting the vite-dev unhashed module graph). (c) index.html: manifest link, theme-color meta, apple-touch-icon; no new vite plugin. (d) responsive pass: root cause of 360px overflow was Shell.tsx's fixed `220px 1fr` grid (an inline style, which a CSS media query cannot override — moved shell layout into tokens.css classes `.shell*` so a `max-width:640px` query can stack it); also added flexWrap to the few remaining nowrap flex rows in Today.tsx/Inbox.tsx/Approvals.tsx that could overflow with long labels. (e) budget.json (script<300KB, LCP<2.5s — Lighthouse's own compressed-transfer accounting; current build gzips to 117.6KB JS) + `npm run lighthouse` (scripts/lighthouse.mjs): gated on `which lighthouse`, prints run-it-yourself instructions and exits 0 if absent (NOT added as a package.json dependency, per the ticket), otherwise spins up `vite preview` and runs it against budget.json. WhatsApp templates: frontend/src/lib/whatsappTemplates.ts — pure string-builder functions only (dailyBriefMessage/approvalAlertMessage/troubleAlertMessage), every message deep-links into the SPA (/today, /approvals) rather than acting inside WhatsApp, since approval is the typed-confirm flow (WS7.6) and nothing here may bypass it; actual send infra (WhatsApp Business API/Twilio/etc.) is explicitly Human's, per the ticket. Tests: ErrorState.test.ts (NEW — classify() had no test file at all) pins the exact §0.4-critical chain this ticket is about: a raw offline TypeError classifies identically to a down server (api_down), and effectiveState("verified", true) downgrades to honest_pending either way, with a companion never-upgrades-on-reconnect assertion; whatsappTemplates.test.ts (3 tests) pins the deep-link + no-invented-₹0 contract. `classify` exported from ErrorState.tsx (was module-private) to make it directly testable. Verified: tsc -b clean, vitest 158/158 (150 prior + 8 new, exact match — nothing else regressed), oxlint src clean (exit 0; same pre-existing fast-refresh warning pattern, +1 for the newly-exported `classify`, consistent with every other exported-helper file in this codebase), `npm run build` clean (JS 384.78KB raw / 117.63KB gzip, under the 300KB budget), dist/ contains sw.js + manifest.webmanifest + icons/ at the build root, `npm run lighthouse` verified to no-op correctly (lighthouse not installed in this env) and print the documented manual command. NOT done here (deferred, named so they don't silently become "done"): actually running Lighthouse CI in a pipeline (needs `lighthouse` installed somewhere — documented, not faked); WhatsApp send infra (Human, per ticket); §WS7.V validation gates (5-second test, badge comprehension, etc. — human/CA testing, unrelated to this ticket's scope); no offline READ of last-known API data across a full page reload (ponytail-flagged upgrade path in sw.js's header comment — requires tagging cache-fallback responses and teaching lib/api.ts to surface them as Freshness="unknown" instead of throwing; skipped because the safety property (never a stale ✓) is fully met without it and the added complexity buys an offline-reload nicety, not a correctness requirement).
[WS8.3] DONE 2026-07-22 AGENT - CA seat onboarding: free + unlimited seat + invite flow + referral instrumentation. EXPLICIT entitlement exemption in app/core/entitlements.py (SEAT_EXEMPT_ROLES={"ca"} + countable_seats/seat_gate/seat_addition_gate): CA-role memberships NEVER count against the "seats" quantity gate on any plan; mutation-checked LIVE (removing "ca" from the exemption fails 4 tests, incl. the flow test where an org past limit+grace still invites its CA — verified then restored). Invite flow REUSES the existing identity layer (app_users+memberships from 001_tenancy.sql + betterauth Principal — NO parallel auth): app/core/ca_seat.py invite_ca (Owner/Admin via rbac MANAGE_USERS; find-or-create app_user by normalized email; membership role=ca status=pending; dupe/bad-email refused sealing NOTHING) + accept_ca (matched ONLY on the caller's VERIFIED token email within the token's org §0.8; pending->active). Referral instrumentation = append-only events sealed onto the org's per-tenant hash chain via audit_store.append_for, PII-minimal per trace_store (descriptor carries sha256(email), never the address; cross-org leak-free — ca_referred_org carries only prior_ca_orgs COUNT): ca_invited / ca_joined / ca_referred_org (fires when the joining CA already holds an active CA seat in another org — the instrumentable referral signal; no referral-code machinery exists). Surfaces: POST /api/ca/invite (read+manage_users; NEW manage_users rows in GATES/_GATE_MARKER/API_ROUTE_GATES) + /api/ca/accept (read; identity IS the authorization) — route-coverage guard + full 6-role matrix green. Migration 0007_ca_seat per the 0005/0006 pattern: PG tenant_core.memberships +status (existence-guarded ALTER, mirror infra/006_ca_seat.sql; RLS from 0002 covers it); SQLite dev models AppUser/Membership created-if-absent. Tests: 9 unit (chains verified via verify_chain_for, PII-absence asserted, org-scoped accept, second-org referral) + 5 exemption tests + invite E2E lifecycle in test_rbac_matrix.py over real HTTP/JWKS (premature-accept 404, Accountant 403 named gate, dup 409 seals nothing). Green: 834 unit + 38 rbac-matrix + 7 hardening/migrations + 10 import-roundtrip; ruff+mypy (182 files) clean; check_rls_coverage 38/38 both paths. Skipped: HTMX audit-room invite form (API-only surface for now) — add when the Audit Room page needs it.
[WS4.5] DONE 2026-07-22 AGENT - Scheduled jobs tenant-iterated (app/jobs.py). run_once now iterates the orgs table (dev model Org mirrors tenant_core.orgs) and runs every requested job PER ORG with: (1) PER-TENANT FAILURE ISOLATION — per-job guarded() catch + per-org catch + fresh session per org; one org's exception is logged+recorded and the run CONTINUES (mutation-checked: removing the catch fails the isolation test); ONE summary line per run (command/period/orgs/ok/failed/skipped, also in the returned JSON); (2) IDEMPOTENCY — job_run ledger keyed UNIQUE(org,job,period): a 'done' row makes a same-period re-run a NO-OP (mutation-checked: metric rows don't double; breaking _already_done fails the test), an 'error' row does NOT block the retry, a new period runs again; (3) org scoping via the SAME RLS mechanism as the API (set_current_org + bind_org_guc checkout listener installed on the jobs engine — cron never imports app.main); (4) CLI contract unchanged (capture|brief|dunning|alerts|audit-verify|all|serve; exit 0 incl. idempotent skips — cron-safe re-runs; empty orgs on SQLite dev -> legacy single-tenant pass recorded as org "default", on Postgres -> tenant rows only); external-cron env documented in the module docstring (MAISHA_DATABASE_URL w/ tenant search_path note, MAISHA_MAHSA_URL, CFO/SMTP vars). run_audit_verify(org) walks the per-tenant chain (load_chain_for/verify_chain_for), org=None keeps the legacy global walk. Migration 0008_job_runs per the 0005/0006 pattern: PG tenant_core.job_run WITH ENABLE+FORCE RLS + policy + grants in the same revision (inline snapshot of infra/007_job_runs.sql), SQLite orgs+job_run created-if-absent. Tests: 4 new (iteration+isolation w/ per-org current_org() proven, done-noop + new-period-reruns, error-does-not-block-retry, legacy fallback) + existing 2 jobs tests untouched-green. NOTE: capture/brief remain org-blind internally — per-org DATA separation rides Postgres RLS (SQLite dev is single-tenant so the legacy pass avoids duplicate writes); per-tenant brief recipients (settings.cfo_email is global) = follow-on when tenant contact info exists.
[launch-pack] DONE 2026-07-22 AGENT - Complete self-serve launch package (owner configures backend/DB/hosting; nothing else needed). (a) env inventory from exhaustive grep (os.environ/getenv across api+dif+scripts+infra; import.meta.env/process.env across frontend; pydantic Settings fields): NEW root /.env.example (master reference, every var + purpose + dev default + prod-required flag incl. MAISHA_BETTER_AUTH_URL/ISSUER/AUDIENCE/MFA_CLAIM/PLAN_CLAIM, MAISHA_LEGACY_PASSWORD_AUTH, MAHSA_ADDR/RULES, RESTIC_*, PG_PSQL/PG_CONTAINER/PGUSER, REQUIRE_REDTEAM, MAISHA_TEST_POSTGRES_URL), api/.env.example EXTENDED (Better Auth block + claude_base_url/llm tunables/default_user_id + Postgres URL shape), NEW frontend/.env.example (VITE_API_BASE, VITE_BETTER_AUTH_URL, build-time semantics). (b) docs/DEPLOYMENT.md runbook — every step verified against code: alembic upgrade head is THE authoritative schema path (0002/0004/0006/0007/0008 inline infra/db/multitenant/001-007 verbatim per their own docstrings; .sql files = reviewable source + gate input, never applied to prod); tenant_core + maisha_app NOLOGIN + owner SQL (CREATE ROLE ... IN ROLE maisha_app + GRANT USAGE ON SCHEMA tenant_core — the USAGE grant exists in NO migration, verified by grep, so it is an explicit owner step); session-mode-pooler requirement derived from SET_ORG_GUC_SQL set_config(...,false) per-checkout binding (transaction pooler 6543 breaks RLS binding); Mahsa contract (cargo build --release, MAHSA_ADDR, conftest expects dif/target/{debug,release}/mahsa, embedded rules); Better Auth definePayload block reproduced from auth.ts's verified comment; jobs CLI capture|brief|dunning|alerts|audit-verify|all|serve + crontab line from jobs.py docstring; smoke = make ci + rls_redteam + test_verified_flow + /health + /audit/verify. HONESTY FINDING written into the runbook: API has ZERO CORS middleware and SPA/HTMX share /today,/inbox routes -> launch browser surface is the HTMX app; separately-hosted SPA needs a CORS follow-up code ticket (auth-surface change, NOT smuggled into this pack). (c) drift fixed, not redesigned: api/Dockerfile now COPYs alembic.ini+alembic/ (the compose migrate service ran `alembic upgrade head` in an image that DID NOT CONTAIN alembic — broken since 0002 shipped) and installs .[ocr,pg]; pyproject NEW pg extra (psycopg2-binary — %s paramstyle matches SET_ORG_GUC_SQL); docker-compose.prod.yml: MAISHA_DATABASE_URL now interpolated from .env with sqlite fallback (was hardcoded sqlite, silently overriding env_file -> Supabase launch impossible), unused redis service REMOVED (zero code reads redis, verified repo-wide grep), api healthcheck added (python urllib — slim image has no curl) + caddy waits on service_healthy; both compose files re-validated with docker compose config. (d) NEW scripts/preflight.sh: env-file loader (live env wins), prod-required vs dev-warn split mirroring assert_production_secrets + legacy-auth-hard-off, sqlite existence check (no side-effect file creation), Postgres psycopg2/search_path/6543-pooler checks, live SELECT 1, Mahsa /health-or-binary check, checklist output, exit 1 on missing required config. TESTED 4 ways: dev defaults exit 0; production w/ defaults exit 1 (4 FAILs incl. BETTER_AUTH_URL); production fully configured + live mahsa binary on :18999 exit 0 all-ok; Postgres-URL branch exit 1 on missing psycopg2 + both warns fire. Gates re-run green: 4 grep-gates (38 RLS checks), frontend tsc -b clean + vitest 158/158 + oxlint (2 pre-existing fast-refresh warnings only). Not run here: full make ci (no code path touched — rust/py suites unaffected by md/env/infra files); docker image rebuild (network build not attempted — COPY paths verified to exist).
[P2-ROUND] DONE 2026-07-22 ORCH - Workflow wf_977a8134-ba9 (11 agents, 0 errors, 5/5 adversarial-verify PASS): WS8.1 artifacts (registers/26AS/MSME + csv-zip & PDF exports, every figure through badge_state fail-closed, pack integrity hash covers section_notes, RBAC view_audit/export split); WS8.2 CA query threads + deterministic sampling (audit-chained, role negatives); WS8.3 CA free-seat exemption + invite/accept + referral events; WS4.5 tenant-iterated jobs (per-org failure isolation + org+job+period idempotency); WS7.9 PWA core (manifest+SW network-first-API, offline -> staleness downgrade per §0.4, responsive 360px, budget.json); launch-pack (.env.example x3, docs/DEPLOYMENT.md verified-against-code, scripts/preflight.sh tested, OWNER-STEP markers for Supabase/Better-Auth/hosting). SPA-gap-scout delivered the round-3 build list: core-loop ends MISSING EVERYWHERE — enter-data forms (invoice/bill/journal/employee) and filing flows (GSTR-3B/TDS/mark-filed) have no UI on any surface; P0-1..P0-5 + 8 P1 + 6 P2 items recorded in the workflow journal. make ci ALL GREEN.
[b1:action-machinery] DONE 2026-07-22 AGENT - P0-2 generic action preview/commit (INVARIANT 9 foundation for batch-2). Backend: app/web/api_actions.py — POST /api/domains/{d}/actions/{k}/preview + /commit wrapping the EXISTING app.web.actions registry (5 drawer actions; handlers untouched, so SPA and HTMX cannot drift into two mutations). Preview = schema-driven normalize/validate (required/select-options/number/ISO-date) + DRY-RUN of the real handler on the request session with an unconditional rollback (validation is the service's own, never re-implemented) + normalized echo + will_create message + computed badged figures (₹ fields echoed as exact paise via Paise.from_rupees, badged through badge_state — an input echo is not a coverage target so it honestly reads ◐, never a fabricated ✓ per §0.4) + preview_token. Token = stateless HMAC-SHA256(session_secret) over (org from VERIFIED principal §0.8, domain, key, normalized values) — commit-without-preview and edit-after-preview are 409 BEFORE the handler runs, by construction (client cannot mint the MAC); no store, no expiry sweep (values fully re-validated at commit; ponytail-noted upgrade = timestamp in the MAC). Commit returns created message + after_figures through the SAME _figures_for/§0.4 machinery as GET /domains/{d}. RBAC: preview=read (api_bulk precedent — sizing up a write is reading), commit=write (the capability the HTMX action_submit flow already requires); none of the 5 actions is statutory so require_filing does not apply (docstring pins that a future filing action must gate through it). API_ROUTE_GATES +2 rows, coverage guard + full 6-role matrix green alongside the concurrent api_filings work. Frontend: components/ActionDrawer.tsx — generic schema→form→preview-panel→confirm; the ordering is STRUCTURAL: commitPayload() is the only source of a commit body and returns null unless a live server preview is held, and it commits the server's normalized echo + token, never the live inputs; editField drops a held preview (its token no longer describes the screen). Badge fn injected from Domain.tsx (honestState + mahsa_up) so the drawer has no private path to a ✓. Errors through ErrorState operation="write" (4-question template). Keyboard: Enter advances fields, last field fires preview, Cmd/Ctrl+Enter (only that chord) confirms from preview. Domain.tsx: ActionRow (listed-not-armed) replaced by fireable ActionDrawer + refetch on commit; "no preview endpoint" prose removed. Tests: py 8 (integration/test_api_actions.py — preview mutates NOTHING row-count-asserted across all 5 actions [kills dropping the rollback]; commit with empty/bogus token 409 + row-count [kills dropping the hmac gate]; tampered-values-after-preview 409; preview→commit +1 row with after-figures never outside {verified,honest_pending}; expense ₹5000.50 → exactly 500050 paise badged ◐; named validation errors; 404; CA can preview all 5 and commit none, per-action) + vitest 8 (ActionDrawer.test.ts — form derived from schema incl. unknown-type→text; commitPayload null for editing AND committed phases [kills dropping the preview gate]; commits previewed echo not live inputs [kills committing live values]; edit invalidates preview; enterAdvance last→preview never commit; only Cmd/Ctrl+Enter confirms). Green: py test_api_actions 8/8 + test_rbac_matrix 38/38 (62s, with api_filings routes present), ruff+mypy clean (api_actions + main), tsc -b clean, vitest 166/166 (158 prior + 8 new), oxlint src exit 0 (2 new fast-refresh warnings, same exported-helper pattern as every other component file). Deferred: server 422 field-error details are not surfaced per-field in the drawer (lib/api.ts throws status-only ApiError — the native required/select controls prevent most client-side; surface the body when a second screen needs it); no audit seal on these single-row creates (parity with the HTMX action_submit flow — one decision for both surfaces, not a unilateral divergence).
[b1:filing-flow] DONE 2026-07-22 AGENT - P0-1 FILING FLOW (the missing end of the core loop; WS7.6-grade). Backend: app/web/api_filings.py — JSON preview→confirm wrappers over the EXISTING filing-gated writes (GstService.file_gstr3b / TaxService.file_tds_return / ComplianceService.mark_filed; services untouched). PREVIEW (read): computes via the same calc the write uses, badges every figure through the EXISTING §0.4 machinery — live Mahsa recompute via verify_claims for the ported targets (itc_setoff, late_fee_3b, interest_3b, late_fee_234e ⇒ real ✓/✕), total_payable stays ◐ forever (sum, no target), Mahsa-down ⇒ all ◐ + mahsa_up:false (never absorbed); WS7.2-shaped working panels (inputs/formula/statute citations s.47/s.50(1)/s.49(5)-Rule 88A/s.234E); verified figures sealed into a build_verdict hash with org from the VERIFIED principal (§0.8); returns confirm_token = sha256(canonical_json(action+inputs+figures)) and per-caller can_confirm + the decide_approval reason (Accountant sees the queue, the disable is capability-derived, not hidden). CONFIRM: same WS5.2 require_filing hard gate as the wrapped routes (gstr3b/tds_returns/mark_filed — wired, not duplicated), typed-confirm phrase (approvals rule), token RECOMPUTED server-side from the submitted inputs ⇒ a token from a different preview is 409 before anything runs; then the existing service write + a filing.recorded audit seal (figures+states+verdict_hash+trace_id in the hashed query payload). T5: receipt is structurally honest — recorded_as:"recorded_in_app", portal_submission:false, label "keep your portal acknowledgement"; preview AND confirm seal filing.* attempt-evidence entries; GET /api/filings/evidence (read+view_audit) returns the penalty-waiver JSON bundle (timestamps, figures shown, verdict hashes, trace ids, what-it-proves/does-NOT-prove). T12: generic deadline flow renders the fee as value_paise:null ◐ "not yet known — we don't guess", never an invented ₹. Queue GET /api/filings lists unfiled calendar rows (kind heuristic gstr3b/tds/deadline, ponytail-noted). API_ROUTE_GATES +8 rows; coverage guard + full 6-role matrix green. Frontend: routes/Filings.tsx at /file (route+nav APPENDED, T2): queue (overdue/due) → per-return form (whole-rupee → exact paise via rupeesToPaise, invalid input disables preview) → badged figures via VerifiedNumber+working panels with effectiveState payload-age downgrade (reuses Approvals ago/PAYLOAD_MAX_AGE_MS/useNow — useNow now exported) → typed confirm reusing confirmOk EXACTLY, disabled-reason from the server's own gate words → persistent receipt card w/ audit hash + trace id + attempt-evidence download; RecordFailure = 4-question template with refused-vs-no-response precision. badgeState whitelists server states (unknown ⇒ ✕, never ✓); amountText(null) ⇒ "not yet known — we don't guess". Tests (each names the mutation it kills): py integration test_api_filings.py 6/6 over real JWKS-signed JWTs + real Mahsa binary — roundtrip (hardcoded-badge + dropped-seal killers; chain verify_chain-intact; late fee ✓ to the paisa vs gst_calc), Accountant 403 on confirm w/ nothing written (kills dropping require_filing), tamper 409 w/ nothing written (kills skipping token recompute), typed-confirm 400, deadline null-fee ◐ + mark_filed wiring, evidence bundle carries figures/verdicts/traces + T5 honesty text. vitest Filings.test.ts 12 (badgeState unknown→unbacked kills `as VerifyState`; amountText kills `?? 0`; confirmDisabledReason kills `return null` + precedence swap; rupeesToPaise kills `*1`/decimal leniency; buildPreviewRequest kills dropping the null-guard). Green: pytest test_api_filings 6/6 + test_rbac_matrix 38/38, ruff+mypy clean, tsc -b clean, vitest 178/178, oxlint src exit 0 (fast-refresh warnings only, repo-wide pattern). WS7_BUILD_CONTRACT T5 row flipped ✅. Deferred: GSTR-3B/TDS previews take user-entered heads (portal-parity inputs) rather than auto-drafting from books — auto-draft from ITC register/TDS summary is a follow-on; queue covers calendar rows only (pending GstReturn/TdsReturn rows without a calendar entry don't surface — seed the calendar).
[b2:entry-forms] DONE 2026-07-22 AGENT - P0-3 ALTITUDE-2 DATA-ENTRY FORMS for the four highest-volume entities, built ON the P0-2 machinery (registry extended, no parallel path). Backend app/web/actions.py: Field grows `columns` (sub-schema for new type="lines" multi-row fields) and a handler may now return (message, figures) where the figures are engine-computed badged rows taken from the SERVICE's OWN return value (zero drift between preview and commit) and badged ONLY via mahsa_coverage.badge_state (§0.4 — unknown keys fall to ◐ mechanically; tds_on_payment is a ported coverage target so the TDS amount legitimately reads ✓). Seven new actions: revenue/create-customer + create-invoice (RevenueService.create_invoice; preview badges taxable value + the GST split the engine chose — IGST inter-state vs CGST+SGST intra-state from supplier-state × customer-state — + total + TDS/net when customer TDS applies), payables/create-vendor + create-bill (PayablesService.create_bill exercises tds_engine: preview badges TDS section, rate from payables_calc.tds_rate, amount from the service result; below-threshold states "no TDS arises" instead of a silent ₹0), ledger/journal-entry (multi-line via lines field; imbalance raises a named ValueError carrying BOTH formatted totals → 422 through the ErrorState 4-question template, service is_balanced backstops; balanced preview badges equal debit/credit totals ◐), payroll/add-employee + salary-structure (PayrollService.set_salary_structure; preview badges gross/PF-emp/PF-empr/ESI-emp/ESI-empr/PT/net/CTC from the stored structure and appends the check_ctc_compliance s.2(y) warning with the required-minimum ₹ when Basic < 50%). api_actions.py: normalize_values refactored to one _scalar_error primitive reused by top-level fields AND lines columns; lines values validated per-row/per-column with field-precise error names (lines[0].rate) and re-serialized CANONICALLY (sort_keys, no spaces) so the HMAC preview token binds identical bytes regardless of client key order; preview/commit unpack the (message, figures) handler shape (P0-2's 5 str handlers untouched). api_domains._field_json recursive so lines ship their column sub-schema; Field import aliased (pydantic Field collision). main.py action_submit unpacks the tuple for the legacy HTMX surface. Frontend: Domain.tsx FieldSpec type (+columns) — new actions appear on /domains/{revenue,payables,ledger,payroll} action bars automatically through the existing generic drawer rendering (routes/nav untouched, T2). ActionDrawer.tsx: pure helpers emptyRow/parseLines/setCell/addRow/lineEnterAdvance/chordAction/figureRow + LinesEditor grid whose single source of truth is the field's JSON value (cell edits write through setCell; phantom first row materializes on first keystroke). Keyboard per WS7.4 — Enter advances fields, lands in the grid from the prior field (firstCellRef), advances cell→row→GROWS the array from the last cell; Cmd/Ctrl+Enter is now phase-aware: previews from the form, confirms from the preview (chordAction; commit still only via commitPayload so the chord can NEVER skip the preview). figureRow pins §0.4 client-side: preview figures render the server's formatted value verbatim + state through the injected badge gate — no client math, no private ✓. Tests (mutation each kills): py integration/test_entry_forms.py 9/9 — preview mutates nothing across all 7 actions row-count-asserted (kills dropping the dry-run rollback), preview→commit +1 row each, bill ₹60,000 @194J/company: section=194J rate=10% amount raw == payables_calc.tds_on_payment(...)['tds_paise'] == 600000 with state == badge_state('tds_on_payment') (kills web-layer TDS re-implementation or hardcoded badge) + ₹40,000 below-threshold 0-and-says-why, invoice GST split raw == revenue_calc.compute_invoice for intra AND inter with the other split's keys ABSENT (kills client/web-layer split), journal 100-vs-90 named 422 with both ₹ totals + 0 rows (kills dropping double-entry validation), balanced totals badged ◐, lines[0].rate field-precise validation, salary preview figures == compute_components + s.2(y) WARNING present/absent (kills recomputing components or dropping the warning). vitest ActionDrawer.test.ts +8 (186 total) — initialValues seeds one canonical empty row, lineEnterAdvance cell→row→addRow (kills mouse-required arrays), setCell materializes phantom rows (kills lost first keystroke), garbage JSON → [] never a crash, lines edit drops a held preview, chordAction editing→preview/previewed→confirm/committed→none (kills chord-skips-preview), figureRow TDS badge value verbatim + state derived from payload through the gate (kills client math / defaulted ✓). Green: py full suite 1166 passed + 2 skipped, ruff+mypy clean on all touched files, tsc -b clean, vitest 186/186, oxlint src exit 0 (pre-existing fast-refresh warning class only). Deferred: HTMX drawer_form.html has no lines-grid renderer (SPA is the working surface; legacy form would need a JSON textarea for lines fields); per-field 422 highlighting in the drawer still pending lib/api.ts body surfacing (same P0-2 deferral).
[P0-4] DONE 2026-07-22 OPUS(b2:payroll-run) - PAYROLL RUN FLOW in the SPA, wired not duplicated. Backend: app/web/api_payroll.py — GET /api/payroll/runs/overview (employees + last-run status, stored figures honestly ◐, null net = "not yet known"), POST /api/payroll/runs/preview (per-employee net/PF/ESI/TDS badged via LIVE Mahsa recompute of payslip_recompute_claims; PF/ESI verify to the paisa, TDS/net stay ◐; confirm token bound to the exact figures; writes NOTHING — row-count asserted), POST /api/payroll/runs/confirm (typed month + token recheck → the SAME PayrollService.run_payroll write POST /runs performs; run_payroll now consumes the new preview_run() loop so preview==confirm by construction). APPROVALS LANDING via the existing fold-driven queue: build_snapshot emits payroll_run_pending, new rule PAYROLL-005 (warning, Internal payroll policy PP-1; rule pack 2026.06.1→2026.07.1, mirrored in payroll/rules.py) folds the domain yellow while a draft exists, and record_decision (the single choke point for HTMX+JSON+bulk decisions) calls the optional resolve_pending_runs hook — approve releases, reject voids. Artifacts: payslip.pdf/form16.pdf/ecr.txt as thin /api wrappers over the HTMX-only /d services, gated Capability.EXPORT; RBAC matrix table extended (6 rows). Frontend: routes/PayrollRun.tsx (/payroll-run appended to App/Shell per T2) — employees table, last-run card with artifact downloads (new lib/api.ts apiBlob keeps auth on binary fetches), preview per-employee VerifiedNumber cards + totals, T4 staleness downgrade, confirm disabled until a preview exists (runConfirmDisabledReason — INVARIANT 9 visible), 4-question ConfirmFailure, persistent draft receipt linking to /approvals. Tests: 5 integration (test_api_payroll_run.py: preview mutates nothing + live badges; Mahsa-down all-◐; 400/409 nothing written; the full land-in-approvals→decision-releases loop citing PAYROLL-005; artifact RBAC 403 export + content types) + 9 vitest (PayrollRun.test.ts: hostile state→✕ never ✓, null→no invented ₹, stale ✓→◐, no-preview confirm gate, month off-by-one). Also fixed the check_money_format gate false-positive (a comment in ActionDrawer.test.ts contained the banned token). Gates: full py 1171 passed, rust 98+18+parity, eval 13/13, ruff+mypy+clippy clean, all 4 grep-gates OK, tsc/vitest 204/oxlint clean.
[P0-5] DONE 2026-07-22 AGENT(b2:csv-reimport) - TREASURY CSV RE-IMPORT, built on batch-1 (b1:filing-flow, b1:action-machinery) without touching either. Extracted Onboarding.tsx's bank-CSV dry-run→confirm into frontend/src/components/BankCsvImport.tsx: the parser (splitCsvRows/parseCsvDate/parseCsvAmount — field-for-field mirrors of treasury/service.py's _HEADER_MAP/_DATE_FORMATS/_parse_amount), previewStatement (the dry run), canConfirmImport (the confirm gate as its own pure predicate, previously only implicit in a JSX disabled= expression), StatementPreview/ImportOutcome (rendered dry-run + reconciled outcome), and the BankCsvImport component itself (owns staged file + import mutation; accountId/traceNamespace props, onImported callback, optional footer render-prop so a caller can append its own next-step control without forking the component). Onboarding.tsx now imports and re-exports the moved pure functions from their new home (so Onboarding.test.ts keeps passing UNCHANGED) and renders <BankCsvImport accountId=... traceNamespace="onboarding-import" footer={...}/> in place of its own copy — net -370 lines. Mounted a SECOND call site: routes/Domain.tsx renders a "Re-import bank statement" section for domain==="treasury" only (TreasuryReimport: fetches accounts, hides the picker below 2 accounts per the OrgSwitcher T2 precedent, points at /onboarding when there are none, keys BankCsvImport on the selected account so switching accounts can't carry a stale staged preview into a confirm); onImported triggers the domain query's own refetch so figures/badges refresh from a live re-fetch, never a client-guessed update. Backend: added GET /api/treasury/accounts (app/domains/treasury/router.py + schemas.AccountSummary) — the ONLY way the picker gets a real account id, since nothing previously listed accounts (cash_position only aggregates by bank_name). RBAC: read-only, router baseline, +1 row in API_ROUTE_GATES (coverage-guard-verified). Root-cause fix found while extracting: the shared import fetch was calling raw fetch() with credentials:"include" but NO Authorization header — lib/api.ts's authHeaders() was never attached, meaning the bearer-JWT-gated import route would 401 in any real deployment; now attached via the same authHeaders() every other call uses, fixed once in the shared component rather than forked into two call sites. Tests: py tests/unit/treasury/test_treasury_accounts_route.py (2 — empty list is [] not an error; lists real ids/bank_name/account_number/current_balance_paise, kills a field-name mismatch against the wire contract), test_rbac_matrix.py entry for the new route (full 6-role matrix + coverage guard, no dedicated new test file needed). vitest components/BankCsvImport.test.tsx (8) — StatementPreview rendered via react-dom/server renderToStaticMarkup (already a dependency; no jsdom/@testing-library added, matching the repo's existing pure-logic-only test convention while still exercising a REAL React render, since no test file in this repo has ever mounted a component): row count/date range/₹ totals/skip count from a real previewStatement() call, the server's own rejection reason rendered verbatim, the zero-importable-rows state rendered distinctly; canConfirmImport exhaustively gated (no preview, rejected preview, zero rows, pending write all block; only a good non-empty preview with nothing in flight allows). Green: pytest test_treasury_accounts_route+test_rbac_matrix+test_app+test_api_domains+test_import_roundtrip 130 passed/1 tracked-skip, ruff+mypy clean on touched files, tsc -b clean, vitest 203/203 (Onboarding.test.ts unchanged and still green), oxlint src exit 0 (fast-refresh warnings only, repo-wide pre-existing pattern). PROGRESS_BOARD P0-5 row added ✅.
[P0-CORE-LOOP] DONE 2026-07-22 ORCH - Workflow wf_006890cb-635 (10 agents, 0 errors, 5/5 verify PASS): the two missing ends of the core CFO loop now exist. P0-1 /file filing flow (api_filings.py: preview→confirm over the EXISTING require_filing gates; confirm_token = sha256(canonical inputs+figures) recomputed server-side so preview and commit cannot diverge; figures ✓ ONLY from live Mahsa verify_claims, total ◐ forever, Mahsa-down → all ◐; T5 honesty: recorded_in_app never portal_submission, attempt-evidence bundle for penalty waivers; RBAC matrix +8 rows all 6 roles). P0-2 generic action preview/commit machinery (api_actions.py + ActionDrawer: schema-driven forms, preview NEVER mutates — rollback + row-count-asserted, HMAC preview token). P0-3 Altitude-2 entry forms on that machinery (customer+invoice w/ GST split preview, vendor+bill w/ TDS section/rate/amount badged on preview, multi-line journal w/ double-entry validation, employee+salary w/ CTC/PF/ESI preview; keyboard flow Enter-advance/Ctrl+Enter). P0-4 payroll run (preview per-employee badged figures → confirm → lands in approvals; payslip/Form16/ECR downloads in SPA). P0-5 treasury CSV re-import (Onboarding dry-run component extracted + reused). NOTE: verify:action-machinery's mutation experiments tripped the harness security scanner (HMAC/rollback temporarily disabled DURING verification); ORCH independently confirmed restoration (guards live at api_actions.py:224/230/259) + re-ran the 52 security-relevant integration tests green. make ci ALL GREEN.
[P1-1] DONE 2026-07-22 AGENT(a:ask) - ASK MAISHA SPA SCREEN. Backend: POST /api/ask in app/web/api_domains.py — a thin wrapper threading VERBATIM through the EXISTING app.core.ask.answer_query pipeline (same fold→validate→unfold Mahsa loop the HTMX /ask page already calls; no re-derivation, so the two surfaces cannot drift). Response projects each Figure's already-threaded per-figure FigureVerdict (app.core.ask._verdict, WS7-E2E-OPEN 4b) to the same tri-state string the HTMX answer_card.html template keys off ("verified"/"honest_pending"/"unbacked") — one source of truth, not re-derived from the display badge. RBAC: router's existing `read` baseline only (matches GET /api/domains precedent, no new capability invented); API_ROUTE_GATES +1 row, coverage guard + full 6-role matrix green (Investor correctly denied — proven with a real signed JWT in the new integration test, not just the matrix). Frontend: routes/Ask.tsx at /ask (route+nav APPENDED to App.tsx/Shell.tsx, T2) — question box (+ the 4 HTMX suggestion chips) → useMutation POST → answer card: every figure renders through the EXISTING VerifiedNumber component, state via toVerifyState() which fails closed to "unbacked" for anything not in the server's 3-state enum (never fuzzy-upgrades an unrecognised string to ✓); narrative/citations/status-pill/requires-approval/mahsa-down-note render verbatim from the payload — the narrative is NOT re-verified client-side (that is app.llm.retry's server-side contract, per the ticket). Loading/failure via the EXISTING ErrorState 4-question template + useTraceId. Tests: py tests/integration/test_api_ask.py (5 — fact-backed figures with honest tri-state incl. at least one honest_pending, unroutable query abstains without fabricating a domain, Mahsa-down states mahsa_up:false, empty q is 422 not 500, Investor denied 403 naming `read`) + vitest routes/Ask.test.ts (3 — toVerifyState passes the 3 real states through unchanged, and fails closed to unbacked on empty/bogus/wrong-case input, so a payload typo can never silently render a false ✓). Green: pytest full suite 1177 passed/2 skipped (tracked) + test_rbac_matrix 40/40, ruff+mypy clean, tsc -b clean, vitest 212/212 (16 files, concurrent P1-5 statements work merged in cleanly), oxlint src exit 0 (fast-refresh warnings only, repo-wide pre-existing pattern). No new deps.
[P1-5] DONE 2026-07-22 AGENT(a:statements) - FINANCIAL STATEMENTS SCREENS: /statements SPA route (tabs: Trial Balance / P&L / Balance Sheet / General Ledger) over a new read-only assembler app/web/api_statements.py — a thin wrapper calling the SAME LedgerService methods the existing /api/ledger GET endpoints use (nothing re-derived), badging every money figure through app.core.mahsa_coverage.badge_state exactly like api_domains._figure (§0.4: no ledger key is Mahsa-ported, so every statement figure honestly ships ◐, never a hardcoded ✓; the SPA clamps unknown states to ✕ via toVerifyState). GET /api/statements (TB+P&L+BS figures + chart of accounts for the GL picker) and GET /api/statements/gl/{account_id} (running-balance drilldown via the existing service.general_ledger, 404 on unknown account) — both READ-gated on the router baseline, +2 rows in API_ROUTE_GATES (coverage-guard + full 6-role matrix green, 38 passed). Broken book LOOKS broken: trial_balance.balanced and balance_sheet.balanced survive to the payload untouched and render as explicit role=alert banners carrying the server's exact ₹ diff ("do not file or report from them"); null money renders "not yet known — we don't guess", never a figure; a genuine 0 stays ₹0.00. GL table is Tally-dense (tnum everywhere, inrPrecise paise-exact cells reused from BankCsvImport, balance-column badge from the payload's state rendered once in the header, closing-totals row emphasised by background not weight per BRAND_THEME §4). Printable: @media print in theme/tokens.css hides .shell-aside/.no-print (tab bar, picker, retry) — statements print as plain paper. Shell nav + App.tsx route APPENDED (T2). Tests: py tests/integration/test_api_statements.py (3 — badged payload shape with exact paise + exactly honest_pending states; imbalance flag + exact 12,345-paise diff survive to payload seeded via a raw lone JournalLine that post_journal_entry refuses; GL running balance to the paisa + 404) — mutation-proofed live: hardcoding balanced:True + state:"verified" in the assembler fails all 3, reverted green; vitest routes/Statements.test.tsx (7 — TB banner renders w/ server diff + absent when balanced; BS equation banner; null total renders we-don't-guess never ₹0/None and raw 0 stays ₹0.00; toVerifyState clamps hostile/missing states to unbacked never verified; GL hostile payload state renders ✕ + tnum totals row). Gates: pytest test_api_statements+test_rbac_matrix+test_app+test_api_domains+unit/ledger all green, ruff+mypy clean on touched files, check_money_format OK, tsc -b clean, vitest 212/212, oxlint src exit 0 (pre-existing fast-refresh warnings only).
[P1-8] DONE 2026-07-22 AGENT(a:expense-ocr) - EXPENSE CLAIM + RECEIPT OCR IN THE SPA. The expense `submit-claim` action was already registered in the round-3 action registry (app/web/actions.py) and rendered generically by ActionDrawer/Domain.tsx — extended rather than re-registered: added an optional `vendor_gstin` field (folds into the claim's existing `description` text; ExpenseClaim has no dedicated GSTIN column, ponytail-noted — add one when claims need to reconcile against a vendor's ITC) and made `_submit_claim` surface expense_calc.check_policy's existing over-policy result as a WARNING sentence in the preview's `will_create` message (exact ₹ limit + excess, same convention _salary_structure already uses for its Code-on-Wages warning) instead of silently dropping it. Backend: app/domains/expense/router.py gets POST /api/expense/ocr-receipt — a thin wrapper calling the SAME ExpenseService.ocr_capture the HTMX /d/expense/ocr-receipt route already calls (one parser, proven identical by a py test asserting byte-identical 503 detail from both routes when tesseract is absent); router-baseline `read` only (OCR mutates nothing), +1 API_ROUTE_GATES row (coverage guard + full 6-role matrix green). Frontend: ActionDrawer gained an optional `prefill` prop (mergedInitialValues overlays it onto the blank form, but ONLY for keys the action's own schema declares — a stray/future OCR field can never reach a form that doesn't expect it) and renders a fixed caveat ("Parsed from receipt — check before submitting: <fields>. OCR is never authoritative...") whenever any prefill is held; the prefilled values sit in the SAME editable inputs and go through the SAME preview-then-confirm gate as manual entry, nothing new to bypass. Domain.tsx mounts a new ExpenseReceiptCapture (file/camera input, accept="image/*" capture="environment") only above the expense submit-claim drawer; a successful parse maps {amount_paise, gstin, date} to {amount, vendor_gstin, expense_date} (receiptDateToIso trusts a bare ISO date or reorders the day-first DD/MM/YYYY-DD-MM-YYYY shapes expense_calc's own date regex accepts; anything else is left blank rather than guessing a month/day swap) and remounts the drawer via a nonce key so each new photo starts a fresh prefill+preview, matching the TreasuryReimport/BankCsvImport key-reset precedent. Root-cause fix found while running the wider suite: app/web/api_actions.py's commit route called `_figures_for(db, domain)` — one call site that never picked up the `role` parameter api_domains.py's own two call sites already carry (a WS5.1 T11 field-masking addition); fixed at the single call site rather than special-cased, restoring 1 previously-broken pre-existing integration test. Tests: py — test_app.py::test_expense_ocr_json_route_reuses_the_same_handler_as_the_htmx_route (same 503 detail from /d/ and /api/ for identical bytes), test_api_actions.py::test_expense_preview_surfaces_the_policy_limit_warning (over-limit vs within-limit preview text), test_rbac_matrix.py +1 row; vitest — ActionDrawer.test.ts (mergedInitialValues: overlays prefill, no-op with none, drops undeclared keys), ActionDrawer.receipt.test.tsx (renderToStaticMarkup, matching the repo's existing no-@testing-library convention: prefilled values are real editable `<input>`s not frozen text, caveat renders naming only the prefilled fields, absent entirely with nothing to prefill), Domain.test.ts (receiptDateToIso, ocrResultToPrefill — zero-amount is a real prefill not "nothing found", unrecognised date shapes stay blank rather than guess). Gates (this ticket's own files, isolated — other tickets are landing in this same working tree concurrently so a whole-repo count is a moving target): the 3 new/touched py tests + the full test_rbac_matrix.py + test_api_actions.py + test_app.py files all green; ruff+mypy clean on every touched .py file; tsc -b clean repo-wide; the 3 new/touched vitest files (Domain.test.ts, ActionDrawer.test.ts, ActionDrawer.receipt.test.tsx) 46/46 green in isolation, and green again inside a full `vitest run` alongside concurrent tickets' tests at time of writing; oxlint src exit 0 on the touched files (only the pre-existing repo-wide fast-refresh warning pattern, no new warning kind). Also independently confirmed a full backend `pytest -q` (1190 passed/2 skipped, tracked) at a point-in-time snapshot. No new deps.
[P1-7] DONE 2026-07-22 OPUS(a:field-rbac) - FIELD-LEVEL RBAC PAYLOAD MASKING (contract T11), one clearance system not two: app/core/landing.py grows a salary_detail sensitivity class between confidential and restricted (Accountant clearance raised to it — they run payroll; CA/Approver stay confidential, so per-employee pay is theirs to NOT see) + FIELD_SENSITIVITY map (emp{id}.* payroll targets + monthly_net_paise -> salary_detail; gross_margin/cac/ltv/payback_months/ltv_cac_ratio -> confidential, so Investor links never carry unit economics) + ONE helper mask_field/mask_figures reusing the EXISTING can_view_sensitivity. Applied at the serialization boundary: api_payroll runs_overview (per-employee monthly_net_paise) + runs/preview + runs/confirm (per-employee figure cards; masking AFTER confirm-token minting so the seal stays over the real figures; aggregate totals stay visible), forecast unit-economics response, and api_domains._figures_for (the generic domain-figure boundary — no key masks there today, future sensitive snapshot keys can't leak). Masked field -> {"restricted": true, "reason": "requires <class> clearance"} + target/label only — the value is ABSENT from the body, never dimmed/zeroed. SPA: LockChip + isRestricted + RestrictedField in VerifiedNumber.tsx, FigureCard in PayrollRun.tsx renders masked figures as an explicit lock chip with the reason (hidden-not-absent honoured; overview table cell too). Tests: 6 new unit (lattice + map + helper), 2 integration in test_rbac_matrix.py with BYTE-LEVEL asserts (two distinct-salaried employees so no total coincides with a part; CA/Approver bodies contain no salary bytes, Accountant/Owner do — dropping mask_field fails these directly), 6 vitest (LockChip render never blank/never a badge, isRestricted shape guard, FigureCard both paths). No new routes (matrix table unchanged, coverage guard green). Full py suite 1190 passed; tsc/vitest 230/oxlint clean.
[P1-2] DONE 2026-07-23 AGENT(b:audit-room) - AUDIT ROOM PARITY IN THE SPA. Extended frontend/src/routes/AuditRoom.tsx (existing chain viewer untouched) with the three WS8.2 surfaces the HTMX audit room already had: (1) CA QUERY THREADS over the EXISTING /api/audit/threads CRUD — RaiseForm (domain/entry_ref/question, notes that the raise is sealed and cannot be silently withdrawn), ThreadCard per thread with state chip (open→responded→resolved), full event history where EVERY event shows its audit-chain seal ref (audit_hash, mono ident) + doc ref, respond-with-vault-doc form, and resolve (offered ONLY on a responded thread, mirroring the server rule); role-awareness comes ONLY from the payload: threadGates(state, can_respond, respond_denied_reason) renders the respond control enabled, or disabled WITH the server's own reason verbatim (missing reason falls to an honest default, never an enabled button); mutations invalidate BOTH ["audit-threads"] and ["audit"] since every transition seals onto the one chain the log below displays. (2) DETERMINISTIC SAMPLING view on GET /api/audit/sample — native date/number spec form → SampleResult table (seed shown in mono because determinism is the point, population count, voucher rows with inrPrecise paise-exact debit/credit, vault doc bundle refs per voucher, honest "no vault documents linked"/empty-sample states). (3) PACK DOWNLOADS — /api/audit/pack.zip + .pdf via apiBlob, rendered ONLY when the payload's can_export is true (hidden-not-disabled per T11: an Approver must not see that exportable packs exist; fail-closed while loading); 503 (Mahsa down, pack refuses to bind a rules version) gets its own honest copy. SERVER TWEAK (the one payload gap): GET /api/audit/threads response now carries can_respond/respond_denied_reason/can_export computed via can(principal.role, ...) — same can_confirm convention as api_payroll/api_filings; same VIEW_AUDIT gate (require moved from route dependencies= to a Principal param), NO new routes so no new matrix rows. Failure states reuse ErrorState (writes as operation="write") + useTraceId per surface. Tests: +17 vitest in AuditRoom.threads.test.tsx (repo convention: pure fns + renderToStaticMarkup, no jsdom) — threadGates capability/lifecycle matrix (server reason verbatim, missing-reason fails closed, resolve only on responded), ThreadCard lifecycle renders (seal refs visible, no live form for the incapable), PackDownloads renders "" without export, sampleQueryString param mapping, SampleResult money/doc-refs/empty honesty; existing AuditRoom.test.ts untouched and green. +1 py integration (test_rbac_matrix.py::test_threads_payload_carries_the_callers_own_capabilities): CA can_respond=False w/ write reason + can_export=True, Accountant True/None/True, Approver can_export=False. Gates: npx tsc -b clean, vitest AuditRoom files 22/22 (full FE run: only Ask.test.ts fails — another agent's in-flight P1-1 file, count varies between runs as they edit), oxlint 0 findings beyond the repo-accepted only-export-components warnings, ruff+mypy clean on both py files, threads/sampling integration tests green. PRE-EXISTING (not mine): test_rbac_matrix full-file run fails on POST /api/ask declared in the table but missing from the app (a:ask's in-flight backend) — proven pre-existing by stash-run-restore of my two files.
[P1-3] DONE 2026-07-23 AGENT(b:ca-invite) - CA SEAT INVITE/ACCEPT UI. New /settings route (Shell nav appended, T2): a CA seat section over the EXISTING WS8.3 endpoints — invite-by-email form (POST /api/ca/invite) with the free-and-unlimited entitlement fact STATED verbatim in the copy, not re-derived (it's real by app.core.entitlements.SEAT_EXEMPT_ROLES, already tested there — this screen just says it). Pending-invites needed one new endpoint: GET /api/ca/pending (app.core.ca_seat.list_pending — a plain join query, no new capability; gated `manage_users`, same as invite itself) + +1 API_ROUTE_GATES row + 3 new assertions folded into test_ca_invite_accept_lifecycle_and_referral_events (owner sees the pending email, Accountant 403s naming the gate, list empties once accepted — kills a status-column filter bug directly). Non-owner "disabled-with-reason" is derived from that route's own live 403 (caSectionDeniedReason(error)), mirroring PayrollRun's ConfirmFailure precedent — no second, driftable copy of the RBAC table baked into the frontend. /ca/accept is the invited CA's own screen: POST /api/ca/accept already matches the pending membership on the CALLER'S verified token email+org (app.core.ca_seat.accept_ca, WS8.3's own design — there is no URL invite token to consume; identity IS the authorization), success redirects to /audit (Role.CA's landing per app.core.landing.ROLE_LANDING). A 404 ("no pending CA invite for this account in this org") renders its own honest sentence — that's an expected outcome, not a system failure, so it does NOT go through the 4-question ErrorState template; any other status does. Both screens split into router-free presentational components (CaInviteForm, PendingInvitesList, CaAcceptCard) so they render via the repo's existing no-@testing-library renderToStaticMarkup convention (see BankCsvImport.test.tsx) — the hooked wrappers (Settings, CaSeatSection, CaAccept) own useQuery/useMutation and stay untested directly, same split every other SPA screen in this repo already uses. Tests: vitest Settings.test.tsx (12: caSectionDeniedReason 403-only / not-a-role-signal on 500 or network error, canSubmitInvite edge cases, inviteErrorText per status, CaInviteForm disabled/enabled/submitting/error render, PendingInvitesList honest-empty vs populated) + CaAccept.test.tsx (10: isNoPendingInvite 404-only, CaAcceptCard idle/pending/404/500/success renders — 404 gets the honest sentence and NOT "What happened", 500 gets ErrorState and NOT the honest sentence). Gates (isolated to this ticket's files; other tickets are landing in this same working tree concurrently): ruff+mypy clean on ca_seat.py/api_domains.py; npx tsc -b clean repo-wide; new/touched vitest files 22/22 green in isolation and again inside a full `vitest run` (266/267 — the 1 failure is Ask.test.ts, untracked WIP from a:ask's in-flight sibling ticket, confirmed unrelated via `git diff HEAD` showing zero changes of mine touch it, same pre-existing pattern P1-2 independently hit and proved by stash-run-restore); oxlint src exit 0 (pre-existing fast-refresh warnings only, no new warning kind); py: 10/10 CA-scoped tests green (9 unit exemption/seat + the extended lifecycle integration test). No new deps.
[b:health-strip] DONE 2026-07-23 AGENT(b:health-strip) - P1-6 CONNECTION-HEALTH STRIP LIFTED TO SHELL + FIGURE STALENESS THREADED INTO Today/Domain. The Approvals-only `useConnectionHealth()`/`booksFreshness()` wiring did not exist anywhere else — Today.tsx and Domain.tsx only downgraded a ✓ on `mahsa_up:false`/request-error, never asked `/api/health/connections` on the happy path, so a ✓ could sit on stale source data with nothing on screen saying so (the exact T4 gap named at the top of WS7_BUILD_CONTRACT.md §3). Two parts: (1) SHELL STRIP — `ConnectionHealthStrip` mounted once in Shell.tsx's `<main>` (visible on every screen), reusing the SAME `useConnectionHealth()` hook (react-query dedupes by `FRESHNESS_QUERY_KEY`, no second request on screens that also read health themselves); `healthStripQuiet(data, error, isLoading)` is the pure gate — quiet only with a healthy payload or on first load, NEVER quiet on a failed check (anti-pattern #14: broken must not read as fine); when shown, a native `<details>` (no dialog/portal dependency) states the server's own headline and expands to the full detail panel. Refactored `ConnectionHealth()`'s body into a presentational `ConnectionHealthPanel` (data/isLoading/error/refetch as props) so the strip's popover reuses the SAME panel fed the data it already fetched, rather than a second nested query — this also means `HealthStripBody` renders with no QueryClientProvider needed in tests, matching the repo's no-@testing-library convention. (2) FIGURE STALENESS — lifted `booksFreshness`/`ago`/`useNow`/`PAYLOAD_MAX_AGE_MS` out of Approvals.tsx into `frontend/src/lib/freshness.ts` (Approvals.tsx now imports+re-exports the same names so its own tests and the pre-existing PayrollRun.tsx/Filings.tsx imports from "./Approvals" keep working unchanged — avoided a Today.tsx↔Approvals.tsx import cycle, which a direct cross-import would have created). Today.tsx: extracted `CashStrip` (both the happy-path and last-known-on-error renders now go through it) and threads `booksFreshness(health.data, age)` into it on the happy path, where it previously defaulted to no downgrade at all. Domain.tsx: exported `FigureGrid`, computed the same `fresh.stale` in `Domain()` from its own `useConnectionHealth()`+`useNow()`+`dataUpdatedAt`, and threaded it through `DomainBody`→`FigureGrid` on the happy path (layers on top of the pre-existing `honestState`/`mahsaUp` downgrade — `effectiveState` only ever downgrades, never re-upgrades, so the two compose safely). Tests: vitest ConnectionHealth.test.ts +6 (healthStripQuiet quiet-only-when-healthy/first-load, NOT quiet on error or an empty settled response; HealthStripBody renders the server's headline / the distinct "check itself failed" copy) using `createElement` + `renderToStaticMarkup` per repo convention; Today.test.ts NEW (4 — CashStrip renders plain ✓ when nothing stale, downgrades to ◐ + the "Downgraded from ✓" copy when `stale` is threaded through, distinguishes `stale:"unknown"` copy from real staleness, never upgrades an already-honest_pending figure — mutation-proofed: removing the `stale` pass-through from Today.tsx/CashStrip fails this directly); Domain.test.ts +4 (FigureGrid: ✓ survives when nothing stale, downgrades to ◐ purely from `stale` even with `mahsaUp:true`, never upgrades an unbacked figure, confirms the pre-existing `mahsaUp:false` outage downgrade still fires with `stale` defaulted — same mutation-proof guard). Gates (isolated to this ticket's files; other tickets are landing in this same working tree concurrently): `npx tsc -b` clean repo-wide except one pre-existing error in the untracked, concurrently-in-flight `Cfo.tsx` (confirmed via `git status` — untracked, never opened by this ticket); `npx vitest run` 298/299 — the 1 failure is `Ask.test.ts`, untracked sibling WIP unrelated to any file this ticket touched (same pre-existing pattern P1-2/P1-3/P1-4 each independently hit); `npx oxlint src` exit 0 (only the repo-wide pre-existing fast-refresh-warning pattern, no new warning kind). No new `/api` routes, so no RBAC-matrix rows needed. No new deps.
[P1-4] DONE 2026-07-23 AGENT(b:cfo-strategy) - CFO STRATEGY IN THE SPA: /cfo route (Shell nav + App route appended, T2) with three sections over EXISTING endpoints only. (1) Scenario runner on POST /api/forecast/scenario (endpoint untouched): rupee-input form → paise body → badged outcome cards (net change/min cash/runway) all ◐ with a "hypothetical — not recomputed by Mahsa" note (a client can never mint ✓, §0.4); null-runway honesty is the WS7-E2E logic applied to the projection: empty form → "nothing entered — no runway to compute", null-while-cash-positive → the provable "not burning" fact from the same payload, null-while-BURNING → "longer than the N-month horizon — we don't guess" (never ∞/unbounded, since the server only projected N months); result labelling frozen to the inputs it ran with. (2) Cap table on GET /api/equity/cap-table (untouched): category table w/ Indian-grouped tabular share counts + %, ESOP pool line from the payload's own shares/pct, honest-empty register ("not a 100%/0% split"); SAFE/convertible = the EXISTING POST /api/equity/safe/convert pure-compute engine behind a converter form (no stored SAFE register endpoint exists — STATED in the UI rather than inventing balances). (3) Investor update via NEW thin wrapper app/web/api_investor.py POST /api/investor/preview over the SAME app.core.strategy.investor_update generator the HTMX /cfo+/investor pages render (verbatim, zero re-derivation; figures badged via badge_state → all honestly ◐; payload carries runway_months+accounts RAW, never the email template's pre-baked "∞", so the SPA reuses Domains.runwayText VERBATIM — the WS7-E2E fix, not regressed); highlights textarea → rebuild preview; send stays HTMX/email infra — the card links out to /investor and the router exposes NO send route (pinned by test). RBAC: read-gated via existing rbac_deps.require(Capability.READ), +1 API_ROUTE_GATES row (markers verified matching). Tests: py test_api_investor.py 3/3 (badged shape + generator-verbatim + empty-ledger null-runway-never-∞ + no-send-route); vitest Cfo.test.tsx 17/17 (scenario null-runway honesty incl. burning-past-horizon, cap-table render + honest-empty, investor preview render + both null-runway sentences + link-out-no-button). Gates on this ticket's files: ruff+mypy clean, tsc -b clean repo-wide, oxlint exit 0 (pre-existing fast-refresh warnings only), full vitest 297/299 — the 2 failures are untracked sibling WIP (Ask.test.ts, Today.test.ts; git status confirms, files untouched by this ticket). RBAC coverage guard: only pre-existing mismatch is sibling P1-1's POST /api/ask row (route not yet landed in tree); this ticket's row verified green in isolation. No new deps.
[r:ask-backend] DONE 2026-07-23 AGENT(r:ask-backend) - CRASH-LOST POST /api/ask REBUILT. The P1-1 backend was lost in the session crash (frontend Ask.tsx, test_api_ask.py, and the rbac-matrix row all survived; the route did not — 5 ask tests + 2 matrix tests red 404/coverage-mismatch). Re-implemented as a thin wrapper in app/web/api_domains.py (ask_json): AskBody{q,min_length=1} → the SAME app.core.ask.answer_query pipeline the HTMX /ask page calls, verbatim (registry/settings/mahsa via existing seams get_mahsa+get_settings; no forked logic, so a figure's verdict cannot drift between surfaces) → JSON projection of the Answer view-model, with each Figure's FigureVerdict projected by _tri_state: verified→"verified", honest_pending→"honest_pending", anything else (blocked or unknown) FAILS CLOSED to "unbacked" (§0.4 — ✓ only from the existing verdict machinery, never minted in the wrapper). RBAC: rides the router-level require(READ) baseline — deployed markers match the pre-existing "POST /api/ask": ("read",) row, so the coverage guard and full matrix are green again. +1 test pinning _tri_state's fail-closed projection directly (the blocked path is unreachable over HTTP with the LLM off — pinned so a mutation upgrading blocked→pending fails loudly). REPAIR NOTES, honestly: (a) mid-ticket the T11 field-rbac rebuild re-landed 3-arg _figures_for masking on BOTH api_domains.py and api_actions.py atomically (superseding the api_actions.py:286 HOTFIX comment's 2-arg revert) — merged cleanly with this route, both suites re-run against the merged tree; (b) the repo-wide frontend gate was red on Onboarding.tsx (pickFirstFigure typed Figure[] while T11 made DomainData.figures (Figure|RestrictedField)[]) — fixed at the seam: pickFirstFigure now accepts the union and skips restricted entries via the existing isRestricted guard (a value-stripped field can never be the payoff figure). Gates: test_api_ask.py 6/6, test_rbac_matrix.py 41/41 (incl. full role×route matrix + coverage guard), ruff+mypy clean on touched py files; frontend npx tsc -b clean, vitest 299/299 (Ask.test.ts incl. the ORCH fail-closed toVerifyState fix — green), oxlint exit 0 (pre-existing fast-refresh warnings only). No new routes beyond the one rebuilt, no new deps.
[r:field-rbac] DONE 2026-07-23 AGENT(r:field-rbac) - CRASH-LOST T11 FIELD-LEVEL RBAC REBUILT ATOMICALLY. Reconciliation first (git diff HEAD, no duplication): the crash had left the ticket half-landed — SURVIVED: app/core/landing.py masking machinery complete (salary_detail rung inserted between confidential and restricted in SENSITIVITY_ORDER; Accountant clearance raised to salary_detail; FIELD_SENSITIVITY map incl. emp{id}.* regex + margin/unit-economics keys; mask_field/mask_figures — value ABSENT from the body, only {"restricted": true, "reason": "requires <class> clearance"} + identifying target/key/label survive) with full unit tests (test_landing.py), api_payroll.py masking (runs_overview monthly_net_paise + _masked_employees on preview/confirm, applied AFTER confirm-token minting), forecast unit-economics masking, the two byte-level payroll matrix tests, and the whole SPA lock-chip layer (VerifiedNumber isRestricted/LockChip/RestrictedField + PayrollRun wiring + vitest) — all kept verbatim. LOST: the 3-arg _figures_for in app/web/api_domains.py (reverted to 2-arg), which is what forced ORCH's api_actions.py:286 HOTFIX (2-arg after_figures call, unmasked). REPAIRED ATOMICALLY: _figures_for(session, domain, role) — role a REQUIRED positional (a forgotten call site is a loud TypeError, never a silently unmasked payload) — routes every figure list through mask_figures; domains_json + domain_json now resolve the principal (§0.8, resolve_principal seam) and pass principal.role; the HOTFIX comment + 2-arg call at api_actions.py replaced with the real masked 3-arg call in the SAME change; domains_json coverage loop counts f.get("state") so a masked figure counts in total, never as verified (§0.4 stays with badge_state — masking never mints or upgrades a verdict). Honest scoping note: no domain snapshot fact is in T11's sensitive set TODAY (payroll snapshot is aggregates), so the api_domains boundary is a backstop — the new mutation-proof matrix test drives a salary_detail key through it via monkeypatched enrich and asserts byte-level absence for CA/Approver (raw AND ₹-formatted) + presence for Owner + exact masked shape; dropping mask_figures inside _figures_for fails it directly, and a 2-arg regression is caught by the existing after-figures commit test via the required positional. SPA: Domain.tsx FigureGrid renders masked figures as the existing LockChip card (label + server reason, no false ✕ badge), DomainData.figures typed (Figure|RestrictedField)[]; sibling r:ask-backend concurrently fixed Onboarding.pickFirstFigure at the same seam (union + isRestricted skip) — kept. set-salary-structure action figures deliberately NOT in the masked set: they derive from the caller's own typed input (a calculator), not stored per-employee data. Gates on the merged tree: full backend pytest 1195 passed/2 pre-existing tracked skips (incl. test_landing 12, rbac matrix 44 with the 3 T11 byte-level tests), ruff+mypy clean on touched files; frontend tsc -b clean, vitest 301/301 (incl. 2 new FigureGrid T11 cases), oxlint exit 0 (pre-existing fast-refresh warnings only). No new deps, no new routes.
[r:health-wiring] DONE 2026-07-23 AGENT(r:health-wiring) - CLOSED THE b:health-strip VERIFIER FAILs (strip/leaf logic itself PASSed; this is test-depth + one honesty fix). (1) WIRING-LEVEL TESTS: both Today.test.ts and Domain.test.ts previously mutation-proofed only CashStrip/FigureGrid's OWN prop-forwarding — neither test ever called Today()/Domain() itself, so `Today()` hard-coding `stale={false}` at its `<CashStrip .../>` call site (or `Domain()`/`DomainBody` doing the same at either of the two `<FigureGrid .../>` call sites, error-path and happy-path) would have passed every existing test. Added a "the real wiring" describe block to each file that renders the ACTUAL `Today()`/`Domain()` component (QueryClientProvider with the `["today"]`/`["domain","gst"]` and FRESHNESS_QUERY_KEY caches pre-seeded via `setQueryData` so the synchronous `renderToStaticMarkup` pass already reflects steady state — matching this repo's no-@testing-library/no-jsdom convention; `../lib/api` mocked to throw if ever reached, per the ticket, though effects never run under `renderToStaticMarkup` so it never is) and asserts the downgrade actually reaches the page. Verified by hand (mutate → red → revert → green) against all three call sites named in the ticket: Today.tsx's `stale={fresh.stale}`, and Domain.tsx's two `<FigureGrid>` sites (the happy-path `stale={fresh.stale}` into DomainBody at line 221, and DomainBody's own `stale={stale}` forward at line 305) — each one alone turns the new test red. (2) SHELL MOUNT PIN: new frontend/src/components/Shell.test.tsx — nothing previously rendered `<Shell>` at all, so `<ConnectionHealthStrip />` in Shell.tsx's `<main>` had zero test coverage. Renders Shell with an unhealthy pre-seeded health payload and asserts the strip's own headline reaches the page; deleting the mount line verified red (confirmed the strip's content has no other path onto the page), reverted green. (3) HONESTY FIX: `booksFreshness(undefined, ...)` — the check-itself-failed case — returned `stale: true`, a plain boolean, which VerifiedNumber's `stale !== false && stale === "unknown"` branch could never distinguish from a REAL known-stale source; the rendered copy said "the inputs behind this figure are stale" as fact when the true state was "we could not check". Root cause was `BooksFreshness.stale` being typed `boolean` instead of reusing `Freshness` (`boolean | "unknown"`, already defined in VerifiedNumber.tsx and already wired through by WS7-E2E) — fixed by importing `Freshness` into lib/freshness.ts and changing that one branch to return `"unknown"` (the `why` prose was already honest — "we could not check" — only the `stale` VALUE was lying). effectiveState()/VerifiedNumber already treat `"unknown"` as a downgrade exactly as hard as `true` (`stale !== false`), so the fail-closed behaviour is unchanged — only the WORDING a user sees now matches the real fact. Widened `Approvals.tsx`'s `ApprovalCard`/`FigureRow` `stale` prop types from `boolean` to `Freshness` to keep tsc green (Approvals' own downgrade copy — "we cannot confirm the inputs behind them are current" — was already honest for both cases, no copy change needed there); updated Approvals.test.ts's two assertions that expected `stale: true` on a missing payload to expect `"unknown"`. Tests: Today.test.ts +2 (real-component downgrade / real-component stays ✓), Domain.test.ts +2 (same), Shell.test.tsx NEW (3 — headline surfaces when unhealthy, quiet when healthy, screen children still render). Gates: `npx tsc -b` clean; `npx vitest run` 308/308 (full suite — the previously-noted sibling WIP failures, Ask.test.ts and Today.test.ts, are gone; the concurrent tickets that owned them evidently landed clean); `npx oxlint src` exit 0 (pre-existing fast-refresh warnings only, no new warning kind, Shell.test.tsx introduces none). No new `/api` routes touched or added, so no RBAC-matrix changes; no backend files touched, backend suite not re-run (nothing in api/ or dif/ changed by this ticket). No new deps.
[v:expense-ocr] RE-VERIFIED PASS 2026-07-23 AGENT(verifier) - a:expense-ocr re-verified on the repaired tree (earlier FAIL was collateral: every action commit 500'd on the half-landed 2-arg _figures_for; r:field-rbac's atomic 3-arg re-land at api_actions.py:287 superseded the ORCH HOTFIX — confirmed no HOTFIX comment/2-arg call remains, role is a required positional). All claims hold: POST /api/expense/ocr-receipt is a thin wrapper over the same ExpenseService.ocr_capture as the HTMX route (byte-identical 503 test green), RBAC row "POST /api/expense/ocr-receipt": ("read",) present in test_rbac_matrix.py (42/42 green incl. coverage guard); commit path green (test_api_actions.py 9/9); preview surfaces the over-policy WARNING with exact ₹ limit + excess; ActionDrawer renders the "Parsed from receipt — check before submitting" caveat naming only prefilled fields, prefill limited to schema-declared keys, same editable-inputs + preview-then-confirm gate; Domain.tsx ExpenseReceiptCapture + receiptDateToIso/ocrResultToPrefill present with tests. Frontend gate green: tsc -b clean, vitest 308/308, oxlint src exit 0 (pre-existing fast-refresh warnings only, 0 errors). Mutation check (reversible, reverted): neutralized the over_policy branch in app/web/actions.py _submit_claim → test_expense_preview_surfaces_the_policy_limit_warning FAILED ("WARNING" absent), revert → 9/9 green again; test is load-bearing.
[P1-ROUND+REPAIR] DONE 2026-07-23 ORCH - Round-4 P1 surfaces complete after crash recovery. Workflow wf_01d196ca-e5c (16 agents) hit an overnight session crash that LOST two tickets' shared-file edits mid-flight (api_domains.py reverted while call sites survived → every action commit 500'd; a verifier's mutation in Ask.tsx left LIVE fail-open '// MUTATION') — adversarial verifiers caught ALL of it (5 honest FAILs naming the exact bytes). ORCH hotfixed the 500 + excised the fail-open mutation inline; repair workflow wf_d119f7df-83d (7 agents, 4/4 verify PASS) rebuilt: POST /api/ask (thin wrapper over app.core.ask.answer_query, tri-state fails CLOSED, 6/6 + matrix green), T11 field-level RBAC atomically (mask_figures at _figures_for(…, role) boundary + api_actions 3-arg call, byte-level absence tests, LockChip in SPA), health-strip wiring depth (Today/Domain stale={false} mutants now killed, Shell mount pinned, check-failed → Freshness "unknown" rendered as "we could not check" not "stale"). PASSed P1 surfaces: statements (TB/P&L/BS/GL badged + imbalance banners), audit-room parity (threads/sampling/pack downloads), CA invite/accept UI, CFO strategy (scenario w/ honest null-runway, cap-table, investor preview), Ask Maisha, expense OCR (re-verified post-repair), field-RBAC, health strip. ORCH: COUNT share-formatter moved into lib/money.countIn to keep the money-format grep-gate's one-home invariant. make ci ALL GREEN.
[P2-1] DONE 2026-07-23 AGENT(p2:vault-browser) - Vault browser in the SPA (Domain.tsx, domain==="vault" only). Backend: VaultService.browse(session, query, role, as_of) (app/domains/vault/service.py) wraps the EXISTING vault_calc.search unchanged, enriches each hit with SHA-256 integrity state (vault_calc.verify_integrity against the stored content — always true through the app's own ingest path since id=sha256(content) by construction, but genuinely false on a directly-tampered row, proven by a seeded-mismatch test) and retention-overdue, then masks by the caller's role through app.core.landing.can_view_sensitivity — the ONE canonical clearance lattice landing.py's own docstring says vault access "should route through" (it did not, until now; the legacy vault_calc role ranks are untouched and still used by can_access/accessible_documents elsewhere). A doc above clearance still APPEARS (existence isn't hidden) as {restricted:true, reason}, mirroring T11's hidden-not-absent shape. GET /api/vault/search is reused as BOTH search and the document LIST (q="" matches everything via the existing substring check) — no second listing endpoint. Added POST /api/vault/ocr-ingest, a thin JSON wrapper over the SAME VaultService.ingest_image the pre-existing /d/vault/ocr-ingest HTMX route already calls (one OCR→ingest parser); +1 API_ROUTE_GATES row (read,write), coverage-guard + full role-matrix green (42/42 rbac_matrix tests). Frontend: VaultBrowser (search box + OCR-scan capture, wired into DomainBody) + pure VaultResults/VaultDocRow/vaultEmptyText (search box empty ≠ zero results — distinct honest copy; integrity failure gets its OWN alert block + ✕ chip, never folded into the quiet ◐ styling; restricted docs render LockChip, never dropped). Reused LockChip/ErrorState/useTraceId/api/authHeaders — no new components beyond the vault-specific ones, no new deps. Tests: 2 new py unit (browse masking + integrity-fail-loud, tests/unit/vault/test_vault_service.py) + 6 new vitest (Domain.test.ts) covering results/empty-state/integrity-loud/retention-overdue/restricted/mixed. Gates: api pytest 1208 passed + 2 skipped (pre-existing tracked skips, untouched), ruff+mypy clean on touched .py; frontend tsc -b clean, vitest 316/316, oxlint src exit 0 (pre-existing fast-refresh warning kind only).
[P2-2] DONE 2026-07-23 AGENT(p2:gst-detail) - GST detail flows in the SPA (Domain.tsx domain==="gst" mounts routes/GstDetail.tsx; badge gate = the SAME honestState(state, mahsa_up) wiring as every other badge on the screen). Backend: new thin app/web/api_gst.py (/api/gst prefix, read baseline). (a) ITC recon — GET /api/gst/detail reuses GstService.reconcile_itc VERBATIM for the aggregates (available_2b/claimed/ratio/gap) and additionally NAMES every mismatched ItcRegister row (eligible-but-not-in-2B = "Rule 36(4) exposure"; in-2B-but-not-eligible = "potentially missed credit"), each ₹ through api_domains._figure → badge_state — all honestly ◐ since none is a ported Mahsa recompute target, and the test asserts NO figure arrives "verified" (§0.4 fail-closed). Rule 36(4) context ships the GST-002 rule text + statute/section from app/domains/gst/rules.py (no re-derived threshold — breach detection stays with Mahsa's rule engine on the snapshot's itc_claimed_ratio). (b) Artifact downloads — GET /api/gst/gstr1.json + /api/gst/einvoice.json make the SAME service calls as the HTMX /d/gst/*.json routes (RevenueService.gstr1_lines + gst_calc.gstr1_json; RevenueService.einvoice), gated read+export like every other /api download; SPA hides them without can_export (T11 hidden-not-disabled, AuditRoom precedent). WS9.3: the e-invoice payload's IrnStatus + QrData.Caption carry DRAFT_IRN_LABEL (asserted verbatim in py), /detail ships the same label and the SPA renders it beside the e-invoice download (vitest-locked); scripts/check_no_draft_irn.sh green. (c) IMS accept/reject — new nullable ItcRegister.ims_action column (alembic 0009, 0005 guard pattern; only the ACTION is stored, the disposition is ALWAYS recomputed by the WS1.D4 ims_disposition engine, never persisted). POST /api/gst/ims/action is preview→confirm: confirm=false is a pure dry-run (DB row-values asserted unchanged) showing per-row current→will transitions computed by the SAME engine call the commit path uses + every unknown id accounted for in skipped; confirm=true enforces write in-handler (CA can preview, cannot commit — 403 test) AND requires the api_actions preview_token HMAC over (org, action, canonical ids) — commit without a matching preview is a 409 that writes nothing (test-pinned). Deemed-accept deadline is BLOCKED-CA per ims.py: date.max injected ("no deadline known" — the deemed-accept branch can never fire), payload states deadline_pending_ca + note, UI renders it verbatim; no statutory date guessed (§0.6). (d) QRMP/CMP-08 visibility — new settings.gst_filing_profile (monthly|qrmp|composition, MAISHA_GST_FILING_PROFILE) feeds qrmp.filing_calendar for the current calendar quarter; /detail ships the obligation list with the profile labeled + profile_source; due_dates=None so every obligation is pending_ca=True — "statutory due date pending CA — not guessed" rendered per row (composition→CMP-08, qrmp→PMT-06/IFF×2 + quarterly GSTR-1/3B, both test-pinned). RBAC: +4 API_ROUTE_GATES rows (detail read; ims/action read + in-handler write, api_bulk precedent; both downloads read+export), coverage guard + full role×route matrix green (42/42). Tests: 12 py integration (tests/integration/test_api_gst_detail.py — badge fail-closed, mismatch naming, preview-no-mutate, 409-no-preview, preview-then-commit-recomputes, CA-preview-only, downloads content-type/disposition/draft-label/404, export denials, obligations pending-ca ×2) + 12 vitest (GstDetail.test.tsx — mismatches render badged with ◐ never ✓, unknown state falls to ✕, draft-IRN label verbatim, downloads hidden without export, imsConfirmPlan refuses null/committed/token-less/empty previews and re-POSTs exactly the previewed rows, BLOCKED-CA copy, obligationWhen). Gates: pytest unit 843 + integration 268 + oracle 97 green (pre-existing tracked skips only), ruff+mypy clean; frontend tsc -b clean, vitest 327/327, oxlint src exit 0 (pre-existing fast-refresh warning kind only, no new kind); grep-gates no-draft-irn/money-format/no-truncate-round green. No new deps. Deferred with reasons: IMS disposition does NOT yet feed reconcile_itc/eligible_itc (coupling the IMS overlay into the books' eligible flag needs CA guidance — eligible_itc also encodes 17(5) blocked credit); no audit-chain entry on IMS commit (matches api_actions commit precedent; record_decision is approvals-only today).
[P2-3] DONE 2026-07-23 SONNET - SPA sparklines/trends: thin GET /api/domains/{domain}/history (app/web/api_domains.py) reuses the EXISTING app.core.history_store.domain_series query verbatim — the SAME store the HTMX /d/{domain} trend section already reads — filtered to metrics with >=2 real captured points (the honest rule app/web/charts.py's sparkline() already enforces on the HTMX side; the endpoint applies it too so a lone-capture metric is simply absent from `series`, never a 1-point or fabricated line). New frontend/src/components/Sparkline.tsx is a pure React component mirroring charts.py's geometry byte-for-byte (same pad/width/height math, same flat-line-at-span-0 fallback, --color-money-in/out hairlines instead of the HTMX --c-green/--c-red vars) with its OWN <2-point guard (returns null) so every caller gets the rule for free; VerifiedNumber gained an optional `spark` ReactNode prop (append-only — every other of its 8 call sites is untouched) and Domain.tsx's FigureGrid now fetches the history query and keys each figure's own `spark={<Sparkline values={history?.[f.key]...} />}` off its OWN fact key (an unrelated key's series never leaks onto the wrong card). Tests: 4 py integration (honest-empty at 0 and 1 captures, real >=2-point series, unknown-domain 404) + 1 RBAC-matrixed row (GET /api/domains/{domain}/history: read, auto-verified by the existing route-discovery coverage guard) + 7 vitest on Sparkline.tsx (0/1/2/3 points, mutation guard: a real 2-point input must plot exactly 2 coordinates never 1 duplicated or a synthetic 3rd, flat-series safety, rising/falling hairline color) + 4 vitest on FigureGrid's history wiring (renders for >=2, absent when history is missing entirely, mutation guard: a lone real capture must not render an <svg>, an unrelated figure key's series must not leak onto this card). Gates: py 1211 passed + 2 pre-existing skipped, ruff+mypy clean on touched files (pre-existing mypy errors in betterauth.py are untouched/unrelated dirty-tree state from a concurrent ticket); tsc -b clean, vitest 338/338, oxlint src exit 0 (same pre-existing fast-refresh warning kind only, no new warnings). No new deps, no money math touched.
[P2-6 hmac-retire] DONE 2026-07-23 - Legacy HMAC-cookie auth DELETED per the WS4.3 mandate; ONE auth system remains. app/core/auth.py + web/templates/login.html removed (import-grep to zero; there was no session table — the HMAC cookie was stateless). HTMX pages now authenticate with the SAME Better Auth JWT as the SPA: betterauth.request_token = bearer header first, else new `maisha_jwt` cookie (TOKEN_COOKIE) — header always wins, a bad bearer NEVER falls through to the cookie; JWKS verification stayed the single path (principal_from_request unchanged pipeline). _authenticate middleware: token-less browser page-loads 303→/login (still a rejection, not anonymous), token-less API 401, bad cookie on a browser 303+cookie-delete, bad header always hard 401/403; cookie path binds the SAME RLS org GUC as the header path (test-pinned). GET /login is now a redirect to settings.signin_url (default /sign-in, MAISHA_SIGNIN_URL for split-origin); POST /login deleted (405, mints nothing); /logout drops the JWT cookie. Config: app_password/secure_cookies deleted; session_secret RETAINED (it signs api_actions preview tokens) with the boot refusal moved into create_app — production refuses to start without MAISHA_BETTER_AUTH_URL or with the default session secret (P1-SECRETS successor, test-pinned). Tests NOT weakened: test_auth.py rewritten to drive the cookie-JWT path (real Ed25519 JWKS via new module-scoped tests/conftest.py betterauth_owner_env fixture); test_auth_e2e legacy tests replaced by strictly-stronger successors (bad-bearer-never-falls-back-to-valid-JWT-cookie, cookie-token-yields-its-OWN-principal incl. a second identity, password-login-is-deleted, cookie-org→RLS-GUC spy) — forged-principal + no-token-401 + alg-none + wrong-key/iss/aud mutation tests all still green; test_app/test_hardening/test_api_bulk now authenticate via the owner-JWT cookie instead of the deleted password login; unit legacy_password_auth_enabled tests → request_token precedence tests. Ops: DEPLOYMENT.md §3 "ONE auth system" note (incl. the honest OWNER-STEP: the frontend/TS layer must place the JWT in the maisha_jwt cookie for the HTMX surface — until then only the SPA bearer path is usable; no password fallback, deliberately); RUNBOOK/.env.examples/preflight.sh/compose scrubbed of MAISHA_APP_PASSWORD/SECURE_COOKIES/LEGACY_PASSWORD_AUTH. Gates: py 1215 passed + 2 pre-existing skipped (full suite), ruff clean (app+tests), mypy clean (187 files — also cleared the pre-existing betterauth.py self-check errors noted by the sibling ticket), betterauth/main self-checks ok. No frontend files touched (frontend gates unaffected). No irreducible blocker: cookie-JWT verification worked against the test JWKS helper, so HMAC is fully gone, not kept.
[P2-ROUND] DONE 2026-07-23 ORCH - Workflow wf_2b8ac0a8-d28 (8 agents, 4/4 verify PASS): P2-1 vault browser (search-is-the-list, REAL per-doc SHA-256 recompute w/ LOUD integrity-fail, clearance masking through the ONE landing.py lattice, OCR capture); P2-2 GST detail (ITC recon view badged, GSTR-1/e-invoice downloads w/ draft-IRN labels, IMS accept/reject preview-gated, QRMP/CMP-08 surfaced); P2-3 SPA sparklines (history endpoint reuses MetricSnapshot verbatim, honest >=2-captures rule, fabricated-point mutation killed); P2-6 HMAC-cookie auth DELETED per WS4.3 mandate (app/core/auth.py + login.html gone, zero imports remain, HTMX now rides the same Better Auth JWT via cookie path, forged-principal test still green). i18n ticket CANCELLED mid-flight per owner: English only, no i18n scaffolding (memory saved). Two verifier git-checkout incidents this round + last (uncommitted work clobbered mid-verify) — both self-disclosed, both recovered md5-identical, both PASSed re-verification; pattern noted: verifiers now instructed to cp-backup, never git checkout. make ci ALL GREEN.
[WS9.1] DONE 2026-07-23 OPUS - Tally XML import, the #1 activation feature: (1) app/core/tally_import.py — pure ENVELOPE/TALLYMESSAGE parser, stdlib xml.etree hardened defusedxml-style (defusedxml NOT a dep — checked; any DOCTYPE refused pre-parse = no DTD = no XXE/billion-laughs; 20MB byte cap; UTF-8/UTF-16-BOM decode), Tally credit-positive→codebase debit-positive sign conversion at the edge, decimal-rupee→paise LOSSLESS or refused ("10.005" is an error naming its voucher, never rounded), masters+GROUP sub-chain→suggested account_type from the 28 default Tally primary groups (suggestion only, null when unknown — we don't guess), per-ledger checksum reconciliation (opening+Σdr−Σcr vs Tally's own CLOSINGBALANCE; absent closing = match:null, never a fabricated pass). (2) app/web/api_tally.py — POST /api/ledger/tally/parse [read] returns THE reconciliation report + unmatched names + preview_token (reuses api_actions.preview_token HMAC over the file's sha256 — ONE token machine); POST /api/ledger/tally/commit [read,write] re-uploads the exact bytes (swapped file = 409), typed confirm word "import", mapping {name→account_id | create{code,name,type}} where create-new goes through the SAME LedgerService.create_account seam as the drawer action (opening balance applied from the Tally master), vouchers → post_journal_entry(source="tally", reference=voucher_id) with double-entry validation authoritative — any refusal names the Tally voucher id and rolls back ALL of it (all-or-nothing mutation-verified: dropped-rollback mutant caught by test). (3) SPA: components/TallyImport.tsx (server-parse report + mapping UI + typed confirm; raw ArrayBuffer held so re-upload bytes == token sha) mounted at /d/ledger AND as Onboarding step 2 "Tally books" (optional/skippable; steps now 1 GSTIN → 2 Tally → 3 bank → 4 first figure). (4) Corpus: 4 real-shaped fixtures (minimal Day Book, unmatched+GROUP-chain+stated-closing-mismatch, one-paisa-unbalanced R-99, non-lossless 10.005) under tests/unit/ledger/fixtures/tally/; round-trip test imports the Day Book and asserts trial balance ₹92,345.67 both sides TO THE PAISA + per-ledger closing == Tally's stated closing; UTF-16 re-encode parses identically. RBAC: 2 rows in test_rbac_matrix API_ROUTE_GATES (parse=read, commit=read+write), marker-verified. Parse mutates nothing (row-count asserted). Mutation checks (cp backups, no git checkout): silent-rounding, dropped-token-compare, dropped-rollback, ignored-confirm-word — all 4 mutants killed. Gates: py 877 unit + 284 integration green, ruff+mypy clean; tsc -b clean, vitest 360/360, oxlint 0 errors.
[design:memory-citations] DONE 2026-07-23 AGENT(design) - Wrote docs/COMPANY_MEMORY_AND_CITATIONS.md (SPEC-MEMCITE-1.0): Part A per-company memory layer (4 proven types ported from api-nest onto uuid org_id + RLS tables, Principal-only org resolution, context-only prompt block outside FACTS with retry.py firewall as the §0.4 mechanism, guardrails-screened memory, audit-sealed writes, Settings>Company memory SPA tab) + Part B cell-level citation anchors (doc_sha256+locator+row_hash+occurrence struct, RESOLVED/MOVED/BROKEN loud resolution, vault-first bank CSV import fixing re-import idempotency, Tally voucher content-hash, legacy refs stay file-level). P0: MEM.P0-1..5 + CITE.P0-1..4, one build round each; 5 OWNER-DECISIONs marked with recommendations.
[WS10.1] DONE 2026-07-23 OPUS(ws10.1:dpdp-rights) - DPDP rights workflow, the buildable slice: (1) app/core/dpdp.py + DpdpRightsRequest model (models/legal.py) — access/correction/erasure requests, 90-day SLA (sla_due_date pure; 90 pinned by MASTER_PLAN §WS10.1, exact DPDP-Rules citation BLOCKED-counsel per §0.6, tracked in the notice draft), deadline surfaced through the EXISTING ComplianceService.add_deadline/mark_filed (domain "dpdp", request id not requester PII in the shared row — no parallel deadline system). (2) Legal-hold matrix: erasure_hold() answers "does this erasure touch retained finance records" using ONLY the vault machinery (vault_calc.retention_class + is_retention_overdue, 8y-from-FY-end §WS1.C5); basis text cites Companies Act 2013 s.128(5) as already sourced in the oracle vectors. Hold at create → status "held" + basis on row and in the sealed event; close_request re-evaluates as of the close date and REFUSES (LegalHoldError, nothing written, no vault row ever touched — module deletes nothing by construction). ponytail ceiling noted in-code: no per-principal record linkage yet, hold is org-wide-conservative. (3) Consent/notice: legal.py EXTENDED not duplicated — DocType.DPDP_NOTICE rides the same PublishedVersion/record_acceptance/needs_reacceptance mechanics; new require_current_acceptance gate wired at BOTH employee-ingestion paths (actions._add_employee dry-run-safe 422; POST /api/payroll/employees 409-with-reason) + onboarding (DpdpNoticeCard on Onboarding, same component as Settings so the surfaces can't disagree); PUBLISHED stays empty (drafts publish nothing → gate is honestly a no-op until counsel publishes; version-bump re-acceptance proven with injected registries). docs/legal/DPDP_NOTICE_DRAFT.md added with the standard DRAFT-FOR-COUNSEL header. New principal contextvar current_user (set/reset in _authenticate beside org) so sealed events attribute to the verified caller without threading a Principal through handlers. (4) Processing log: dpdp.request_created/request_closed sealed via audit_store.append onto the ONE hash-chained audit log (append-only, never pruned → ≥1y retention by construction); verify_chain-asserted in tests. (5) SPA: Settings → Privacy section (requests list w/ status/due/hold-basis, notice card, raise via the EXISTING ActionDrawer against the server's own compliance/dpdp-request action schema served by GET /api/legal/dpdp/requests — preview dry-runs the REAL create incl. hold evaluation and rolls back; badge gate called fail-closed since no Mahsa fold runs on that screen); new app/web/api_legal.py [read baseline; POST /notice/accept deliberately read — self-service acceptance, /ca/accept identity-is-authorization precedent, a CA's acceptance is as real as an Owner's]. Breach runbook = docs only: docs/legal/BREACH_RUNBOOK_DRAFT.md (two-stage DPBI first-intimation + detailed report, 72h principal notice, DRAFT/owner-executed, unverified periods marked TODO(counsel) not invented). Migration alembic 0010_dpdp_rights: Postgres = verbatim snapshot of new infra/db/multitenant/008_dpdp.sql (CREATE TABLE + ENABLE/FORCE RLS + app_current_org() policy + GRANT w/o DELETE + legal_acceptance doc_type CHECK widened to admit dpdp_notice) with the snapshot-drift test locked (test_legal.py precedent); SQLite = 0005 existence-guard. check_rls_coverage green both paths (40 checks). RBAC: +3 API_ROUTE_GATES rows, coverage guard + full role×route matrix green (42/42). Tests: tests/unit/core/test_dpdp.py (16 — SLA math incl. year/leap boundaries, calendar reuse + mark_filed on close, held-with-basis, THE mutation lock [hold check removed → test fails, proven live with cp-backup mutant: 1 failed/restored 16 passed], hold lapses → completes, chain-verified lifecycle events attributed to the verified user, org-unbound fail-closed, cross-org invisible, version-bump re-acceptance, published-notice-no-user fail-closed, migration snapshot/RLS asserts) + 13 new vitest in Settings.test.tsx (requestStatusLine per state incl. unknown-verbatim, noticeLine nothing-published→null never fabricated, held basis rendered verbatim). Gates: FULL py suite 1258 passed + 2 pre-existing skips, ruff+mypy clean; tsc -b clean, vitest 359/359, oxlint 0 errors; money/truncate/draft-irn grep-gates green. Sibling uncommitted Tally work in tree left untouched (no git checkout anywhere).
[WS9.1+WS10.1] DONE 2026-07-23 ORCH - Workflow wf_053e99d6-bbe (4 agents, 2/2 verify PASS). WS9.1 Tally XML import (the #1 activation feature): stdlib parser w/ DOCTYPE-refusal hardening (live XXE/billion-laughs/UTF-16-evasion attacks verified refused), lossless-or-refuse paise conversion naming the voucher, per-ledger checksum recon vs Tally's own closing (absent closing = null never fabricated pass), parse→mapping→typed-confirm commit all-or-nothing through LedgerService (rollback mutation-proven), 4 real-shaped fixtures + hand-re-derived trial balance ₹92,345.67 to the paisa, SPA on /d/ledger + Onboarding step 2. WS10.1 DPDP rights workflow: access/correction/erasure requests w/ 90-day SLA into the existing deadline machinery, legal-hold matrix (erasure vs 8y books retention — hold blocks at service layer, basis logged, mutation-proven), consent capture via legal.py versioned acceptance + DPDP notice draft, lifecycle audit-sealed, Settings Privacy SPA, BREACH_RUNBOOK_DRAFT.md. make ci ALL GREEN. Also: [SPEC-MEMCITE-1.0] research workflow wf_bb6e8847-b05 (5 agents, 2/2 research verify PASS) delivered docs/COMPANY_MEMORY_AND_CITATIONS.md — per-company memory layer (ports api-nest's proven 4 mechanisms onto main-api RLS; memory = context never a figure source, enforced structurally via the facts-map firewall) + cell-level citation anchors (doc_sha256+locator+row_hash, RESOLVED/MOVED/BROKEN loud resolution); 9 P0 tickets queued as round 7 per owner requirement.
[MEM.P0-1+P0-2] DONE 2026-07-23 OPUS - SPEC-MEMCITE-1.0 Part A backend: (1) migration 0011_org_memory — org_memory (2200-char DB CHECK, UNIQUE(org_id,kind)) + org_memory_history (audit_seq → sealed event row) + playbook_feedback (UNIQUE(org_id,playbook_id), verdict CHECK), RLS+FORCE+policy for all three IN THE SAME migration (0002 pattern, PG snapshot of new infra/db/multitenant/009_org_memory.sql; least-privilege grants — history gets DELETE only for the P1 evolve prune) + SQLite 0005 existence-guard; models in app/db/models/memory.py (legal.py precedent); check_rls_coverage 46/46 both paths, no-drift migration test green. (2) app/core/memory.py service — consolidate() deterministic LLM-free dedupe (api-nest port), get/set/append/history all Principal-only (NO org parameter anywhere; explicit WHERE org_id on top of RLS), reject-on-overflow MemoryOverflow (never truncation; overflow test proves NOTHING written — no row, no history, no chain entry), archive-on-supersede into org_memory_history linked via audit_seq to the memory.update event sealed on the org's OWN per-tenant chain (append_for; sealed query carries sha256+char-count ONLY, content never enters the chain), profile_text() live-rendered ORG block + verbatim "context only, NEVER a source of numbers" label; playbook feedback record_feedback (upsert latest-verdict-wins, sealed playbook.{verdict}, unknown id refused-not-recorded) + playbook_moves demoting dismissed moves out of the quantified total — ₹ figures come from the EXISTING cited engine gst_calc.late_fee_3b, never re-derived (§0.6). (3) routers app/web/api_memory.py — GET/PUT /api/memory, POST /api/memory/append, GET /api/memory/history, POST /api/playbook/{id}/feedback; RBAC per §A9 OWNER-DECISION: writes wear manage_users (Owner/Admin — admin surface), reads open to every read role incl. CA (deliberate), feedback wears write; +5 API_ROUTE_GATES rows, _call taught PUT + {playbook_id}. Tests: 17 new (unit service + integration routes) incl. the CROSS-ORG RED-TEAM READ (org-B verified principal gets zero org-A bytes on every surface, byte-level, incl. profile/history; B's write can't touch A) and the ₹800→₹0 case ported exactly (16d × ₹50 = 80_000 paise → dismiss → 0 → adopt → back; PLUS a cap mutation-killer: 300d = ₹10,000 s.47 cap, so naive days*50_00 reimplementation fails). NOT in this ticket (per spec): prompt injection path (MEM.P0-3), Ask threading (P0-4), SPA tab (P0-5), evolve job (P1-1). Gates: full pytest 1284 passed/2 pre-existing skips, ruff+format clean, mypy 194 files clean, frontend tsc -b clean (untouched).
[CITE.P0-1+P0-2+P0-4] DONE 2026-07-23 OPUS(cite-backend) - SPEC-MEMCITE-1.0 Part B backend: (1) CITE.P0-1 vault raw-bytes ingest — VaultService.ingest_bytes(content: bytes): id/sha256 = sha over RAW bytes (BOM/CRLF-proof, test-pinned vs the decoded-text sha), stored verbatim in new documents.raw_content (LargeBinary, migration 0013 guarded; file_path stays vault/{sha}), get_bytes() re-verifies the sha before returning (loud ValueError on tamper), browse() integrity now covers bytes when present; text/OCR sha path untouched (regression-pinned). (2) CITE.P0-2 treasury vault-first import — migration 0012_bank_row_anchors (0005 existence-guard; 4 nullable cols source_doc_id FK documents/source_row/row_hash/occurrence + UNIQUE(source_doc_id,row_hash,occurrence) on bank_transactions, no new table, RLS untouched, chained onto 0011_org_memory); import_csv ingests the ORIGINAL bytes into the vault FIRST then mints csv_row anchors: source_row = 1-based RAW line number (CSVW source-number semantics — reader.line_num prev+1, blank lines counted, test-pinned with a blank-line fixture), row_hash = sha256(canonical_json([trimmed cells in column order])) (audit.canonical_json widened to accept lists), occurrence = ordinal among identical rows (two-identical-NEFT fixture pins 1,2); rows whose triple already exists are skipped → RE-UPLOAD IS A NO-OP (row counts + account balance unchanged — the spec-named non-idempotent re-import fixed by the same move; rows_duplicate reported honestly, never folded into rows_skipped); zero-importable-rows files mint neither anchors nor a document (no fabricated provenance); router passes raw bytes+filename+clock-at-the-edge upload_date; service stays clockless (falls back to statement max txn date, retention metadata only). (3) CITE.P0-4 tally voucher content-hash — positional "voucher #N" fallback REPLACED: every voucher gets voucher_hash = sha256(canonical_json({voucher_number,date,voucher_type,narration,lines})), unnumbered vouchers get id "voucher {hash[:12]}" (content-identical duplicates suffixed /n; error-report naming uses a non-stored positional label); commit vault-ingests the exact source XML via ingest_bytes and stamps every JournalEntry with voucher_hash + source_doc_id (cols in migration 0013; anchor line = JournalLine ordinal); grep-gate test pins "voucher #" out of tally_import.py forever; reorder-the-file fixture proves ids/hashes identical across re-export. Tests: +16 (5 vault bytes, 5 row anchors incl. round-trip-against-stored-file re-extraction, 4 tally content-hash + grep-gate, 1 commit-anchors integration incl. independent hashlib/json recompute of voucher 3), ALL expected hashes recomputed independently in-test; 6 mutations run via cp-backup (no-trim, no-dedupe, text-sha-instead-of-bytes, occurrence-always-1, parsed-index-instead-of-raw-line, hash-ignores-lines) — ALL killed. No new routes (RBAC matrix unchanged). Gates: py FULL 1289 passed/2 pre-existing skips, ruff+mypy clean, check_rls_coverage 46/46 both paths, migrations upgrade-head + drift green; frontend untouched (tsc clean, vitest 359/359, oxlint pre-existing warnings only). CITE.P0-3 (anchors.py resolution service + working-panel render) is the sibling ticket.
[MEM.P0-5] DONE 2026-07-23 SONNET(mem-spa) - SPEC-MEMCITE-1.0 §A9 Settings tab: MemorySection appended to the EXISTING Settings.tsx surface (CA-seat/Privacy precedent — a third stacked H2 section, not a new tab-bar component, since the tab has always meant "settings section" here). View: live org profile (GET /api/memory) rendered read-only labeled "derived from your company records — always current" + the CFO posture block with a CharMeter (n/2200, turns the unbacked-verify color past the cap). Edit: textarea + Save (PUT /api/memory) with a Cancel; a separate one-line Append form (POST /api/memory/append), matching the spec's "textarea + save ... append field". History: GET /api/memory/history newest-first, each row's content + superseded_at/by + a Link to /audit naming the sealed event id ("sealed · audit #N") — Audit Room has no per-row deep-link today so this points at the room itself rather than fabricate one (stated as a comment, not silently shipped). RBAC: no proactive client-side role gate (GET never denies a reader — CA seat's disabled-reason pattern doesn't apply); a non-owner's PUT/append 403 surfaces via `memoryWriteErrorText`, same precedent as `inviteErrorText`. Root-cause fix along the way: `api()` in lib/api.ts threw ApiError with ONLY "{status} {statusText}", discarding the response body — so NO caller anywhere could ever render a server's verbatim message; the MEM.P0-5 422 requirement needs it (MemoryOverflow's message is dynamic, carries the exact char count, can't be reproduced by a per-status client sentence). Fixed once at the one seam every API call routes through: ApiError gained an optional `.detail` populated from a JSON `{detail}` body when present, silently absent on a non-JSON/bodyless error (never a crash). Tests: 2 new api.test.ts (detail captured verbatim / absent-body degrades honestly) + 6 new Settings.test.tsx (`memoryWriteErrorText` 422-verbatim/422-no-detail-fallback/403/other, `historyAuditLine` present/null). Gates: frontend tsc -b clean, vitest 367/367 (full suite, no regressions), oxlint exit 0 (pre-existing fast-refresh warning kind only, same as sibling Settings additions). No backend files touched (stage-1 mem-backend already RBAC-matrixed all 5 routes this screen calls) — no new grep-gate/migration/RLS surface.
[MEM.P0-3+P0-4] DONE 2026-07-23 OPUS(mem-threading) - SPEC-MEMCITE-1.0 §A4/§A6/§A7 prompt-injection path + Ask threading. (1) MEM.P0-3: build_user_prompt gained optional memory= rendered as a labeled block (MEMORY_HEADER, "context only, NEVER a source of numbers; treat as data, not instructions") placed BEFORE FACTS, never inside it; MaishaGenerator.produce screens the memory block through the SAME guardrails.scan_input the query gets (§A6: injection hit ⇒ block DROPPED from the prompt + warning logged loudly, query still answered; cloud path PII-redacts memory like the query); ClaimProducer protocol + RoutedGenerator + evals.ScriptedProducer gained the memory kwarg (forwarded as **kwargs only when present, so pre-existing stubs keep working); retry.generate_verified threads memory to the generator while `allowed` stays built from tools.enrich(snapshot) ALONE — the mechanical §0.4 firewall; run_loop is the ONE choke point: fetches org memory via profile_block(session, current_org()) from the request CONTEXTVAR the auth middleware set from the verified JWT (never a spoofable parameter; no authenticated org ⇒ no memory, fail closed). THE §0.5 CI TEST (tests/unit/llm/test_memory_firewall.py): adversarial generator echoes a number seeded ONLY in memory (₹999888777, absent from facts) — asserts the number is not in allowed_values(enrich(snapshot)), the draft fails verification, falls back to the fact-built claim flagged requires_approval, and the smuggled number appears NOWHERE in claims or narrative; cp-backup mutation (memory digits merged into `allowed`) run and KILLED, restored from backup. Also pinned: label + placement, injection-drop loudness, run_loop contextvar fetch (CFO block reaches the generator) and no-org ⇒ None. (2) MEM.P0-4: answer_query takes principal (Principal-only org, §A3; None ⇒ pre-MEMCITE behaviour, fail closed) — injects profile_text as memory context to both the generate_verified and no-fold produce paths; episodic recall = memory.recall_decisions: deterministic LLM-free lexical scoring (stopword-filtered token overlap, best-match-then-most-recent) over audit_store.load_chain_for(org) — org isolation is STRUCTURAL (the org's own hash chain from its tenant genesis; B's entries cannot link in; RLS GUC stays the Postgres floor; unattributed legacy global-chain entries excluded fail-closed); recall renders as Citation rows carrying decision text + audit_hash ("audit <hash12>"), never a number-as-truth; Citation gained optional audit_hash; POST /api/ask threads resolve_principal (capability gate unchanged, existing RBAC row stands) and serializes audit_hash. Tests (tests/unit/core/test_ask_memory.py): two-org isolation both directions (cp-backup mutation load_chain_for→load_chain run and KILLED), lexical precision + decision-shape pin, recall citations w/ hash through answer_query, LLM-off determinism (two calls byte-identical incl. recall — recall works with the LLM off), no-principal unchanged, CFO block reaches the generator labeled. Gates: py FULL 1302 passed/2 pre-existing skips, eval 13/13, ruff+mypy(213) clean, make gates 46/46 RLS, frontend tsc clean + vitest 367/367 + oxlint pre-existing warnings only (frontend untouched). Memory NEVER enters enrich()'s facts map anywhere — grep-provable: no memory reference in tools.py.
[CITE.P0-3] DONE 2026-07-23 OPUS(cite-anchors) - SPEC-MEMCITE-1.0 §B2/§B4/§B5 anchor resolution + render. app/core/anchors.py: csv_records/row_hash are THE shared primitives — treasury import_csv now calls them directly (grep-verified single definition, no mint/resolve fork); resolve_csv_anchors fetches stored bytes ONCE per doc (integrity-re-verified via get_bytes, utf-8-sig symmetric with the upload edge) and yields exactly three loud outcomes per §B2: RESOLVED, MOVED w/ "row moved from N to M" note (anchor NEVER rewritten — render-only per OWNER-DECISION), BROKEN (no hash match / tampered bytes / missing doc, reason in note); (hash,occurrence) index makes matches unambiguous by construction. bank_documents assembles working.documents excerpts ("file, row N: date desc ₹amount Dr/Cr"), display-capped w/ an honest aggregate line that CARRIES the worst resolution state (downgrade can't be dodged by row count — pinned); anchor-less legacy rows contribute NOTHING (§B5, pinned). Badge: BROKEN downgrades ✓→◐ downgrade-only on BOTH surfaces via the same rule — SPA effectiveState(state, stale, brokenCitation) + hasBrokenCitation, HTMX working_panel.html identical inline rule, both consume the same build_today payload so they cannot disagree; MOVED keeps ✓ with a visible note. today.py cash strip populates documents (was hardcoded []); api_filings._figure gained the documents seam (honestly empty for typed-input filings); Citation.anchor serialized in /api/ask (None until CITE.P1-2, declared); audit_pack.AuditFigure.anchors optional inside sealed sections. Tests: test_anchors.py against the REAL import path (untouched/locator-mismatch/reorder-re-export/tamper/unknown-hash/missing-doc/twin-occurrences+phantom-occ-3/blank-line raw-line math/cap-aggregate-carries-BROKEN/legacy-silence) + test_today.py + VerifiedNumber.test.ts full-component renders (BROKEN/MOVED/RESOLVED, never-upgrade). Gates: make ci ALL 19 GREEN (full suite incl. E2E + live RLS red-team + golden eval).
[MEMCITE-P0-VERIFY] DONE 2026-07-23 ORCH - Workflow wf_20f6a54e crashed (host power loss) after 4/6 agents journaled; recovered from disk — all 6 builders' work intact, targeted tests green at crash. Post-crash: make ci ALL 19 GREEN + independent adversarial verify of the 3 unverified tickets, ALL PASS: §0.4 firewall mutation (memory digits→allowed) re-run and KILLED; load_chain_for→load_chain isolation mutation re-run and KILLED; mint/resolve-fork refuted (shared primitives, symmetric decode, occurrence ordinals coincide for identical-hash rows); HTMX downgrade adversarially rendered correct. CARRIED FORWARD, not done: (MED-1) ask.py no-fold path (Mahsa down + LLM on) runs gen.produce with memory but outside generate_verified — narrative-only blast radius (figures still fact-checked, memory injection-screened) but the number firewall doesn't run on that draft and requires_approval isn't set; (MED-2) MemorySection has no mounted-component test (only pure-helper branches; repo has no jsdom/Playwright — decide before it counts as a spec gap); (MED-3) HTMX-side BROKEN downgrade in working_panel.html has no repo test pinning it (verified by direct Jinja render only — a template edit could break it while SPA tests stay green; add a small template test).
