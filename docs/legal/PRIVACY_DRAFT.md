> **DRAFT FOR COUNSEL REVIEW — NOT FINAL, NOT LEGAL ADVICE.**
> Generated by an engineering agent for ticket §WS10.4 (versioning + acceptance-logging
> machinery). This is a structural placeholder, not a document to publish. No clause below
> should be read as an assertion of compliance with DPDP Act 2023, its rules, or any other law.
> `TODO(counsel)` markers are points requiring an actual legal determination. Where a DPDP-related
> mechanism is described as "engineered" (consent capture, rights workflow, breach runbook), that
> refers to work tracked separately under MASTER_PLAN §WS10.1 — this document does not itself
> assert those mechanisms are complete, correct, or sufficient.

# Privacy Policy — DRAFT

**Document type:** `privacy`
**Draft version:** `v0-draft` *(not yet published)*
**Last edited:** 2026-07-21 (by engineering agent, pending counsel review)

## 0. Versioning (mechanism, not legal content)

Same mechanism as the ToS (see `TOS_DRAFT.md` §0): a published version is a `legal_document`
row (`doc_type="privacy"`); acceptance is a `legal_acceptance` row; a version bump requires
re-acceptance before continued use, tracked per-user.

## 1. What personal data is processed

`TODO(counsel + product)`: enumerate categories actually collected (account/contact details,
company financial records, payroll/employee PII ingested by the payroll domain, uploaded
documents/OCR text, usage logs). Do not describe categories that are not actually collected;
the categories must match the real schema in `api/app/db/models/`.

## 2. Purpose and legal basis for processing

`TODO(counsel)`: map each category in §1 to a legal basis under applicable law (e.g. DPDP Act
2023 consent / legitimate use grounds). No basis is asserted here — that determination belongs
to counsel and to the consent-capture engineering under MASTER_PLAN §WS10.1.

## 3. Sub-processors

See `DPA_DRAFT.md` §3 (sub-processor annex) for the current list (Supabase, the LLM provider,
email delivery) and their roles. This policy should link to that annex rather than duplicate it,
so the two never drift apart.

## 4. Data subject rights

`TODO(counsel)`: describe the access/correction/erasure rights workflow and its SLA once
MASTER_PLAN §WS10.1 (rights workflow, 90-day SLA tracking) is implemented and reviewed. Do not
assert a specific SLA number here unless it matches what is actually built and enforced.

## 5. Retention

`TODO(counsel)`: the retention periods actually enforced (e.g. the legal-hold matrix balancing
erasure requests against statutory books-and-records retention, MASTER_PLAN §WS10.1). No
retention period is asserted here without that matrix existing and being cited.

## 6. Security measures

`TODO(counsel + security)`: describe measures actually in place (encryption at rest/in
transit, RBAC per `app.core.rbac`, hash-chained audit log, RLS-scoped multi-tenancy per
`infra/db/multitenant/`). Do not describe a control that is not actually implemented and
verified — an inaccurate security claim in a privacy policy is itself a liability.

## 7. International transfers

`TODO(counsel)`: dependent on where sub-processors (§3 / DPA annex) actually host data.

## 8. Children's data

`TODO(counsel)`: state whether the product is offered to minors (expected: no — B2B startup
tool) and the resulting policy position.

## 9. Breach notification

Cross-reference the breach runbook under MASTER_PLAN §WS10.1 (two-stage DPBI + 72-hour
principal notice) once that runbook exists and has been tabletop-tested. `TODO(counsel)`
before asserting a specific notification timeline to data subjects in this policy.

## 10. Changes to this policy

Governed by the versioning mechanism in §0.

---
*Generated as scaffolding for ticket §WS10.4. Replace every `TODO(counsel)` and remove this
notice block only after actual counsel review and sign-off.*
