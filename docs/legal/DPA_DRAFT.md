> **DRAFT FOR COUNSEL REVIEW — NOT FINAL, NOT LEGAL ADVICE.**
> Generated by an engineering agent for ticket §WS10.4 (versioning + acceptance-logging
> machinery). This is a structural placeholder, not a document to publish or execute with a
> customer. No clause below asserts DPA-readiness or compliance with any law. `TODO(counsel)`
> markers require an actual legal determination. The sub-processor list in §3 is drawn from
> what the codebase actually references today (2026-07-21) — it is a snapshot for counsel to
> verify against the real production deployment, not a confirmed-live inventory.

# Data Processing Addendum — DRAFT

**Document type:** `dpa`
**Draft version:** `v0-draft` *(not yet published)*
**Last edited:** 2026-07-21 (by engineering agent, pending counsel review)

## 0. Versioning (mechanism, not legal content)

Same mechanism as the ToS/Privacy docs: a published version is a `legal_document` row
(`doc_type="dpa"`); acceptance is logged per-customer-signatory in `legal_acceptance`; a
version bump (e.g. a sub-processor added/removed) requires re-acceptance.

## 1. Roles

`TODO(counsel)`: confirm controller/processor (or fiduciary/processor, under DPDP
terminology) characterization for each data category. Not asserted here.

## 2. Scope of processing

`TODO(counsel + product)`: subject matter, duration, nature/purpose, data categories, data
subject categories — must mirror the Privacy Policy §1 exactly so the two documents never
describe different processing.

## 3. Sub-processor annex

This is a **code-derived snapshot**, not a confirmed contractual list. Counsel and ops must
verify each row against the actual production deployment before this DPA is executed.

| # | Sub-processor | Role | Basis for inclusion (as of 2026-07-21) | Status |
|---|---|---|---|---|
| 1 | **Supabase** | Planned Postgres hosting + auth provider | Referenced as the target identity/DB provider in `infra/db/multitenant/001_tenancy.sql` and `api/app/core/identity.py` ("a future Supabase-Auth ... provider"); the current identity layer is a **local** provider (stdlib scrypt + TOTP), not yet Supabase. | `TODO(counsel)`: confirm whether Supabase is actually live in production before naming it as a contracted sub-processor. If it is not yet deployed, it should be listed as a **planned** sub-processor with an activation-notice clause, not a current one. |
| 2 | **LLM provider (Anthropic, "Claude")** | Drafting-layer text/number generation, gated so no unverified figure reaches a user (`app.core.mahsa_client`, `app.llm.retry`) | `api/app/config.py` (`claude_model`, `claude_base_url = https://api.anthropic.com`); used only when `llm_provider` is explicitly set to `claude` — local Ollama is the default/primary per `CLAUDE.md` §7. | `TODO(counsel)`: confirm current deployment default (Ollama-only vs. Claude-fallback-enabled) and whether customer data (vs. only computed facts) is ever sent in a prompt — see `app.llm.guardrails` for the injection/PII-redaction control that bears on this. |
| 3 | **Email delivery** | Transactional/notification email (e.g. daily CFO brief, compliance reminders) | `api/app/core/email/transport.py`: SMTP via `aiosmtplib`, MailHog in local dev; production transport is a "self-hosted SMTP" per the code comment — **no named third-party ESP (e.g. SES/SendGrid/Postmark) is currently wired.** | `TODO(counsel + ops)`: name the actual production SMTP relay/provider before this annex is finalized. If self-hosted, state that explicitly (no sub-processor for delivery, only for the relay's own upstream network if applicable). |

Rules for maintaining this annex (mechanism, not legal content):
- Adding, removing, or changing the *role* of a sub-processor is a version bump of this
  document (§0) and should trigger a customer notice per whatever advance-notice clause
  counsel drafts in §4.
- Do not add a sub-processor to this table without a corresponding code reference (or an
  explicit product/ops confirmation) — this table must stay traceable, per §0.6's spirit that
  a legal/statutory claim needs a citation, not a guess.

## 4. Sub-processor change notice

`TODO(counsel)`: advance-notice period and objection mechanism for a customer when a
sub-processor is added.

## 5. Security measures

Cross-reference the Privacy Policy §6; do not duplicate and risk drift.

## 6. International data transfer

`TODO(counsel)`: dependent on final hosting location(s) of §3's sub-processors.

## 7. Assistance with data subject rights / breach notification

Cross-reference Privacy Policy §§4, 9 and MASTER_PLAN §WS10.1 (rights workflow, breach
runbook). `TODO(counsel)` for the contractual assistance/timeline commitments to the customer
(distinct from the regulator/data-subject-facing timelines in the Privacy Policy).

## 8. Term and deletion/return of data on termination

`TODO(counsel)`, balanced against the legal-hold matrix (erasure vs. statutory books-and-records
retention) tracked under MASTER_PLAN §WS10.1.

---
*Generated as scaffolding for ticket §WS10.4. Replace every `TODO(counsel)`, verify §3 against
the real production deployment, and remove this notice block only after actual counsel review
and sign-off.*
